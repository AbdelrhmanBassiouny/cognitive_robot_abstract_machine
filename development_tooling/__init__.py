"""
Development-workflow tooling for this repository - the Python behind the ``.claude/``
stack tooling and plan dashboards, importable from the repository root with no
installation step.

..note:: This package is repository tooling, not part of the robot stack: it is never
    published and is not part of any default install. The ``pyproject.toml`` inside
    this directory exists only for optional installation (e.g. by a CI workflow
    running outside a checkout of the repository root).
"""
