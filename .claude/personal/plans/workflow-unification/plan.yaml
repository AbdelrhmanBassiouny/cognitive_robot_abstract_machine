schema_version: 1
id: workflow-unification
title: "Workflow & tooling unification (stack workflow, personal notes, dashboards)"
description: >
  Unify the distributed workflow tooling into four clean homes: reviewed machinery on
  main via cram2 (stack tooling, ROUTINE.md doctrine, setup skill — stacked on PR #101),
  personal data/config on claude/personal-notes only, one auto-publishing Pages site for
  all plan dashboards (plans are the single surface; the stack board is retired in favor
  of GitHub's native stack map, per the one-dashboard decision of 2026-07-31), and no
  scheduled LLM Routine: stack mechanics come from GitHub's native stacked-PRs preview,
  deterministic duties move to a plain scheduled Action, and judgment work (cascade
  conflicts, red CI after restacks) runs in on-demand sessions. Then retire the
  superseded session-hooks and stack-workflow-tooling branches. Portability rule
  throughout: runnable on repos other than cram2 (native or fork-overlay install, no
  hardcoded repo names outside config defaults; hand-rolled restack derivation survives
  as the fallback for repos without the stacks preview).
default_repository: AbdelrhmanBassiouny/cognitive_robot_abstract_machine
tracking_issue: 102

waves:
  - id: immediate
    name: "Immediate cleanups"
    description: "Independent of upstream review; can start now."
  - id: upstream
    name: "Tooling to main via cram2"
    description: "The stacked PRs on top of PR #101; land through cram2 review."
  - id: cutover
    name: "Cutover & retirement"
    description: "After the upstream wave reaches cram2/main and fork main fast-forwards."

tracks:
  - id: personal-data
    name: "Personal-notes slimming"
    wave: immediate
  - id: stack-tooling
    name: "Stack tooling & setup skill"
    wave: upstream
  - id: dashboards
    name: "Dashboard unification"
    wave: upstream
  - id: cutover
    name: "Cutover & retirements"
    wave: cutover

items:
  - id: eql-roadmap-migration
    branch: null
    title: "Migrate the EQL living roadmap out of cram-notes.md into its own plan"
    track: personal-data
    depends_on: []
    status: done
    notes: >
      Done. Ran /plan-create eql-verbalization from the cram-notes living-roadmap
      section; folded the P1-P3 review-round logs into the three pr-progress files;
      slimmed cram-notes.md to pure conventions; fixed the stale "hook lives on
      claude/session-hooks" reference; deleted superseded rdr-roadmap.md. No PR —
      personal-notes branch only.
  - id: session-hooks-retirement
    branch: null
    title: "Tag and delete the superseded claude/session-hooks branch"
    track: personal-data
    depends_on: []
    status: done
    notes: >
      Done. claude/session-hooks's tip (ece182c5) was a literal git merge-base
      --is-ancestor of main (via PR #432), so nothing was lost by skipping the tag:
      every commit on the branch already remains reachable from main. The
      tag-push/branch-delete git operations turned out to be unavailable from a Claude
      Code session (git proxy returns 403 on tag pushes and branch deletes; no GitHub
      MCP tool substitutes) - documented as a standing platform constraint in
      roadmap.md's 2026-07-29 addendum, and flagged on the sibling item
      tooling-branch-retirement, which has the same shape. The user deleted
      claude/session-hooks directly (outside the harness) once this was confirmed
      safe. Remaining cleanup: origin/claude/push-scope-test-zsq7jc, a throwaway
      branch pushed while diagnosing the tooling gap (identical content to main),
      still needs deleting the same way.
  - id: personal-settings-sync
    branch: claude/local-settings-dashboard-sync-8sx0tf
    title: "Sync personal .claude/settings.local.json off the personal-notes branch"
    pull_request_number: 109
    track: personal-data
    depends_on: []
    status: done
    session: https://claude.ai/code/session_01XkWmfMzYYAaCsgyrHDuoKn
    notes: >
      Extends the session-start hook so the notes branch also carries this project's
      local Claude Code settings (.claude/personal/settings.local.json ->
      .claude/settings.local.json, gitignored), plus save-personal-settings.sh for the
      write half; seeded with a rule allowing the Artifact tool without prompting. A
      copy that is skipped whenever the local file changed since the last sync, since
      Claude Code writes its own "don't ask again" grants into that same file - the
      baseline is a hash stamp in .claude/.personal-settings-sync-hash. This widens the
      track beyond its "slimming" name: personal-data is now personal-notes data and
      config generally, which is what the plan's target architecture already says that
      branch holds. Independent of the #101 stack - branched off fork main. Same session
      also added the personal-notes rule that any session changing a plan's data
      republishes that plan's dashboard in the same turn (notes-branch only, no PR).
      STALLED 2026-07-31 to 2026-08-03 and RESOLVED by
      https://claude.ai/code/session_01XkWmfMzYYAaCsgyrHDuoKn (commit 5281e3f3), on two
      blockers neither of which this entry had recorded - see blockers below and the
      2026-08-03 roadmap entry. The overlap warning above resolved itself: #107's and
      #110's files (resolve-personal-notes-config.sh, session-start.sh) auto-merged
      clean; what actually conflicted was a duplicated test fixture against main. PR
      #109 is a draft again, labelled cram2-link-sent only, mergeable against main
      9b090fc1, all three review threads resolved; 36 tests pass under
      .claude/hooks/tests (main's 28 plus this module's 8).
    blockers:
      - "Both cleared 2026-08-03."
      - >
        (1) Merge conflict against main since the 2026-07-31 fast-forward -
        mergeable_state dirty, labelled needs-resolution - in
        .claude/hooks/README.md, tests/conftest.py and tests/test_save_plan_sh.py.
        Root cause was a duplicated artifact, not drifting text: main extracted the
        hook-test scratch repository as ScratchRepository (via #101's review round)
        while this branch independently extracted the same thing as
        ScratchProject. Resolved by adopting main's superset and deleting
        ScratchProject, so both test files are now byte-identical to main.
      - >
        (2) Three unresolved review threads on test_personal_settings_sync.py
        asking for the inline JSON literals to move into separate files -
        AGENTS.md's no-inline-snippets rule - now three .json files under
        tests/fixtures/. Note for anyone replying on this PR: those threads belong
        to an unsubmitted PENDING review, and GitHub allows one pending review per
        user, so inline replies 422 until it is submitted; the resolution was
        explained in a PR-level comment instead.

  - id: setup-personal-notes-pr101
    title: "Land /setup-personal-notes (PR #101) — the foundation the stack PRs base on"
    branch: claude/patch-pr-rheubx
    pull_request_number: 101
    track: stack-tooling
    depends_on: []
    status: done
    session: https://claude.ai/code/session_01GR1i6dZb8goJpCZBMCnVLM
    notes: >
      Done - merged into fork main 2026-07-31T10:36:43Z (head 90f69f56, by
      AbdelrhmanBassiouny); .claude/skills/setup-personal-notes/ and
      .claude/hooks/check-setup.sh are both on main, verified by git cat-file rather
      than from the merge notification alone. Pre-existing PR, not authored by this
      plan; tracked because everything in the upstream wave stacks on its branch,
      and stacking never waited for the merge. Final content beyond the original
      patch: the hooks README rewritten as a step-based guide (378 -> ~140 lines)
      with the worked example surfaced at the top, a skill step checking the
      merged/bug/in-review labels exist in the fork, and a review round that moved
      the duplicated hook-test scratch repository onto a shared ScratchRepository
      dataclass + conftest fixture and made the check-setup report typed
      (SetupCheck/CheckStatus StrEnums, CheckResult/SetupReport dataclasses).
      Follow-up agreed and deliberately NOT in this PR: extract
      .claude/hooks/setup-personal-notes.sh --remote <name-or-url> [--starter-notes]
      taking over skill steps 4-7 and 9 so the mechanical setup runs with no session
      at all, leaving the skill only the remote-ownership decision and the label
      check. Kickoff prompt was handed to the user in the session; it is now
      unblocked, since it required this PR merged first.
      Two things worth carrying forward: three merge commits authored as
      "Claude <noreply@anthropic.com>" landed on this branch from outside any
      session (against AGENTS.md's Version Control rule) and were left unrewritten
      rather than force-pushing shared history; and the robotics CI jobs flaked
      repeatedly here on tests this .claude/-only PR cannot reach
      (test_world_sim_state_sync, test_attached_self_collision_avoid_stick), which
      the user instructed be ignored rather than chased.

  - id: stack-tooling-on-main
    branch: claude/stack-tooling-on-main
    title: "PR 1: stack workflow tooling on main (.claude/stack/)"
    pull_request_number: 106
    track: stack-tooling
    depends_on: [setup-personal-notes-pr101]
    status: done
    session: https://claude.ai/code/session_01Y9egTHHu5RTXmkFL4SwXM9
    notes: >
      Implemented as draft PR #106 (2026-07-29): stack.py ported to .claude/stack/ minus the
      round-robin/WIP-cap subsystem, per-user config layering from the personal-notes branch,
      canonical ROUTINE.md + 1-page README, tests via ScratchRepository; restack.js and board
      rendering not ported (board deferred to PR 3/PR 4 as planned). Landed as plain scripts — the
      dev-tooling Python package is created in PR 3 instead (see dev-tooling-python-package).
      Self-reviewed and un-drafted since: open and ready, so it unblocks its dependents by the
      plan's normal open-and-ready rule. Now also the parent of #107, which was restacked onto it
      (2026-07-30) so the upstream wave is a linear chain rather than two siblings on #101.
      Re-scoped 2026-07-31 (native-stacks pivot, user-approved): before review goes deep, add one
      trim commit — (a) read stack structure from the stacks REST API when the PR is a stack member,
      keeping PR-base derivation as the fallback for preview-less repos; (b) ROUTINE.md merge
      doctrine becomes merge-async + poll only, never the classic merge API or the MCP merge tool on
      a stack member, with stack base.sha lagging the trunk head as the needs-cascade signal; (c)
      Phase 1 REPARENT must special-case stack members — PATCH base 422s on them ("Cannot change the
      base branch because the pull request is part of a stack"), so reparent via unstack → PATCH
      base → re-stack, or leave it to gh stack sync / the UI Rebase-stack button. (c) is
      load-bearing before the next cram2 landing of any stacked branch: stack #112 already exists on
      the fork. Upgraded to a cut the same day (user decision, "decision 11" in the roadmap): the
      restack subsystem (restack-plan, next, status/structure derivation) is removed from the PR
      entirely rather than trimmed — native stacks own structure, gh stack / the Rebase-stack button
      own cascading, and the preview-less-repo fallback is YAGNI (the preview is account-wide; the
      old tooling branch remains as a tagged archive). The PR keeps: ROUTINE.md rewritten around
      native mechanics (incl. the Phase-1 reparent special case and merge-async doctrine),
      stack.toml + per-user config layering, the label/cram2-link helpers, a small reparent-recovery
      script, and tests. Re-scope instruction posted on the PR itself. Assessed 2026-08-02 against
      #110 (see roadmap): the two are split correctly, but three artifacts had been built twice.
      This PR keeps all of them by landing first. Shipped here (36eda87a): `stack.py remotes`
      becomes `configuration`, printing one `field<TAB>value` line per Configuration field instead
      of four hand-written hyphenated keys, with `upstream_setup_command` moved onto Configuration
      and omitted when the remote already exists. That makes this PR's command surface and #110's
      independently-built `config` one surface, so #110's rebase deletes the resolution internals
      only. 286 CI tests pass, was 283. Description updated to match. Re-scoped again 2026-08-02
      (user-approved, session_01BByQFT6he8xf5qvBWtyUPV): the doctrine becomes a skill and the
      recipes become subcommands. ROUTINE.md is deleted;
      .claude/skills/stacked-pr-maintenance/SKILL.md carries the doctrine, is invocable as
      /stacked-pr-maintenance from any session, and is what POINTER.md's registered block now
      resolves and runs. Context resolution is arguments first (fork=/upstream=, passed through as
      configuration --fork/--upstream, so nothing is inferred when both are given), then stack.py
      configuration, then - only interactively - an AskUserQuestion whose answer is persisted to
      .claude/personal/stack.toml on the personal-notes branch; --non-interactive turns that
      question into a stop, which is what a scheduled run needs given its own rule against opening a
      discussion. Landed as one commit on this branch (e20b0bb4) rather than as a child PR: every
      path it touches is absent from main, so by the scope rule nothing standalone would have
      remained, and shipping a ROUTINE.md that its successor deletes would change the live doctrine
      twice - the pointer resolves origin/main first. Four new stack.py subcommands, each replacing
      a rule a reader had to remember: labels (the complete set a label write must send, computed
      from the current labels), preflight (refuses a wrong checked-out branch, a mismatched refspec,
      a non-fork destination, and the child-becomes-ancestor-of-parent false merge; exit 5),
      promotion-link (owns the compare URL's encoding and length limit), and reparents/landed (phase
      1 derived from git ancestry rather than pull-request state). 331 tests pass, was 294. An MCP
      server was considered and rejected: the raw PATCH base change 403s on the session git-proxy
      credential, so a local server could not own the riskiest operation, and subcommands survive
      routine-cutover's no-LLM endgame. Decision 11's half-applied cut is deliberately not
      completed: print_next and print_restack_plan are kept, because phases 2 and 3 call them and
      they are the derivation half of the scripts-over-prose choice made here. print_restack_plan's
      docstring no longer names "the restack workflow's args". One review thread stays open by
      design - stack.toml:23, the remote inference. The skill answers its interactive half; the
      remaining caller is a non-interactive run with no arguments on an unconfigured checkout, so
      the ~120-line deletion is still #110's, alongside the setup that writes fork_repository.
      Replied on the thread and on #110 rather than resolving it. stack.py:84 (from_remote_url
      parses without validating) also remains open and dies with the same deletion. Review round
      2026-08-02 (25 comments) applied in 8b7435bb; three decisions were the user's, each taken
      after the options were costed. (1) GitHub marks a pull request merged - not closed - the
      moment its head becomes an ancestor of its base, proven from live data: #101, #103 and #105
      all carry merged_at 2026-07-31T10:36:43Z, the second fork main fast-forwarded, and two carry
      no merged label. So the label-then-close step was duplicating the fast-forward and is deleted,
      the merged label with it; landed becomes a report. The fast-forward moves after the reparent
      sweep, because auto-detection only fires for a pull request whose own base moved. (2) The
      skill never writes code: a conflict it cannot merge cleanly, or a red check, is reported to
      the branch's owner and skipped. That removed every krrood, ormatic, coraplex and
      semantic_digital_twin mention, so the instructions are now repo-generic. A conflict it does
      resolve while restacking must be reported in a comment. (3) POINTER.md, prompt_model.py and
      test_prompt_documents.py are deleted - the registered prompt is a skill call now, so the
      duplication the prose tests guarded no longer exists. One test survives, that the skill names
      no fork of its own: an absence computed from live configuration rather than a sentence
      asserted against a document. 313 tests pass, was 331; the difference is exactly those 18 prose
      assertions. Consequence to carry: the HARD RULES no longer bind before the first file is read,
      which reverses design decision 4. Flagged to the user on the PR rather than buried - the
      window is now one turn, and routine-cutover deletes the scheduled run anyway. Also: commands
      are a Command StrEnum with a needs_a_board property; a pre-flight refusal carries a
      RefusalReason so callers act on which refusal rather than its wording; BranchMove is
      ProposedCommitMove; label tests read their labels from the configuration that defines them;
      promotion writes its compare link into the fork pull request's description so it outlives the
      summary. Settled 2026-08-02 (user decision): stack.py is not split in this PR. It stays ~1,540
      lines here and dev-tooling-python-package does the surgery once, when it moves every .claude/
      Python file into the package - rather than twice, with #110 and #111 rebasing across the first
      attempt. The review comment that raised it stays answered rather than resolved. Manual step
      still due: register the routine's prompt as a call to /stacked-pr-maintenance with fork=,
      upstream= and --non-interactive. Until this lands on main the skill cannot be invoked by name
      from a fresh clone, so the prompt must read the file from a ref until then. Resolved
      2026-08-04 (session_01F6tM5mDZr5pTB37UgBV6N5): CI in upstream cram2 failed
      test_the_skill_names_no_fork_of_its_own with ForkRemoteNotFoundError - the checkout topology
      of a cross-fork PR's base-repo CI run (only origin, pointing at cram2 itself) has zero fork
      candidates, which load_configuration() treats as an error. Not new - #110 already hit and
      fixed the identical bug when it removed the remote inference; its fixed test computes
      candidate forks directly from the checkout's remotes and tolerates zero of them, using only
      symbols already in #106's stack.py. Ported that fix back to this branch verbatim (b3e240e6)
      rather than waiting on #110's rebase, since #106 is the parent and runs in this topology once
      promoted. Verified against a scratch clone whose only remote is cram2 (reproduces the exact
      reported traceback pre-fix, passes post-fix) plus the full test_claude_dev_tooling scope (321
      tests, no regressions). See roadmap.md for the full writeup. Merged into fork main
      2026-08-05T14:10:36Z (merged_by AbdelrhmanBassiouny, head b3e240e6 - the fork-resolution fix's
      own commit). Verified by presence on fork main rather than from the merge notification alone:
      .claude/stack/stack.py, .claude/skills/stacked-pr-maintenance/SKILL.md and
      .claude/stack/tests/test_maintenance_skill.py all git cat-file -e clean on origin/main. This
      is the fork-internal merge only - the separate cram2 promotion (the PR's own "Promote"
      compare-and-create link) is still a manual step, so routine-cutover's gate ("PR 1 on
      cram2/main and fork main fast-forwards") is not yet met by this alone. Unblocks
      #107/#110/#111/#139, all based on this branch, to rebase onto main. The only CI red on this PR
      at merge time was test_each_lib (giskardpy)'s test_pacer.py::test_with_executor - a
      physics/timing assertion unrelated to this PR's 11-file .claude/-only diff, matching this PR's
      established pattern of unrelated robotics-stack flakes; test_claude_dev_tooling itself was
      green.

  - id: setup-stacked-prs-skill
    branch: claude/setup-stacked-prs-skill
    title: "PR 2: /setup-stacked-prs guided one-command setup skill"
    pull_request_number: 110
    track: stack-tooling
    depends_on: [stack-tooling-on-main]
    status: in_progress
    session: https://claude.ai/code/session_01QurCwih1r6STYtP34bpJpf
    notes: >
      Mirrors PR #101's pattern (read-only check-stack-setup.sh + guided skill +
      prerequisite-check.md) and #107's script/skill split. Two install modes: native (tooling on
      the target repo's main) and fork-overlay (a never-merged tooling branch on the user's fork;
      re-running is the updater). Creates missing labels, writes personal stack.toml, emits the
      Routine prompt. Based on claude/setup-personal-notes-script (#107) rather than #106, because
      setup-stacked-prs.sh:44 sources github-api.sh, which exists on no other branch; #107 already
      carries all of #106, and git merge-tree gives the identical conflict set either way, so the
      base costs nothing and keeps decision 10's one-linear-chain property. Scope assessed
      2026-08-02 and confirmed separate: removing this item's edits to #106's files leaves ~2,645
      lines of standalone setup infrastructure, so it is ordinary stacking, not a disguised
      modification of its parent.

      Rebased onto #106's review round 2026-08-03 (eecd765d + b9f8f752), which is what the
      2026-08-02 instruction list called for; that list is now consumed and is not re-recorded
      here. Five files conflicted, all under .claude/stack/, every one resolved in favour of the
      parent: ROUTINE.md's delete accepted, this branch's routine-prompt.md deleted as a duplicate
      of the skill's, stack.py's config/print_config/BOARD_FREE_COMMANDS dropped for #106's
      configuration, stack.toml and test_stack.py taken from the parent. The one thing this branch
      had that its parent did not survives: cram2_link_sent_label, without which a fresh fork's
      first promote writes a label nobody created.

      One real defect rather than a no-op, found by reading the script rather than reasoning about
      the deletions: check-stack-setup.sh:51-55 required STACK_ROUTINE_DOCUMENT and
      STACK_ROUTINE_PROMPT_FILE and :62 reported them present by name, both pointing at files that
      exist nowhere - so the check reported needs-setup on a correctly installed checkout. Both
      constants now resolve to .claude/skills/stacked-pr-maintenance/, written failing first. That
      fixed fork-overlay for free: OVERLAY_FILES already listed both, so an overlay was about to
      ship tooling with no instructions.

      The ~120-line remote inference is deleted, closing #106's two deliberately-open threads
      (stack.toml:23 and stack.py:84, both replied to and resolved). AmbiguousForkRemoteError is
      gone because ambiguity is unreachable once the fork is named; ForkRemoteNotFoundError becomes
      ForkRepositoryNotConfiguredError at the same exit 4, so the maintenance skill's step 0 keeps
      the status it keys on. RemoteResolution is symmetric now - a missing fork remote yields a
      git remote add command exactly as a missing upstream already did, which is what lets setup
      run before either remote exists. Setup writes fork_repository at install time and merges the
      override file rather than replacing it: both writers exist now, and a whole-file write
      silently discarded whichever ran first (a defect nobody had recorded). --fork/--upstream take
      an owner/name, a URL, or a remote.

      408 tests pass across the three directories CI runs, was 334 on this branch before the
      rebase. Scratch fixtures now build their bare repositories at paths naming an owner/name and
      address them as file:// URLs - not cosmetic: remotes are matched by the repository their URL
      names, and a bare local path deliberately names none. Verified live in this clone: the
      stack_tooling_files row reads ok, configuration exits 4 naming both ways to answer, and
      resolves when told. Still unverified, same reason as #107: live label creation, exercised
      only through a stub because this environment's token is a fine-grained installation token.

      Merged its base a second time 2026-08-05 (44df7fdb), after main moved a long way beneath it
      - #106 landed there, along with #109, #115 and #119. Conflicts were .gitignore and
      tests/scratch_repository.py, both the benign additive shape: two independent blocks in the
      first, two different methods added at the same place in the second (notes_branch_commit here,
      update_notes_branch_file from main). Base stays #107 - github-api.sh is still not on main,
      re-checked rather than assumed.

      One integration break the conflict markers did not show, which is the part worth carrying:
      test_personal_settings_sync.py arrives from main and installs the hook scripts it calls, but
      this branch made write-personal-notes-file.sh delegate to write-branch-files.sh - a
      transitive dependency that does not exist on main. Two tests went red only after the merge,
      with "No such file or directory". Resolving the markers alone would have left the suite
      broken; the fixture installs it now. 433 tests pass, was 408.

      Merged its base a third time 2026-08-10 (44df7fdb to 5d3cf34b), pushed by another actor
      rather than this session - a clean fast-forward, so nothing of the rebase was lost, verified
      by ancestry rather than from the notification. main had moved 203 files and ~25,700 lines
      beneath it (#516 rdr-backward-inference, #525 world_specification_fresh_worlds, #508
      stretch_real_demo among them). Re-ran the whole suite rather than trusting the merge, since
      the second merge had already produced exactly this surprise: 463 tests pass, was 433, the
      difference being main's own new tests. check-stack-setup.sh's stack_tooling_files row still
      reads ok, so the defect this item fixes survives both merges. test_claude_dev_tooling green
      on the new head, mergeable_state back to unstable, needs-resolution left for the maintenance
      pass to clear itself rather than raced. Description brought current: 463 tests, and the two
      base merges recorded on it.

      One CI red on that head is worth carrying beyond this item, because it blocks every pull
      request in the repository rather than this one: test_each_lib (robokudo) fails during uv
      sync, before any test runs, because greenlet 3.5.5 was published to PyPI with wheels for
      macOS and Windows only. greenlet is pinned nowhere here, so it resolves fresh on every job
      on every branch. Not this branch's by construction either - its entire non-.claude/ diff
      against main is a 4-line .gitignore addition, touching no dependency manifest. Nothing
      pushed for it; the mechanical workarounds are constraining greenlet below 3.5.5 or the
      tool.uv.required-environments entry uv itself suggests.

  - id: stack-maintenance-executor
    branch: claude/plan-item-kickoff-workflow-koufa6
    title: "Deterministic maintenance executor: board, fast-forward, restack, run-report"
    pull_request_number: 139
    track: stack-tooling
    depends_on: [stack-tooling-on-main]
    status: done
    session: https://claude.ai/code/session_014E9nB1MUvm4jwgC2UTN5GT
    notes: >
      User's proposal, 2026-08-03: automate the stacking pass with a deterministic script that
      fetches the relevant pull requests, performs the git commands, and reports everything - less
      work for the session running the skill, and more reliable. The gap is real and was verified
      rather than assumed: grep -n "git push" over stack.py's 1,600 lines returns nothing. stack.py
      is read-only derivation only, so every fetch, merge, rebase and push in the workflow is
      performed by a session following prose, and board.json is hand-assembled from MCP output - the
      same hand-assembled-input failure class as #119, where a dropped field was indistinguishable
      from a legitimate one.

      Four commands in a new module (.claude/stack/maintenance.py + its own test module), so the
      only edits to #106's files are the SKILL.md steps and the dispatch wiring: board --write
      (fetches the fork's open pull requests itself), fast-forward (step 2, refusing a
      non-fast-forward rather than forcing), restack (drives restack-plan, runs preflight before
      every push, never force-pushes without the rebase label, reporting per branch pushed /
      up-to-date / conflict <files> / refused <RefusalReason>), and run-report --json as the one
      machine-readable document the skill renders and routine-cutover's Action emits directly.
      SKILL.md steps 2-4 collapse to "run it, act on the report" - the same
      computed-rather-than-recalled move #106 already made for labels/preflight/promotion-link.

      Step 0 before any design is committed: a credential probe. Already on record are the raw PATCH
      base 403, the MCP update_pull_request 200, and the stacks endpoints working via curl +
      GH_TOKEN. The 403's message names the base branch specifically, so three writes are unknown
      and decide how much can move into code - PUT /issues/{n}/labels, POST /issues/{n}/comments,
      and a body-only PATCH /pulls/{n}. Record the results either way: routine-cutover's Action
      needs the same table for its own credential.

      Staying with a session/MCP regardless: base changes, conflict resolution, and whichever of the
      three probed writes come back blocked. Scoped as its own item rather than folded into #106
      (user's call): the executor is new files that stand alone, #106 is already 3,413 additions
      across 29 commits and out of draft after a 25-comment review round, and this is a direct
      enabler of routine-cutover's no-LLM endgame. It does not reverse decision 11 - that cut
      structure derivation in favour of GitHub's stack object; this executes an already-derived
      plan.

      Kicked off and implemented 2026-08-04 via /plan-item-kickoff as draft PR #139, based on #106's
      head as a sibling of #107/#111 (it needs only #106's files; git ls-tree origin/main --
      .claude/stack is empty, so main was never a possible base). The gap was re-verified against
      #106's head rather than taken from the earlier grep: stack.py contains no push, merge or
      rebase outside docstrings and enum members.

      Step 0's credential probe came back the opposite way round from the fear it was written
      against. On throwaway PR #138, four calls minutes apart through the same GH_TOKEN and the same
      git proxy: PUT /issues/{n}/labels 200, POST /issues/{n}/comments 201, body-only PATCH
      /pulls/{n} 200, and - as a control on the same pull request - a base-branch PATCH 403. So the
      recorded 403 is a property of the base field specifically, not of writes generally and not of
      the credential as a whole. Label writes, conflict-report comments and description writes are
      all available to deterministic code; only the base change needs the MCP server. That table is
      what routine-cutover's Action needs for its own credential, and it is recorded here rather
      than only in the pull request. This PR deliberately did not widen its scope to use those
      writes - the four commands are what was agreed - so moving the needs-resolution labelling and
      the conflict comment into code is a concrete follow-up, now known to be possible rather than
      assumed blocked.

      Delivered: .claude/stack/maintenance.py with its own argparse entry point and zero edits to
      stack.py - which keeps stack.py's read-only contract and means this branch cannot conflict
      with #110's inference deletion in that file. Entry-point shape was the one genuinely open
      question at kickoff ("the dispatch wiring" read two ways) and the user chose the separate CLI
      over new stack.py subcommands. GitCommandRunner is the one new seam: stack.py's git helper
      returns "" on failure, which is right for derivation and wrong for a push. Forcing is decided
      in exactly one place, push_arguments, and only for the rebase strategy - which build_stack
      sets only from the rebase label - and even then with a lease, so a branch somebody else moved
      comes back push-rejected and untouched.

      342 tests pass, was 321 on this branch (the 313 in #106's description predates its merges from
      main). 21 new, written failing first, against real git in a scratch fork with bare
      repositories addressed as file:// URLs per #110's fixture rule. Each was checked by breaking
      the implementation and confirming only it failed. One test corrected a wrong premise of its
      own: the executor starts each integration from the branch's published tip, so a branch that
      moved under the pass is incorporated rather than raced - pinned rather than left implicit.

      One regression found and fixed rather than shipped: board --write makes a local board.json
      routine, and two tests - one pre-existing in test_stack.py - assert on a missing board while
      reading the real board.json beside stack.py. Running a pass then the suite failed both. Proven
      by writing a board and watching them fail, then fixed with an autouse fixture in
      .claude/stack/tests/conftest.py that sets an existing snapshot aside and restores it.
      .gitignore gains board.json; #110 adds the identical line, so whoever lands second drops one
      copy - a deliberate, named duplication rather than a fourth same-artifact-twice instance.

      Not verified live and deliberately so: restack and run-report push to real branches, and the
      upstream remote is outside a session's repo scope, so they are covered end to end in the test
      harness instead. board --write was run live (44 open pull requests, and the export parses
      straight back through stack.py status); fast-forward was run live as far as its guard (exit 6
      naming the missing cram2 remote, before any push).

      Residue: the throwaway branch claude/credential-probe-koufa6 needs deleting outside the
      harness, per the standing session-cannot-delete-branches constraint.

      Scope widened 2026-08-04 on the user's instruction, once the probe had shown the three writes
      available: "do it as well and anything you found doable in code related to this". All three
      moved into the executor rather than staying prose. A conflict is now handled end to end - the
      branch is labelled needs-resolution with the complete set LabelWrite computes, and its owner
      gets a comment naming the conflicting files and addressing the session link - and the next
      pass reads mergeable_state to either withhold the branch while it is still dirty or clear the
      label and restack it. That closes the loop the label was invented for: previously nothing
      removed it, so a resolved branch stayed withheld until somebody noticed. A fifth command,
      promote, builds each ready branch's compare-and-create link, writes it into the fork pull
      request's description under a Promote heading (replacing any already there, since the
      description is rewritten on every run that rebuilds one), adds cram2-link-sent, and drops that
      label from anything since promoted or landed. in-review stays the developer's to add. Reads
      and writes are one GitHubRepository behind PullRequestReader/PullRequestWriter, so the board
      export depends only on the reader. Retargeting a base is now the single step still reported
      rather than performed, and the report says so.

      351 tests pass, was 342. Four more mutations checked, each caught by exactly the test naming
      it: a label write computed from the addition alone, a still-dirty branch restacked anyway, a
      promotion link appended rather than replacing, and an already-linked branch promoted twice.
      The write client was exercised live against closed throwaway PR #138 - read, description write
      proven idempotent by a second write, label replace, comment - then restored. restack, promote
      and run-report are still not run against the live fork: they rewrite real descriptions and
      push real branches.

      CI then caught a real ordering defect the local run could not: resolving the credential had
      moved ahead of deriving the board, so restack on a checkout with neither reported a missing
      token instead of the missing board its caller fixes with the previous command. A session
      environment exports GH_TOKEN, so the wrong branch was never taken locally. Fixed, and the
      command-line tests now strip the credential for every run - the same ambient-state coupling as
      the board.json fixture, met a second time in one PR: a test that reads ambient state cannot
      assert about the state it is reading.

      Run live against the fork 2026-08-04 on the user's authorisation, using throwaway pull
      requests #140 (parent, non-draft) and #141 (child, draft) and a board scoped to those two
      only, from a detached worktree so the executor's checkout could not disturb the branch under
      review. All five commands exercised: restack integrated a moved parent and pushed the child; a
      real conflict pushed nothing, labelled #141 needs-resolution and posted the naming comment; a
      second pass withheld it while mergeable_state was still dirty and posted no second comment;
      once the conflict was resolved and pushed the next pass cleared the label by itself and the
      branch rejoined; promote wrote the compare link under a Promote heading taking only the first
      paragraph, applied cram2-link-sent, and on a second run replaced the section rather than
      adding another - which also survived GitHub appending its own footer to the description write;
      run-report emitted the whole pass as one document. board --write and fast-forward's guard had
      already been verified live.

      The live run found two defects the unit tests had not, both of the same shape: a restack that
      hit a conflict exited 0, and a run-report whose fast-forward was refused also exited 0. A
      caller acting on the status alone - which is precisely what routine-cutover's no-LLM endgame
      does - would have read both as nothing outstanding. Every command now derives its status from
      one exit_code_for over the report, so no two can disagree about what a clean pass is: 7 for a
      refused fast-forward (outranking the rest, since every branch is measured against that base),
      5 for a pre-flight refusal (a fault in the move, not the branch), 10 for a branch left
      unpublished for its owner. Tests written from the two observations and checked by restoring
      the old behaviour. 357 tests pass, was 351.

      Also settled live, from the user's question about supplying their own token: they cannot make
      the base-branch reparent work from a session. gh is not installed; GH_TOKEN is a 14-character
      "prox..." placeholder rather than a GitHub token; a junk Authorization header and no header at
      all both return 200 on this private repository, so the proxy discards whatever the process
      sends and substitutes its own identity; and the base-branch PATCH 403s identically with a junk
      token and with none, carrying a documentation_url on docs.anthropic.com rather than
      docs.github.com. The refusal is generated by the agent proxy from the request shape, not by
      GitHub and not by the credential, so a personal access token would be discarded before it ever
      reached GitHub. This does not bear on routine-cutover's Action, which runs outside this proxy
      and is a genuinely different actor - still worth verifying there, but for a different reason
      than the one recorded so far.

      Residue from the live run: throwaway branches claude/throwaway-parent-koufa6 and
      claude/throwaway-child-koufa6 need out-of-harness deletion; their pull requests are closed and
      their labels cleared.

      Statuses name themselves as of 2026-08-04, from the user's question about strings beside or
      instead of the numbers. Instead is not available - a process exit status is an integer by
      definition, and these stay aligned with stack.ExitCode value for value - but beside was a real
      gap: the meaning was already in the enum member and nothing emitted it. MaintenanceExitCode
      gains name_for_a_caller, derived from the member so a status can never carry a name belonging
      to another; main prints it once for any non-zero run ("maintenance.py: branch-needs-attention
      (10)") and stays silent on success; and run-report --json leads with status and exit_code,
      which is the case it matters most for, since routine-cutover's Action reads the document
      rather than the process status. 361 tests pass, was 357; three more mutations checked.

      Review round 2026-08-05 (25 comments) applied in 7048a962. Three were design decisions taken
      with the user rather than corrections, and the largest reshaped the skill: the document now
      leads with run-report --json as step 2 and demotes the four individual commands to a reference
      section for resuming a partial run. Writing it that way round the first time was not arbitrary
      - the per-command sections carried doctrine that had nowhere else to live, so explaining a
      command and stating a rule had become one paragraph. What made the change possible was
      splitting those apart: a What this pass never does section now holds the rules with no command
      attached, and it is where the reviewer's two other points landed - the pass never resolves a
      conflict (the "if you resolve one yourself" escape is deleted, and the header's promise
      changed with it from "the only file changes are conflict resolutions" to no file changes at
      all) and it never attempts to open the upstream pull request, which previously read as a call
      to try and expect to fail.

      The shape the reviewer asked for repeatedly is a member carrying its own specification, and it
      is now the module's idiom twice over: PullRequestField(FieldSpecification, Enum) carries each
      field's key, how its value has to be read and whether a record omitting it is rejected, and
      Command(CommandSpecification, Enum) carries each command's name and help text. Both hit the
      same two hazards #143's roadmap entry already records - a member's value must be the argument
      tuple rather than a built specification, and the field cannot be called name because Enum
      reserves it - so the first is guarded by a test and the second is invoked_as. That collapsed
      _required, _branch_reference and _label_names into PullRequestField.read, and took the three
      raw record.get calls in promote and the conflict check with it.

      ExternalCallFailed is the shared parent of GitCommandFailed and GitHubRequestFailed, holding
      status and detail and composing the message from an abstract call - krrood's
      DataclassException idiom mirrored rather than imported, per decision 12's stdlib-only
      boundary. GitCommandRunner has one named method per command it runs, which deleted two free
      functions and replaced four hand-spelled merge-base invocations, one of which had its
      arguments in the opposite order to its neighbours. ProposedPush is where forcing is decided,
      so the runner's push cannot decide it separately. BranchAncestry is a frozen dataclass rather
      than a returned closure; ForkPullRequests names the reader-plus-writer union, with the two
      halves kept separate so the board export cannot write.

      Dispatch is open/closed now: each command is a MaintenanceCommand subclass owning its flags
      and its work, listed once in COMMANDS, which is also what builds the parser - so a command
      that exists but is unreachable is not expressible. MaintenancePass preserves the
      board-before-credential ordering CI caught earlier. run-report deletes board.json when the
      pass finishes with it (the user's call, scoped to that command alone, since the other four are
      the resume path); a stale snapshot read as current is worse than none, and exit 3 names the
      command that fixes it.

      Three threads left open deliberately: the two asking why not gh or PyGithub - answered with
      the reason for now (gh absent, stdlib-only tier) and deferred to
      dev-tooling-github-api-unification, which exists to settle exactly that once - and the
      question about supplying a credential that can reparent, which is answered but calls for no
      change here. 364 tests pass, was 361; two new (what the export reads out of each field's own
      shape, and that every field is named by a string) plus the board-deletion one, each checked by
      mutation.

      Second review round the same day (12 comments) applied in eb648308. Two were put back to the
      user rather than guessed at, and both answers shaped the result. PreFlight becomes
      CommitMoveChecks everywhere - stack.py included, with CommitMoveRefusal, the check-move
      subcommand and MOVE_REFUSED in both tools' exit codes - because the old name said when it ran
      rather than what it did, and the new one pairs with the ProposedCommitMove it is handed. And
      "actions as dataclasses with a shared abstract parent" meant the restack's per-branch steps:
      each is now a RestackStep subclass that either concludes the branch or lets the next run, with
      BranchUnderRestack carrying what a step needs, so _restack_branch is a loop rather than a
      chain of ifs and the two free functions taking six arguments became the steps that own them.
      Unlike the commands these stay listed rather than discovered, because their order is the
      procedure; a branch no step concludes raises rather than returning silently.

      The Command enum is deleted - with a class per command it was a second registry that could
      name a command nothing implemented - and COMMANDS is derived from
      MaintenanceCommand.__subclasses__(), so a command class that exists is reachable by
      construction. PullRequestField's members name their arguments through a keyword-only helper,
      since an enum member's value has to be the argument tuple and BODY = ("body",) said nothing
      about what the two omitted values meant. FieldShape and FieldSpecification take the
      PullRequestField prefix; reading a field is a match statement.

      The skill was cut on one rule the user's own framing supplied: a sentence describing what the
      code does has nothing holding it true, so it is a future lie with a delay attached. Out went
      the conflict-labelling description, the force-with-lease rule, the board deletion, the
      per-outcome table and the flag lists - each already documented next to the code that does it.
      What is left is what the agent decides: act on the status, act on reparents, name the branches
      left for their owners. Step 2 is ~30 lines, was ~70.

      366 tests pass, was 364. Two threads stay open on purpose: whether the stack.py half of the
      rename belongs on #106's branch by the prefer-the-change rule, and whether the test pass
      should use __post_init__ regardless now that making the derived values fields removed the need
      for it (and for object.__setattr__, which is what the neighbouring comment objected to).

      Third round, four comments, in 62133080 and 16d04f99. The reviewer's reading that a member
      could be written as a constructor call was right, and prototyping it settled the shape the
      previous two rounds had both got wrong: written plainly it builds and is silently wrong - an
      enum hands a member's value straight to the type it mixes in, so the whole specification lands
      in key and required reverts to its default - but an __init__ on the enum that unpacks the
      specification onto the member fixes it, and the arguments become real keywords rather than a
      helper's. That helper, which existed only to return its arguments unchanged, is deleted.
      Removing the __init__ fails three tests, so the silent form cannot come back unnoticed.

      The same reviewer's suggestion to find RESTACK_STEPS from its subclasses was applied and then
      reverted on their own call once they had read why it was a list. The mutation check is what
      settled it and is worth keeping: swapping PublishBranch ahead of RefuseAnUnsafeMove fails
      test_a_push_the_move_checks_refuse_is_not_made, a behavioural test that publishes to a real
      scratch fork and asserts the destination ref never moved. So the order is load-bearing
      behaviour a test already catches, which also made the order test added during the
      auto-discovery attempt redundant - it restated the tuple - and it was dropped with the revert.
      COMMANDS stays discovered, because its list carried no meaning; the steps stay listed, because
      theirs is a decision about what a pass does.

      stack.py's enum members and its two undocumented module variables gained docstrings, swept by
      an AST pass over both modules rather than fixed at the one line flagged. 366 tests pass.

      First live run against the real stack, 2026-08-05, and it found a defect in this pull request
      that every throwaway run had missed. The pass restacked, hit a .gitignore conflict on this
      very branch, labelled it needs-resolution at 14:10:42 - and then promote stripped that label
      at 14:11:46, in the same pass, replacing the set with cram2-link-sent alone.

      Root cause: board --write exports a snapshot at the start of a pass, restack then withholds a
      conflicted branch by labelling it live, and promote read neither back. promotion_order's
      needs_resolution exclusion and LabelWrite.replacing both took branch.labels, which is the
      snapshot - so promotion promoted a branch the same pass had just conflicted on, and computed a
      whole-set label write from a list already stale. That is the production incident
      LabelWrite.replacing exists to prevent, re-entered through a different door: the rule is
      compute it from the labels the pull request carries now, and branch.labels is at board time.
      The consequence was the loop this item exists to close staying open, since with the label gone
      the next pass has nothing to withhold on and re-reports the same conflict.

      Fixed by reading both the decision and the write off the pull request record promote already
      fetches for its title and description. Two tests written failing first, each checked by
      mutation. One pre-existing test changed setup only, not assertions: the behaviour it names - a
      branch already carrying the link label is not promoted again - is unchanged and still correct,
      and only where "already carrying" is read from moved.

      Also merged main, which had moved a long way: #106 landed, so this branch's base became main
      and .claude/stack/ plus the maintenance skill are now on main (maintenance.py is not, so the
      routine prompt still has to resolve this branch). The .gitignore conflict was the benign
      two-additive-blocks shape - this branch's board.json entry and main's .plan-state-sync-sha
      stamp arriving with #115 - both kept. 385 tests pass, was 366; the difference is main's own
      tests plus the two added here.

      Confirmed rather than assumed while diagnosing: the conflict comment addressing this session
      was correct rather than a bug - get_session_link_in reads the link out of the pull request's
      own description, so a report reaches the branch's owner.

      Review round 2026-08-11 (four comments) applied in ebf67734, and three of the four were
      structural. Two named a module split - the restack step classes into their own module with
      RESTACK_STEPS in another, and the command classes into one with COMMANDS - and the third was
      the earlier constants comment, whose seven plain values moved with them. The commands one is
      what inverted the file, and the reasoning is worth keeping because it was not obvious until it
      was tried: a commands module cannot both be imported by maintenance.py and import everything
      the commands do. So maintenance.py is the command line and nothing else now, and the work sits
      below it in eleven modules named for what they do. The earlier deferral to
      dev-tooling-python-package - the one that kept stack.py unsplit - is therefore overridden for
      maintenance.py alone, on the user's explicit instruction; stack.py is untouched and still
      waits for that item.

      The command's name and description became abstract class properties, replacing
      __init_subclass__ and the ClassVars. This is the third round on the same two members, and the
      measurement that settled it is the one nobody had made: a plain classproperty silently loses
      the abstractness, because ABCMeta decides what is still abstract with getattr(cls, name),
      which calls the descriptor and gets a plain string back - so a subclass supplying nothing was
      not abstract at all and answered None. __get__ answers with the descriptor itself while it is
      abstract, which restores the refusal, and COMMANDS instantiates every subclass, so it lands as
      the module is imported. classproperty is class_property.py rather than krrood's, per decision
      12's stdlib-only tier.

      The three fork Protocols became abstract dataclasses, on the user's answer to the question the
      previous round left open. It caught a hole the same day it was applied: the fork stand-in
      never implemented open_pull_requests, which structural typing could not see because restack
      and promote never call it. Completed rather than stubbed. One constraint learned rather than
      assumed: dataclasses refuse a non-frozen subclass of a frozen base, so making the bases frozen
      - the module's idiom - made both test stand-ins frozen too.

      154 tests pass, was 151. Four new assertions, each checked by breaking the implementation, and
      one existing test changed where the refusal moved from class definition to construction - it
      keeps its assertion and instantiates rather than defining. Two threads stay open deliberately:
      the --quiet question, which is answered and calls for no change, and the doc-formatting one,
      whose outstanding half is whether to sweep main's 8 unformatted files in a separate pull
      request. The gh/PyGithub pair stays deferred to dev-tooling-github-api-unification as before.
      Session: https://claude.ai/code/session_01DHbsXEZCDRYbegKU4iVGyP

      /plan-item-resolve run 2026-08-12 (session in this manifest's own history at the time of
      writing): PR #139 itself is not blocked - CI is green on all 21 checks at ebf67734, it is
      mergeable_state clean against fork main, and review is substantively settled (the two open
      threads and the PullRequest.ci comment are deliberately deferred elsewhere, not blockers).
      The "merge conflicts with upstream main" flagged for this resolve is real but latent, verified
      by direct measurement rather than assumed: fork main is 160 commits behind cram2/main (0
      ahead) - expected, since stack-tooling-on-main (#106) promoted to cram2 as PR #501
      (af935a19) and cram2 has moved on since, while fork main's own fast-forward from cram2 is
      deliberately deferred to routine-cutover's not-yet-built Action. `git merge-tree --write-tree`
      of cram2/main against this branch's head produces one real content conflict, in
      .claude/skills/stacked-pr-maintenance/SKILL.md - both sides independently rewrote it (cram2
      picked up further edits after #501 merged; this branch's own 25-comment round rewrote the
      same sections). Nothing today is merging cram2/main into this branch, so it blocks nothing
      now; it will surface as the first restack conflict the moment fork main is fast-forwarded and
      this branch (or fork main after it merges) is restacked onto the new tip - exactly the
      needs-resolution flow this PR implements. Recommendation: land #139 as-is; do not
      pre-emptively resolve against a cram2 main that is still moving, and do not have a session
      quietly resolve it out of band, which the pass's own design deliberately avoids doing to
      someone else's conflict. Recorded here for whoever runs the first fast-forward + restack pass
      (most likely while finishing routine-cutover) so it isn't rediscovered from scratch.

      Second /plan-item-resolve of 2026-08-12, and it supersedes the paragraph above rather than
      extending it. That entry concluded "#139 is not blocked" and "nothing today is merging cram2/main
      into this branch", both true when written and both false a few hours later: fork main was
      fast-forwarded from cram2 (origin/main is now e123c383, 142 commits ahead of this branch's merge
      base), so the conflict it had measured as latent against cram2 main became live against this pull
      request's own base. GitHub reported mergeable_state dirty and a maintenance pass labelled #139
      needs-resolution at 09:49:07Z, naming the same single file the earlier measurement had predicted -
      .claude/skills/stacked-pr-maintenance/SKILL.md. The prediction was right and the recommendation
      (wait for the fast-forward rather than pre-resolving against a moving target) cost nothing, since
      the wait turned out to be hours.

      Resolved by merging origin/main, in 614eaccd. The conflict was small on main's side and large on
      ours: main had reworded two lines of the conflict-report step when no-pr-subscriptions (#153,
      upstream #535) landed, while this branch's 25-comment round had rewritten the whole document. Kept
      this branch's version and folded main's surviving instruction - write the comment to stand alone,
      because nothing delivers it - into the one bullet that still tells a person to write one. Keeping
      both mechanically would have been wrong in the other direction: the branch already carries the
      no-subscription rule twice, so the only content at risk was that clause. .gitignore and
      .claude/stack/README.md auto-merged. A merge rather than a rebase because #151 and #154 are both
      based on this branch.

      Three of the four review comments of 2026-08-11 11:06-11:10 applied in ba674d1d. They are a fourth
      round, not the one the entry above records as applied in ebf67734 - those were the 09:01-09:04
      comments, and these arrived ninety minutes after that push. The trailing _s on a test name was a
      possessive that had lost its apostrophe; two test-local stand-ins were plain classes while every
      neighbour and every real subclass is a frozen dataclass.

      The doc-formatting comment had a cause worth recording, because the obvious fix was not one:
      scripts/format_docstrings.py reports no change on the flagged file, while docformatter --check
      disagrees with it on 33 docstrings. The script keeps the plain black-formatted content whenever
      docformatter's result does not survive a second black pass, which is the case its own module
      docstring names - and here it is one blank line, after an attribute docstring immediately preceding
      a decorated definition. So the file was not unformatted through an omission; the repo's own
      formatter had silently declined it. Four modules plus the test module were on the wrong side of it,
      not the one flagged. All five are formatted by hand as black then docformatter then black, which is
      stable: re-running the script afterwards returns them byte-identical, and the only difference
      docformatter still reports is that blank line. stack.py is in the same state and left alone, since
      it is main's file and the sweep question belongs to it.

      450 tests pass across the three directories CI runs, which includes main's own arriving with the
      merge. The one open thread is why the abstract-command refusal is a TypeError rather than a custom
      error: it is ABCMeta's, and raising our own means dropping the abstractmethod chosen in the
      previous round, so it is answered with that measurement and left open on the user's call.

      Also repaired, both predicted on this pull request on 2026-08-05 and since realized: the
      description had been truncated at the literal "## Promote" heading it carried in prose, because
      description_with_promotion_link partitions on the first occurrence - so the live-fork evidence
      section ended mid-bullet. Rewritten without a heading that can be partitioned on, and the session
      link restored: it had been lost, which is why the 09:49 conflict comment says "This pull request's
      description names no session to address" and reached nobody. Session:
      https://claude.ai/code/session_01PyGLT8ofaqHShryiut281r

  - id: setup-personal-notes-script
    branch: claude/setup-personal-notes-script
    title: "Extract a deterministic setup script from /setup-personal-notes"
    pull_request_number: 107
    track: stack-tooling
    depends_on: [setup-personal-notes-pr101, stack-tooling-on-main]
    status: in_progress
    session: https://claude.ai/code/session_01MyxyUN3bsvPsJnb5QuNzxd
    notes: >
      Implemented as draft PR #107 (2026-07-29), based on claude/patch-pr-rheubx rather
      than main: the user overrode this item's "requires #101 MERGED" rule. That rule was
      never really about ordering — all six files this item builds on arrive with #101, so
      basing on it is what made the work possible at all.
      Restacked onto #106 on 2026-07-30 (base retargeted, #106 merged into the branch) so
      the upstream wave is one linear chain instead of two siblings on #101: PR 2 needs
      .claude/stack/* and github-api.sh together, and the diamond alternative would have
      been invisible to stack.py restack-plan, which derives exactly one parent per branch
      from the PR base. depends_on gained stack-tooling-on-main to match. GitHub retargets
      the chain upward as each parent merges.
      The item's original premise is disproved and no longer applies: the two steps
      recorded as session-only are both scriptable (GET /user for the login,
      GET /repos/{owner}/{repo}/labels/{name} for the labels, POST .../labels to create
      one — the "no create-label tool" limit is true of MCP, not of the API). So the whole
      setup runs with no session, and SKILL.md became a thin question-asking wrapper
      (238 → 153 lines) rather than the "script plus two API steps" split the dispatch
      prompt described. Delivered: .claude/hooks/setup-personal-notes.sh
      (--remote required, --starter-notes, --create-labels), .claude/hooks/github-api.sh,
      4 new path constants, 254 tests passing (was 236), all network- and
      credential-free. Carries two incidental fixes as their own commits, both hard
      prerequisites: current_branch_upstream_remote returning git's 128 instead of "no
      upstream" (which aborted create-personal-notes-branch.sh on any branch never pushed
      with -u), and a test helper that hid an executable by dropping its whole PATH entry.
      Still unverified: label creation against a real token (this environment's is a
      fine-grained installation token, so POST .../labels is exercised only via a stub).
      Cross-note stands but shifts: PR 2 (/setup-stacked-prs) should mirror this
      script/skill split, where the skill keeps only the questions and the un-scriptable
      environment-variable step — not a split reserving the GitHub calls for a session.

  - id: shared-pr-state-chips
    branch: claude/shared-pr-state-chips
    title: "PR 3: shared pr_state module + LOC/CI/conflict chips in plan dashboards"
    pull_request_number: 111
    track: dashboards
    depends_on: [stack-tooling-on-main]
    status: in_progress
    session: https://claude.ai/code/session_014KoJeaTUxyECZZpfWiVmvr
    notes: >
      Extract one PR-state fetch/compute module (CI rollup, LOC vs base, mergeable probe,
      session-link parse) shared by stack.py and plan-dashboard's pr-data fetching; add
      board-semantics chips to build_dashboard.py items; add a headless static-HTML build
      entrypoint for the Pages Action (Artifact path keeps working during transition).
      Since PR 1 (#106) landed as plain scripts, this PR also CREATES the dev-tooling
      Python package (top-level directory importable from the repo root with no install,
      plus a pyproject for optional installation) and puts pr_state in it as its first
      module — the shared import home is what forces the package into existence here.
      Kicked off 2026-07-30 (branch based on claude/stack-tooling-on-main, a sibling of
      #107 on #106 — needs only #106's files, so decision 10's linearization doesn't
      apply). Two calls settled at kickoff: the package is named development_tooling
      (abbreviation-free per AGENTS.md), and the headless site build also pushes
      merged-to-done manifest auto-corrections, same as the live refresh path.
      (This kickoff state was accidentally reverted by a concurrent stale-scaffold save
      on 2026-07-30 14:49 and restored by the routine-cutover kickoff session.)
      Implemented as draft PR #111 (2026-07-30): development_tooling package (pr_state +
      personal_notes modules, pyproject for optional install), stack.py export restored
      through it, optional chip fields in pr_data.json rendered as CI/size/conflict
      chips (old pr_data still renders chipless), and build_site.py writing _site/ for
      PR 4's Action. 302 tests passing across both CI invocations (was 254); the new
      test/development_tooling_test suite runs with --confcutdir since the shared
      test/conftest.py imports the robotics stack. One old bug fixed in the port:
      merge-tree exits 1 for both a conflict and a missing reference, so the old probe
      reported a typo as a conflict - the port verifies references first.
      Added 2026-07-31 (native-stacks pivot, user-approved): pr_state also reads the
      PR's native stack object via REST (X-GitHub-Api-Version 2026-03-10; GraphQL is
      unusable from sessions) and renders a stack chip ("layer p/n · stack #N") when
      the PR is a stack member. #106's cut may simplify or remove the "stack.py
      export restored through the package" interplay; pr_state itself is unaffected.
      Restacked onto main 2026-08-05 (session_014KoJeaTUxyECZZpfWiVmvr): once #106's
      chain landed, GitHub retargeted #111 to main and the maintenance pass reported a
      real conflict (needs-resolution) in stack.py, README.md and build_dashboard.py -
      this branch's edits against main's re-reviewed versions. Resolved by taking
      main's restructured files and re-applying the deltas in their idiom: export is a
      Command member in the argparse dispatch resolving the fork via
      Configuration.fork_repository (the branch's own URL helper deleted -
      Repository.from_remote_url owns that now), ExitCode gains GITHUB_UNAVAILABLE 6,
      main's duplicate personal-notes resolvers are replaced by the
      development_tooling.personal_notes imports, and from_mapping keeps both #119's
      MissingMergeTimestampError guard and the optional chip fields. 401 tests green
      (357 + 44); pushed as merge 52e3ede4, mergeable again, label clears on the next
      pass. The stack-chip addition recorded above is still not implemented.

  - id: dev-tooling-python-package
    branch: null
    title: "Migrate the .claude Python into the dev-tooling package, tests under test/"
    track: dashboards
    depends_on: [setup-personal-notes-pr101, stack-tooling-on-main, shared-pr-state-chips]
    status: not_started
    notes: >
      Move the existing Python (.claude/stack/stack.py, build_dashboard.py + siblings,
      plan_manifest_tools.py) into the package PR 3 created, with all their tests moved to the
      standard test/ directory and the test_claude_dev_tooling CI job repointed. Boundary: SKILL.md
      files, settings.json, and the bash hook entry points stay in .claude/ (Claude Code discovers
      them by path) as thin wrappers calling python -m into the package. Constraints: zero-install
      must survive (importable from repo root; installation optional), and the package stays visibly
      dev-tooling (own directory, not published, not in the default install). Stronger dependency
      than the open-ready rule: requires #101 MERGED (this moves files in .claude/hooks/tests/ that
      #101's diff adds) and PR 3 merged or at least stable (it moves files PR 3 edits) — sequence it
      last in the upstream wave.

      Fifth carrier recorded 2026-08-11, from two review comments on #151 asking why its command
      classes are not #139's and whether the duplication across the tooling pull requests can be
      fixed. The command-class pattern now exists twice: #139's MaintenanceCommand in
      .claude/stack/maintenance_commands.py, with classproperty in .claude/stack/class_property.py,
      and #151's Subcommand in .claude/hooks/plan_item_bootstrap.py - each a frozen dataclass ABC
      whose registry comes from __subclasses__(), each with its own tests.

      They differ only in where the parser is built from, which is what decides whether the name has
      to be readable off the class: #139 builds from the classes and needs classproperty, and had to
      discover that a plain one silently loses abstractness because ABCMeta resolves it through
      getattr; #151 builds from instances, where plain ABCMeta refuses a nameless command as the
      module imports. Neither is wrong for its own module, and making them textually identical today
      means copying class_property.py into .claude/hooks/ - a fifth duplicated file added in answer
      to a complaint about duplication.

      The mechanical cause is the one this item exists to remove: .claude/hooks/ and .claude/stack/
      are separate sys.path entries, so nothing in one can import the other without the hackery
      decision 8 ends. So the command base, its registry and its tests join run_git, the GitHub
      backend rule and ItemStatus on the list this migration unifies, rather than being reconciled
      pairwise beforehand. Basing #151 on #139 was considered and declined for the same reason it
      was declined for #143: it holds a dependency-free immediate-wave item behind an unlanded
      stack-tooling pull request and still does not let either import the other.

  - id: dev-tooling-notes-core-python
    branch: null
    title: "Bash→Python 1: notes-core modules (config, git seam, writer, markers)"
    track: dashboards
    depends_on: [dev-tooling-python-package]
    status: not_started
    notes: >
      Pure addition to development_tooling, no bash file changes (decision 12):
      errors.py (stdlib-only dataclass-exception base mirroring krrood's
      DataclassException idiom — typed context fields, abstract
      error_message/suggest_correction composed in __post_init__ — without importing
      krrood), personal_notes_configuration.py (ConfigurationSource StrEnum, frozen
      ResolvedSetting and PersonalNotesConfiguration.resolve with the git-config >
      environment > default precedence), git_interface.py (thin GitRepository
      dataclass as the dependency seam; the real-git ScratchRepository harness stays
      the primary test strategy, no fakes), notes_branch.py (fetch_notes_branch with
      the upstream-remote fallback, ScratchWorktree context manager with
      pid+uuid-suffixed scratch branch names, NotesBranchWriter with a bounded
      re-fetch/re-apply push retry), marker_sections.py (single source for the
      CLAUDE.local.md BEGIN/END markers currently duplicated between session-start
      and every save script), and __main__.py with only the configuration
      subcommand. The SessionStart-reachable surface is stdlib-only, enforced by an
      import-block test. Includes a real two-writer non-fast-forward retry test
      against the bare notes remote.

      Third carrier recorded 2026-08-05, from a review question on #135 asking whether its
      check_scope_overlap.py duplicates #139's GitCommandRunner. Read against both rather
      than inferred: #139's is not a duplicate - its attempt/run split and ~14 named methods
      exist because a push that silently did nothing must not read as one that worked, and
      #135's script never writes. But #143's plan_item_bootstrap.py run_git and #135's are
      near-identical, both generic and both raising, differing only in error type and
      parameter order. So the git_interface.py seam this item already plans has three
      callers waiting, not one, and the same subprocess.run(["git", ...]) boilerplate is
      written out three times. Not unified at the time of finding: all three files are
      unlanded on three branches targeting three different directories, and main has no
      shared module to hold the seam until dev-tooling-python-package creates one - so
      extracting early would mean either a cross-dependency between three in-flight pull
      requests or the sys.path hackery decision 8 exists to end. Note the precedent does
      not cover this one: #143's own review accepted the stack.py overlap because the
      contracts were deliberately opposite (_git returns "" where run_git raises), and
      #135's and #143's contracts are not opposite.
  - id: dev-tooling-save-commands-python
    branch: null
    title: "Bash→Python 2: the four notes writers become python -m subcommands"
    track: dashboards
    depends_on: [dev-tooling-notes-core-python]
    status: not_started
    notes: >
      save-personal-notes.sh, save-pr-progress.sh, write-personal-notes-file.sh and
      create-personal-notes-branch.sh become three-line shims (cd to root, exec
      python3 -m development_tooling <subcommand> "$@"); their bodies move onto the
      core modules, collapsing the four near-identical scratch-worktree write dances
      into NotesBranchWriter. Known defects fixed here, each proven by a failing
      test first: a missing BEGIN-PERSONAL-NOTES marker pushed the entire
      CLAUDE.local.md (PR-progress section included) into the notes file; fixed
      scratch branch names raced between concurrent sessions; a rejected
      non-fast-forward push lost the edit after the cleanup trap deleted the
      committed scratch worktree; a trailing flag died with "$2: unbound variable"
      instead of a usage message. Flag names, stdout lines and exit codes are
      external API and stay unchanged, pinned by shim-level subprocess contract
      tests. Sequence after the in-flight bash-touching PRs (#107 #109 #110 #115
      #121 #126) land — a wholesale body rewrite cannot be merged by the
      whichever-lands-second-merges convention.

  - id: dev-tooling-save-plan-python
    branch: null
    title: "Bash→Python 3: save-plan and the plan manifest tools"
    track: dashboards
    depends_on: [dev-tooling-save-commands-python]
    status: not_started
    notes: >
      save-plan.sh becomes a shim; plan_manifest_tools.py is absorbed as
      plan_manifest.py (read-id, regenerate-branch-index, YamlBooleanCoercionError).
      New validation with curated errors instead of cp/traceback noise: the roadmap
      marker block must be non-empty (today an empty block silently overwrites
      roadmap.md) and the --manifest/--roadmap source files must exist.
      test_plan_manifest_tools.py carries over with an import change;
      test_save_plan_sh.py carries over as the shim contract test.

  - id: dev-tooling-session-start-python
    branch: null
    title: "Bash→Python 4: session-start behind an inert probing shim"
    track: dashboards
    depends_on: [dev-tooling-notes-core-python, session-start-plan-and-setup-guards, git-identity-from-personal-notes]
    status: not_started
    notes: >
      session-start.sh keeps its path (settings.json stays byte-identical) but its
      body becomes the one guarded shim: command -v python3 plus a >=3.11 version
      probe, exiting 0 with a single diagnostic line when absent, so the hook stays
      inert on machines without python3 (user-accepted floor raise — every targeted
      environment ships 3.11+). session_start.py is stdlib-only end to end (yaml,
      jinja2, markdown, nh3 blocked in an import test) and reproduces the 4-line
      stdout summary and every written marker/header byte-for-byte, pinned by
      golden-output fixtures recorded from the bash version before deletion. The
      tracking_issue grep pipeline that kills the hook under pipefail when the field
      is absent (found by #121's first session-start tests) becomes a re.search
      returning None. Depends on #121 and #126 so their run_hook_script subprocess
      tests carry over as the shim contract tests instead of colliding.

  - id: dev-tooling-setup-checks-python
    branch: null
    title: "Bash→Python 5: check-setup as typed rows"
    track: dashboards
    depends_on: [dev-tooling-notes-core-python]
    status: not_started
    notes: >
      check-setup.sh becomes a shim over setup_checks.py: SetupCheckIdentifier and
      SetupCheckStatus StrEnums, frozen SetupCheckRow, SetupReport with the exact
      TSV row order and exit-code semantics preserved (external API for
      setup-personal-notes and prerequisite-check.md). The parsing enums
      test_check_setup_sh.py already defines move into the production module and the
      test imports them instead. Two fixes: the settings.json check parses the JSON
      and verifies a SessionStart hook command rather than grepping for the
      substring session-start.sh anywhere in the file, and the inline python3
      heredoc becomes missing_requirements() via importlib.metadata. Runs parallel
      to the other conversions off the core item.

  - id: dev-tooling-dashboard-refresh-python
    branch: null
    title: "Bash→Python 6: refresh_dashboard orchestration in-process"
    track: dashboards
    depends_on: [dev-tooling-save-commands-python]
    status: not_started
    notes: >
      refresh_dashboard.sh becomes a shim over dashboard_refresh.py, which calls
      sync_manifest_status/build_dashboard in-process (dev-tooling-python-package has
      moved them into the package by this point) and pushes manifest corrections
      through NotesBranchWriter instead of shelling out to
      write-personal-notes-file.sh. refresh_dashboard_support.py — which exists only
      as JSON plumbing for the shell orchestrator — is deleted along with its
      REFRESH_DASHBOARD_SUPPORT_SCRIPT constant; count-corrected/merge-summaries
      become internal functions. The merged single-JSON stdout contract and the
      --plan-id flag surface stay unchanged; test_refresh_dashboard_sh.py carries
      over as the shim contract test.

  - id: dev-tooling-config-shim-slimming
    branch: null
    title: "Bash→Python 7: slim the sourced config file to constants"
    track: dashboards
    depends_on: [dev-tooling-save-commands-python, dev-tooling-save-plan-python, dev-tooling-session-start-python, dev-tooling-setup-checks-python, dev-tooling-dashboard-refresh-python]
    status: not_started
    notes: >
      resolve-personal-notes-config.sh keeps its sourcing contract (10 doc locations
      plus ci.yml twice source it for variables and the cd-to-root side effect —
      zero mandatory SKILL.md changes by construction) but shrinks to the cd
      preamble, the three NOTES_* resolutions, the path constants, and the trivial
      pure-string path helpers. The behavioral functions
      (fetch_personal_notes_branch, current_branch_upstream_remote,
      default_branch_name, pr_progress_path, plan_id_for_branch) are deleted once a
      grep of all SKILL.md/*.md confirms no prose instructs a session to call them.
      The residual bash/python duplication of the precedence rules is guarded by a
      CI consistency test asserting the sourced variables equal python3 -m
      development_tooling configuration output. hooks/README.md and plan-schema.md
      prose touch-ups ride along.
      Second carrier found 2026-08-05, while answering a review question on #143 about
      duplication with stack.py: stack.py already reimplements the same precedence in
      Python as _resolve_personal_notes_remote/_resolve_personal_notes_branch, whose own
      docstrings concede "by the same precedence as resolve-personal-notes-config.sh".
      So the consistency test above has two Python copies to hold equal, not one, and
      stack.py's is the older of them. plan_item_bootstrap.py (#143) deliberately adds no
      third: its fetch_notes_branch sources the shell file and calls its own
      fetch_personal_notes_branch rather than restating the rules.

  - id: dev-tooling-github-api-unification
    branch: null
    title: "One github backend: the gh-CLI-else-token rule implemented once"
    track: dashboards
    depends_on: [setup-personal-notes-script, shared-pr-state-chips, dev-tooling-notes-core-python]
    status: not_started
    notes: >
      github-api.sh (decision 9, arrives with #107) and pr_state (arrives with #111)
      implement the same gh-CLI-else-GH_TOKEN/GITHUB_TOKEN+curl dual-backend rule
      twice — the strongest concrete duplication on record in this plan. One
      development_tooling github_backend.py implements it once; github-api.sh
      becomes a shim. Blocked until both carriers land; scope-boxed to the backend
      rule, not the callers' features.

      Third carrier recorded 2026-08-05: .claude/stack/maintenance.py (#139) now makes the same
      calls a third time, in urllib rather than curl. Its review asked directly why not gh and why
      not a GitHub Python library, and both were deferred here rather than answered in that pull
      request - adding a third copy of the rule, even a better one, is what this item exists to
      stop. So this item's scope gains one decision it did not have: which backend all three
      converge on. gh is absent from a session container but present on Actions runners; PyGithub
      would be the natural client but is a dependency, and the SessionStart-reachable tier is
      stdlib-only by decision 12, so the choice turns on whether the shared backend is allowed to
      require an install - which is the same question for all three carriers and should be
      answered once here.

      Fourth carrier considered and declined 2026-08-07 by upstream-review-reader: that item
      needed the same GitHub access and chose gh api graphql, which its Actions context
      guarantees, rather than adding a fourth hand-rolled client. It is therefore evidence for
      this item's open question rather than another carrier for it - the install-or-not question
      only bites the SessionStart-reachable tier, and an Actions-only caller can already assume
      gh. The three recorded carriers are unchanged.

  - id: stack-board-single-site
    branch: null
    title: "PR 4: plans-only Pages site (stack board retired for the native stack map)"
    repository: AbdelrhmanBassiouny/stack-board
    track: cutover
    depends_on: [stack-tooling-on-main, shared-pr-state-chips]
    status: not_started
    notes: >
      Re-scoped 2026-07-31 per the user's one-dashboard decision: the board index is
      not built at all — GitHub's native stack map covers derived stack mechanics, and
      the plans dashboards are the single surface. board.yml checks out fork main (not
      the tooling branch) + fetches personal-notes for plans and config; publishes
      _site/ = plans/<id>/ per plan, refreshed every poll with no live session, via
      PR 3's build_site.py. Adds the every-fork-PR-belongs-to-a-plan invariant: the
      build lists open fork PRs and flags any that no plan.yaml claims. Parameterize
      repo/branch/upstream as repository variables (portability). Fix the three
      inconsistent poll-interval statements. dashboard-urls.yaml / Artifact publishing
      become optional legacy.

  - id: routine-cutover
    branch: null
    title: "Retire the scheduled LLM Routine: deterministic Action + on-demand sessions"
    track: cutover
    depends_on: [stack-tooling-on-main]
    status: in_progress
    session: https://claude.ai/code/session_017JMftn3ujp7xsyhwcMaF75
    notes: >
      Target revised 2026-07-31 (native-stacks pivot, user-approved): the endgame is no scheduled
      LLM run at all, finally aligning the Routine with the no-scheduled-checks rule. A plain
      scheduled Action takes the deterministic duties — fork-main fast-forward from cram2/main,
      label hygiene, cram2-link comment, plans-site build, and the happy-path cascade (gh stack sync
      is documented as safe in automation: fetches, fast-forwards the trunk, rebases the remaining
      branches, force-pushes, syncs PR status, aborts cleanly on divergence in non-interactive
      terminals; Actions runners ship gh). The judgment residue — cascade conflicts and red CI after
      a restack — runs in on-demand sessions, surfaced by the plans dashboard rather than a timer.
      The 2026-07-30 pointer-prompt design (roadmap addendum of that date: replacement prompt,
      update_trigger mechanism, gate checklist) is superseded as the endgame but survives as an
      optional interim step if the Action lags the tooling wave; the live Routine is trigger
      trig_01N79jHmLo3bSbg8pLM6MNTB ("PR Stack Monitor and Update"), and the final cutover deletes
      or disables it. Gate unchanged: only after PR 1 is on cram2/main and fork main fast-forwards;
      one green Action cycle flips this done. Base-change requirement, corrected 2026-08-02 by
      session-safe-pr-reparent's probe: the 403 recorded here is a property of the session git-proxy
      credential, not of sessions or of the operation - the MCP `update_pull_request` tool changes a
      base fine (verified on throwaway PR #129), so the "no session can ever do a real base-branch
      reparent" reading was wrong and no close+create fallback exists to inherit. The Action's own
      credential (default GITHUB_TOKEN or a stored PAT) is a third identity again, and the probe
      makes it much more likely to work than feared, since the block is narrow rather than
      platform-wide. Still verify it concretely with a real base change from the Action before
      relying on it; if it does 403, the Action can call the same MCP-equivalent REST endpoint under
      a token that does not carry the restriction rather than degrade to renumbering pull requests.
      Also fold in event-triggered detection rather than cadence-only: trigger the Action (or an
      additional one) on `pull_request` `closed`, ancestry-test that PR's head branch immediately,
      and reparent any open fork PR based on it right then if it turns out to have landed elsewhere
      - this is what actually prevents the #40/#41 orphan pattern from recurring with a multi-cycle
      delay, rather than only shrinking the window to one scheduled tick. Corrected 2026-08-02
      (traced #40/#41's actual history, at the user's request, to answer "who closed the parent and
      can automation stop that"): the human action (the user closing #40 as ordinary hygiene after
      rebuilding its branch, then normally merging its successor #53 nine days later) is not
      something automation should ever try to prevent - it is correct, deliberate maintenance, not a
      mistake. The trigger design above is one PR-closed-event narrower than it needs to be: the
      real defect is that ANY pull request leaving the open set drops it from board.json, whether it
      closes unmerged (#40) or merges normally and GitHub auto-closes it (#53) - `pull_request:
      closed` fires for both, so the Action should re-sweep git-ancestry for every remaining open
      fork PR's base on that one event type generally, not just check the closed PR's own branch. In
      this real case that would have caught #41's orphaning at #53's merge on 2026-07-25, days
      before a review round noticed it by accident on 2026-07-31. Interim pointer prompt ADOPTED AND
      LIVE 2026-08-02. The user replaced the Routine's ~17.5k inline prompt at
      claude.ai/code/routines with the short pointer this item had specified: HARD RULES inline
      (they must bind before any file is read), plus "read .claude/stack/ROUTINE.md and execute the
      fenced text block". It resolves origin/main first and falls back to
      origin/claude/stack-landed-parent-detection while that is in review, because .claude/stack/ is
      NOT on main yet - it is only on #106 and #117. So #117's branch is currently live production
      input, and an edit to ROUTINE.md now ships to the running workflow on push with no deploy
      step. This is the "optional interim step if the Action lags the tooling wave" this item
      already described, so the endgame is unchanged: a plain scheduled Action plus on-demand
      sessions, no scheduled LLM. What the adoption buys now is that doctrine corrections stop
      needing a manual paste - which is what let the base-PATCH error sit in the live prompt after
      ROUTINE.md had been fixed. Adoption immediately exposed a latent ordering bug, fixed in #117:
      SETUP step 0 told the Routine that stack.py and stack.toml "are already on this checkout -
      they live on main". Written in anticipation of #106 landing, false today, and harmless only
      while nothing read it. Under the pointer the Routine would resolve its doctrine fine, believe
      the tooling was present, then fail on the first stack.py call in Phase 2 - after Phase 1 had
      already mutated pull requests. Step 0 now fetches .claude/stack/ from the same ref rather than
      asserting it. Verified end to end in a worktree at origin/main: stack.py absent before,
      `stack.py --help` working after. Two deletions are due when #106 lands and .claude/stack/
      reaches main: the pointer prompt's source-2 line (needs a manual paste) and step 0's fetch
      fallback (a normal commit). Neither breaks anything if forgotten - the fetch becomes a no-op
      and the fallback ref stops resolving - but both are dead weight.

  - id: tooling-branch-retirement
    branch: null
    title: "Tag and delete claude/stack-workflow-tooling after one green cycle"
    track: cutover
    depends_on: [routine-cutover, stack-board-single-site]
    status: not_started
    notes: >
      Only after one full routine cycle and one board-Action cycle run green on the new
      paths. Tag for history first. Platform constraint (see roadmap addendum
      2026-07-29): tag pushes and branch deletes get a 403 through a session's git
      proxy, so those two steps must run outside the harness (user's machine or gh
      api); a session can still do all verification and prepare the exact commands.

  - id: native-stacks-prototype
    branch: null
    title: "Prototype GitHub's native stacked-PRs preview against our stack tooling"
    track: stack-tooling
    depends_on: []
    status: done
    session: https://claude.ai/code/session_015db4P7UfiFTTfrbxYjU5yW
    notes: >
      Evaluation + live prototype of GitHub's stacked-PRs public preview, run on
      throwaway proto-* branches in the stack-board repo (trunk = its own throwaway
      branch; main untouched). All probes conclusive - full findings and their
      per-item consequences in the roadmap addendum of 2026-07-31. Headlines: every
      stacks REST endpoint works from a session's installation token (API version
      2026-03-10) - read, create (drafts accepted, non-main trunk, works beyond the
      fork so the preview is account-wide), extend, dissolve; merge is a new async
      endpoint (merge-async + poll) that merges everything below in one operation
      and auto-retargets the PRs above within seconds, refusing stale stacks with a
      precise not-a-linear-descendant error and refusing drafts; the classic merge
      and update-branch APIs are hard-403'd for stacked PRs (including via the
      GitHub MCP server). The one gap: no REST endpoint triggers the server-side
      cascading rebase - cascade is UI-only or a local git rebase+force-push, which
      preserves stack membership (verified). pull_request webhook/Actions payloads
      carry a stack object (id/number/position/size/base), retargets arrive as
      "edited" events, and the stack base.sha lagging the real trunk head is a
      machine-readable staleness signal. User decisions recorded: one dashboard
      only (the plans dashboard; the stack-board surface dies; every fork PR must
      belong to a plan, enforceable at dashboard build), and Stack #112 already
      wraps the seven D-core PRs on the fork. The pivot re-scopes were approved
      2026-07-31 and are recorded on the affected items (stack-tooling-on-main,
      setup-stacked-prs-skill, stack-board-single-site, routine-cutover). Round-2
      probes (same date, second addendum): PATCH base 422s on stack members (the
      Phase 1 REPARENT hazard), push-based merges are detected as merged but do not
      auto-retarget children (unlike merge-async), and unstack → PATCH base works as
      the API-only recovery. Leftover cleanup: proto-* branches on stack-board need
      out-of-harness deletion (session branch-delete 403).

  - id: plan-updates-since-helper
    branch: claude/plan-item-kickoff-workflow-n814iz
    title: "plan-updates-since.sh: delta recheck of plan state and tracking-issue comments"
    pull_request_number: 115
    track: personal-data
    depends_on: []
    status: done
    session: https://claude.ai/code/session_01UrxbEr6ZHMThA2p4Vcnrqv
    notes: >
      Implements the recheck-deltas convention recorded in cram-notes.md (2026-07-31): a small hook
      script next to save-plan.sh that, given a plan id and the last-seen personal-notes commit SHA,
      fetches the notes branch and prints only the delta - `git diff <sha>..FETCH_HEAD --
      .claude/personal/plans/<id>/` - plus the plan's tracking-issue comments newer than that
      commit's timestamp (github-api.sh dual backend once #107 lands, else curl). session-start.sh
      stamps the SHA it loaded plan state at (gitignored file) so sessions have the baseline for
      free; the script updates the stamp. Sized as a small independent PR off fork main, like #109 -
      not part of the #101 chain. Tests via ScratchRepository, network-free. Kicked off 2026-07-31
      via /plan-item-kickoff: confirmed with the user this is based off fork main directly,
      independent of #109 (not stacked on it) - "off fork main, like #109" in this item's own notes
      means the same *kind* of independent, main-based PR, not a stack on #109's branch. Expect a
      textual overlap in the same two files #109 already edits (resolve-personal-notes-config.sh,
      session-start.sh) - same no-depends_on, whichever-lands-second precedent already established
      for #107/#109/ #110 sharing files with no dependency between them. github-api.sh is confirmed
      not yet on fork main (only on #107's branch, which this item doesn't depend on), so the
      dual-backend GitHub call this item needs (fetching tracking-issue comments) is implemented
      inline, matching github-api.sh's gh-then-curl+token style, rather than depending on that file
      - a later cleanup once #107 lands can dedupe the shared credential/repo-resolution helpers.
      Implemented as draft PR #115 (2026-07-31), same session, based directly on fork main
      (claude/plan-item-kickoff-workflow-n814iz): PLAN_STATE_SYNC_STAMP + record/read helpers in
      resolve-personal-notes-config.sh, an unconditional stamp call in session-start.sh, the new
      plan-updates-since.sh script, .gitignore/README updates, and test_plan_updates_since_sh.py (13
      new tests, self-contained scratch-repo + stubbed gh/curl fixtures since neither sibling's
      shared fixture has landed on main yet). All 29 tests under .claude/hooks/tests pass. Verified
      live against the real personal-notes branch and tracking issue #102. Two real bugs caught by
      the tests before they shipped: an f-string with escaped quotes inside the expression (invalid
      before Python 3.12, and this repo's own test tooling runs 3.11), and a missing `|| true` on
      the default_repository grep that let `set -e` silently kill the script on a plan with
      tracking_issue but no default_repository. Resolved 2026-08-05 via /plan-item-resolve (session
      https://claude.ai/code/session_016kAVapbHxDokYjZBukKA5X): the item had sat in_progress since
      2026-07-31 with notes asserting readiness and no blockers field, while the pull request was
      mergeable_state: dirty and labelled needs-resolution - the same stale-entry failure #109's
      resolution recorded two days earlier. The conflict was .gitignore (two independent additive
      blocks, concatenated) and .claude/hooks/README.md. The README was not merely a conflict: this
      branch was cut before #101's 378-to-140-line rewrite, and an earlier merge here resolved that
      rewrite hunk-by-hunk, resurrecting the pre-#101 "Setup: overriding the default
      remote/branch/path" and "Verifying it worked" sections alongside the "Configuration" sections
      that had replaced them, and stranding the labels list's closing sentence 75 lines from its
      list. Re-authored against main rather than merged, per #109's own generalization, and only
      after confirming both resurrected sections are superseded verbatim. The branch's README delta
      is now +12/-2, was +86/-0. resolve-personal-notes-config.sh and session-start.sh auto-merged
      and were checked semantically, not just textually - the summary block carries both #109's
      local-settings line and this branch's plan-state-SHA line. Checked for the same-artifact-twice
      pattern and found no instance: the test module already imports main's shared
      ScratchRepository, tests/stubs/ has no counterpart on main, and plan_updates_since_support.py
      does not overlap plan_manifest_tools.py. 243 tests pass across the two suites CI runs (194
      plan-dashboard, 49 hooks - this module's 13 plus main's 36), none lost. Verified live:
      session-start.sh prints all five summary lines and writes the stamp, and plan-updates-since.sh
      against the real plan returned a delta that included a manifest change another session had
      pushed mid-resolve. Pushed as 99c92c3c; mergeable_state back to unstable, needs-resolution
      dropped (full label set re-sent so cram2-link-sent survived), pull request back to draft. This
      unblocks its half of decision 12's sequencing constraint, which cannot land ahead of the
      in-flight bash-touching pull requests. Merged into fork main 2026-08-05, hours after the
      resolve above cleared its conflict. Verified by content rather than from the merge
      notification alone: 99c92c3c is a git merge-base --is-ancestor of origin/main, and
      plan-updates-since.sh, plan_updates_since_support.py and test_plan_updates_since_sh.py are all
      present on main, with README.md at the re-authored 180 lines. The recurring
      test_world_sim_state_sync Mujoco flake, red on this pull request since 2026-07-31, passed on
      the post-merge run, so the branch went green on every check rather than merging with a known
      red. The convention this item implements is therefore live on main: session-start.sh stamps
      the notes-branch commit on every run, and any session can now recheck a plan by delta instead
      of rereading it.

  - id: ready-to-start-dependency-free-fix
    branch: claude/funny-feynman-wexezs
    title: "Fix ready-to-start dashboard list dropping dependency-free items"
    pull_request_number: 103
    track: dashboards
    depends_on: []
    status: done
    session: https://claude.ai/code/session_016mfBqcBibxA7Wa5eNfWtoL
    notes: >
      Pre-existing bug-fix PR (open before the plan tracked it; adopted 2026-07-31 under
      the every-fork-PR-belongs-to-a-plan rule). Fixed exactly the
      build_dashboard.py:_compute_next_steps guard that skipped items with no
      dependencies, which resurfaced when plan-updates-since-helper (depends_on: [])
      failed to appear in the sidebar. Based on fork main, independent of the #101
      chain. TDD per AGENTS.md: added test_dependency_free_not_started_item_is_ready_to_start
      (proved the bug, failed before the fix) and a companion
      test_dependency_free_blocked_item_is_neither_list pinning that a dependency-free
      blocked item stays out of both lists. Two pre-existing tests
      (test_blocked_item_with_partial_dependencies_done_is_recheck_candidate,
      test_not_started_item_with_partial_dependencies_is_neither_list) had an incidental
      dependency-free NOT_STARTED fixture item that legitimately became ready-to-start
      under the corrected semantics; changed that item's status to BLOCKED so each test
      keeps isolating only the behavior it names, without touching any assertion. 189
      plan-dashboard tests + 16 hook tests green. Merged 2026-07-29 - the one recurring
      CI red (semantic_digital_twin's test_world_sim_state_sync physics-settling flake)
      was noted on the PR as pre-existing/unrelated and cleared on the automatic re-run.
      Textual overlap resolved with #111 and #105, which also edit build_dashboard.py.

  - id: dependency-chips-blocked-fix
    branch: claude/plan-dashboard-dependencies-1am49n
    title: "Show unmet dependency chips as blocked on plan dashboards"
    pull_request_number: 105
    track: dashboards
    depends_on: []
    status: done
    notes: >
      Pre-existing bug-fix PR, adopted 2026-07-31 under the every-fork-PR-belongs-to-a-plan rule.
      Renders an item's unmet dependencies as blocked chips instead of neutral ones on the
      dashboards. Based on fork main, independent of the #101 chain; bug + in-review labels -
      already promoted to cram2. Same build_dashboard.py overlap note as its sibling fix (#103) and
      #111.

  - id: merge-timestamp-required-fix
    branch: claude/cram2-main-drift-4owm9a
    title: "Reject closed pull request data that omits merged_at"
    pull_request_number: 119
    track: dashboards
    depends_on: []
    status: done
    session: https://claude.ai/code/session_01BsUHGwZaRpxo97jVDUmaau
    notes: >
      Opened 2026-07-31 from the false drift flags this plan's own dashboard raised
      against #103 and #105 ("closed without merging" for two pull requests GitHub
      records as merged at 2026-07-31T10:36:43Z). Root cause is the hand-assembled
      pr_data.json: merged_at is the only signal separating merged from closed-unmerged
      (state is "closed" for both), list_pull_requests lets a caller drop it via fields,
      and pr-data-fetching.md only ever required labels. #101 escaped the same fetch
      because it carries the hand-applied merged label. from_mapping now raises
      MissingMergeTimestampError for a closed entry missing the key, keeping
      key-present-and-null as the genuine closed-unmerged case, and the fetch doc names
      the minimum field set. Draft PR #119, bug label, based on fork main and
      independent of the #101 chain; build_dashboard.py overlap with #103/#105/#111.

  - id: deferred-dependency-drift-check
    branch: null
    title: "Flag an item stacked on a deferred/closed dependency instead of leaving it to a stale note"
    track: dashboards
    depends_on: []
    status: not_started
    notes: >
      Found 2026-08-12 while investigating rdr-refactor's D-ui stack: D-ui-splice-fix (#78)
      was closed as superseded (the fix landed elsewhere, via dag-facade-hardening's
      insert-at-ownership-parentage, #118), but D-ui-rendering (#79) and D-ui (#76) still
      carry depends_on: [D-ui-splice-fix] and are still in_progress, with no automated
      signal anywhere that their base is dead - only a previous session's freeform note,
      which nothing re-reads or re-verifies. Root cause: build_dashboard.py's
      _drift_description_of only compares an item's own status against its own live PR
      state; it never checks a dependency's status against the dependent's. Item.
      is_ready_to_unblock_dependents() has the right predicate already (correctly False
      for a deferred/closed_unmerged dependency) but is only invoked on demand, per named
      item, by plan-item-kickoff/plan-item-resolve - never proactively across the whole
      graph on a dashboard refresh. And _compute_next_steps only evaluates
      not_started/blocked items, so an in_progress item stacked on a dead dependency is
      skipped by every existing check. Same defect shape as landed-parent-detection (#117,
      stack-tooling track): that fix taught restack_plan to decide a parent has landed from
      git ancestry rather than board membership; this item is the analogous fix for
      plan-dashboard's own depends_on graph. Proposed fix: a drift check for any item whose
      status is not deferred/done but whose depends_on includes an item that is itself
      deferred or closed_unmerged, surfaced the same way other drift is (not a note),
      ideally naming that dependency's own depends_on as the suggested reparent target.
      Not implemented here - left for another session.

  - id: landed-parent-detection
    branch: claude/stack-landed-parent-detection
    title: "Decide a parent has landed from git ancestry, not board membership"
    pull_request_number: 117
    track: stack-tooling
    depends_on: [stack-tooling-on-main]
    status: done
    session: https://claude.ai/code/session_01URVDhViP2TijuQAHAysUKL
    notes: >
      Bug fix, found while handling a review round on PR #41. board.json carries only
      OPEN fork PRs, so a parent branch whose own PR was closed is absent from it, and
      both consumers of the parent lookup read that absence as "no parent":
      restack_plan left the child on the stale parent instead of reparenting it onto
      the upstream base, and parent_landed treated the missing parent as a root and
      reported the child promotable. Stack now carries the merged predicate and answers
      has_landed_upstream() from git ancestry - the test the doctrine already names as
      the definition of merged - and both call sites derive from it. ROUTINE.md Phase 1
      gets the matching rule (ancestry-test the base branch of every open fork PR whose
      base is neither main nor another open PR's head), and carries the full
      dissolve -> PATCH -> restack -> re-create sequence for the stack-member 422 that
      native-stacks-prototype logged as the Phase 1 REPARENT hazard, matching the
      doctrine already live on the Routine. That last point was reversed mid-review: the
      first revision treated the 422 as report-and-continue, which optimised for the
      diff-size symptom rather than the goal - a child left on a landed base cannot
      reach main and is closed outright when Phase 1 deletes that base, so deferring the
      reparent leaves the PR broken while the routine re-reports it every run. The
      doctrine now also rules out fast-forwarding the landed base branch as a shortcut:
      it fixes the merge-base only, and desynchronises a stack's recorded base.sha when
      that base is the stack's trunk. Three TDD tests, 247 dev-tooling
      tests green. Stacked on stack-tooling-on-main since the code it fixes is not on
      main yet; the identical fix was also pushed to claude/stack-workflow-tooling's
      dev/ copy (78a093e4, 37/37 there) so the live Routine stops orphaning children
      before routine-cutover - that copy dies with tooling-branch-retirement. The live
      Routine's own Phase 1 prompt text (trig_01N79jHmLo3bSbg8pLM6MNTB) is NOT updated:
      the user asked for the patch text to review and paste rather than a direct
      update_trigger call, so until they do, restack-plan emits the right parent but
      Phase 1 still will not retarget on GitHub.
      Validated 2026-08-02 (/plan-item-resolve session) against the real PR #41 stack
      the bug was found on, not just the item's own synthetic tests: confirmed live that
      ripple-down-rules-refactor (PR #40's head) is still an ancestor of origin/main while
      PR #41 is still based on it (unreparented), and that #41 is native GitHub Stack #112
      (#41 -> #63 -> #64 -> #65 -> #66 -> #67 -> #98, all open). Ran restack_plan/
      parent_landed from both #106's pre-fix stack.py and this item's fixed stack.py
      against that real 7-PR set (real git-ancestry predicate, not a stub): pre-fix
      restack_plan reparents #41 onto the stale ripple-down-rules-refactor branch exactly
      as the bug describes; the fixed version correctly emits parent: main for #41 and
      leaves #63..#98's own parent chain untouched. This was read-only (local script, no
      push, no GitHub write) - the live reparent (dissolve Stack #112 -> PATCH #41's base
      -> restack -> re-create) was proposed and the user explicitly approved executing it.
      Attempted the same session: dissolved Stack #112 (POST .../unstack, 204) succeeded,
      but PATCH .../pulls/41 with a new base 403'd with "Changing a pull request's base
      branch is not permitted for this session type". That was read at the time as a
      platform-level block on ANY base change from a session; it is not - see the
      correction below.
      Per ROUTINE.md's own stop-and-report rule, re-created the stack immediately with the
      same 7-PR list to avoid leaving it dissolved for no gain - GitHub assigns it a new
      number (Stack #128; #112 cannot be reused once dissolved) but the PR list, order and
      every base are otherwise identical to before the attempt. PR #41 was left unchanged that day.
      Corrected 2026-08-02 (later, by session-safe-pr-reparent's probe on throwaway PR
      #129): the 403 is a property of the session git-proxy credential, not of sessions.
      The MCP `update_pull_request` tool changes a base successfully; only the raw curl +
      GH_TOKEN PATCH is refused, and the earlier attempt reached for curl because the
      stacks endpoints have no MCP tool. So this item's doctrine was right about the
      mechanics and wrong only about the client, the "no session can ever PATCH any base"
      reading does not hold, and base-change is not a third member of the
      tag-push/branch-delete family (those two have no MCP tool at all). ROUTINE.md's
      REPARENT paragraph and the NATIVE-STACK MEMBERS sequence are both session-executable
      once they name the MCP tool, which this branch now does directly: the correction was
      first opened as stacked PR #133 and then folded into this PR, so #117 no longer ships
      a step that 403s. PR #41 has since been reparented onto main with that sequence -
      268 files/+27,825 down to 7 files/+1,318, number, labels and review thread all kept -
      so this item's fix is now validated live end to end rather than only in its own
      tests.

  - id: session-safe-pr-reparent
    branch: claude/stack-landed-parent-detection
    title: "Reparent a base through the GitHub MCP server, not a raw PATCH"
    pull_request_number: 117
    track: stack-tooling
    depends_on: [landed-parent-detection]
    status: done
    session: https://claude.ai/code/session_01WvRrTrtiznzAyvaoeCdNSq
    notes: >
      Premise disproven and item re-scoped 2026-08-02. It was spun out of
      landed-parent-detection on the reading that PATCHing any pull request's base 403s
      from a Claude Code session unconditionally, and was to replace every reparent with
      close-old-PR -> create-new-PR. A direct probe on throwaway PR #129 showed the block
      is a property of one credential, not of sessions: the MCP `update_pull_request`
      tool changed the base successfully (200), while a raw PATCH with curl and GH_TOKEN
      through the session git proxy 403'd on the same pull request minutes apart. The
      earlier attempt only ever saw the 403 because the stacks endpoints have no MCP tool,
      so that session was already in curl and stayed there.
      The stack-member 422 is unchanged and orthogonal, and the full repair
      (dissolve -> MCP base change -> re-create) was rehearsed end to end on a throwaway
      stack (204/200/201) - which is exactly the sequence landed-parent-detection already
      prescribes. That item was right about the mechanics and wrong only about the client.
      So no close+create fallback, and no reparent-recovery script: the script this item's
      notes previously said #106 "already scopes" was never actually written (verified by
      git ls-tree on both #106 and #117 - .claude/stack/ holds only README.md, ROUTINE.md,
      stack.py, stack.toml, tests/), and with the base change available there is nothing
      for it to work around. Not writing new mechanics also matches decision 11's
      direction: GitHub maintains the mechanics, we maintain policy.
      What shipped: a "BASE CHANGES GO THROUGH THE GITHUB MCP SERVER" rule stated once in
      Phase 1 recording the 403 and its cause, both reparent sites deferring to it, step 3
      of the native-stack sequence using the MCP tool and stating that the child keeps its
      number/labels/review thread, and the README source-of-truth row naming the tool.
      Four contract tests written failing first over ROUTINE.md, which is prose and had no
      coverage at all; 251 tests pass in the dev-tooling suite, was 247.
      This item has no pull request of its own. It was opened as draft PR #133 stacked on
      #117, then folded into #117 itself on the user's instruction that a PR must be
      self-sufficient and correct: #133's only purpose was correcting a section #117 had
      just introduced, so leaving it stacked meant #117 would sit in review carrying an
      instruction already known to 403. The commit fast-forwarded onto
      claude/stack-landed-parent-detection, which GitHub detected as merging #133 into its
      own base and auto-closed it - merged into that branch, not into main. #117's
      description was rewritten to explain the 403-vs-422 distinction directly so it stands
      alone, and #117 was returned to draft per the always-re-draft-after-pushing rule.
      Both this item and landed-parent-detection therefore point at #117 and land together.
      Validated live on PR #41, the case the whole thread started from: recorded Stack
      #128's composition, dissolved it (204), changed #41's base to main via the MCP tool,
      re-created the stack (201). #41's diff went from 268 files/+27,825 to 7 files/+1,318
      - exactly the four rdr/ modules and three test files its own description names -
      mergeable_state clean, and it kept its number, its four comments and its
      cram2-link-sent label. Nothing was pushed to any branch; only the base moved.
      Stack #134 carries the same seven pull requests in the same order, now trunked on
      main; #63-#98's own bases were untouched. GitHub will not reuse a dissolved stack's
      number, so #112 -> #128 -> #134 is expected, not a symptom.
      Note for anyone reading the roadmap's session-blocked-operations thread: base-change
      is NOT a third member of the tag-push/branch-delete family. Those two have no MCP
      tool at all and stay genuinely blocked; this one simply needed the other client.

  - id: ready-to-promote-upstream-links
    branch: null
    title: "PR 5: ready-to-promote sidebar list + one-click upstream create-pull-request links"
    track: dashboards
    depends_on: [shared-pr-state-chips, sidebar-bug-fix-chips]
    status: not_started
    notes: >
      Adds a fifth sidebar group to the plan dashboard - "Ready to promote" - listing every
      item whose fork pull request is open-and-ready (out of draft, i.e. self-reviewed),
      cleanly mergeable, and based on the fork's default branch, and gives each one a
      one-click link that opens the upstream compare-and-create page with the title and
      body prefilled and the fork pull request linked from it. This is ROUTINE.md Phase 3's
      link building turned into a surface instead of an emailed summary: the same
      compare URL format, computed deterministically at dashboard build time, so it needs
      no scheduled LLM run to reach the user - which is exactly what routine-cutover's
      endgame requires of every deterministic duty. Portability rule applies as everywhere
      else: the upstream is a new optional plan.yaml field (upstream_repository, plus an
      optional upstream_base defaulting to the fork's own base), never a hardcoded cram2;
      a plan without it renders no group and no links. Decisions settled with the user at
      planning time (2026-08-01): the link builder lands in the development_tooling package
      PR 3 creates, next to pr_state, so the scheduled Action can reuse it verbatim; the
      prefilled body is the fork pull request's own description truncated to one paragraph
      plus a "Full detail" link back to it, matching the doctrine's existing warning that a
      compare URL silently drops an over-long body; check state is shown as a chip and
      never excludes an item, since a red robotics job routinely has nothing to do with the
      pull request under it; and an item leaves the list when the user applies the in-review
      label after clicking Create, with cram2-link-sent left alone entirely - it exists only
      to stop the Routine re-emailing a link, which a stateless recomputed list does not
      need. Requires three new fields in pr_data.json that PR 3's pull request entry does
      not yet carry (base, title, body) plus pr-data-fetching.md's minimum field set updated
      to match; mergeable and the check rollup come from PR 3 as-is. Sequence before
      dev-tooling-python-package, which moves build_dashboard.py wholesale.

  - id: sidebar-bug-fix-chips
    branch: claude/dashboard-bugfix-pr-section-161vf8
    title: "Mark bug-fix pull requests with a chip in the dashboard sidebar"
    pull_request_number: 120
    track: dashboards
    depends_on: []
    status: done
    session: https://claude.ai/code/session_01UkRVjgoWouwbndZa1siY29
    notes: >
      Started 2026-08-01 to surface bug-fix pull requests in the plan dashboard's "What
      to do next" sidebar. First attempt was a separate top "Bug fixes" group with a red
      outline; the user rejected it on the grounds that the sidebar groups by action and
      being a bug fix is not one - such items already sit in ready-to-start,
      blocker-may-be-cleared or ready-to-review - and that a fifth outline would collide
      with the existing per-group outlines, which are not mutually exclusive with it. It
      is therefore an attribute, rendered as a small bug chip on the entry wherever the
      item already appears, never moving an item between groups. Adds Item.is_bug_fix
      (filled from the pull request's bug label via a new _pull_request_record_of lookup),
      which gives the already-defined but unused PullRequestLabel.BUG its first consumer,
      and collapses the four near-identical sidebar group blocks into one next_step_group
      Jinja macro so the chip has a single home. Independent of the #101 chain, based on
      fork main; build_dashboard.py overlap with #103/#105/#111/#119 is the usual
      whichever-lands-second-merges pattern for this track.
      Extended 2026-08-01 with the filter the chip made possible: a "Bug fixes only"
      checkbox on the sidebar card hides every non-bug entry and any group left holding
      none, rendered only when some entry is a bug fix so it can never empty the card.
      Both counts per group are rendered up front and swapped by CSS rather than
      recomputed in script, matching the done-items toggle's two precomputed indent
      levels. The committed example now demonstrates both - pull request #103 carries a
      bug label - and all three walkthrough screenshots were regenerated from it
      (the third, dashboard-bug-filter.png, is new and shows the filter applied).
      206 tests passing.
      Placement settled 2026-08-01: stays based on fork main, exactly one commit off the
      tip, touching only plan-dashboard files and needing nothing from the #101/#106
      chain - the same treatment #103, #105 and #119 get. It carries no bug label of its
      own: it surfaces bug fixes, it does not fix a bug. The one real ordering constraint
      is downstream, recorded as a depends_on on ready-to-promote-upstream-links: that
      item adds a fifth sidebar group, which is one next_step_group call once this macro
      has landed and a fifth copy of the markup if it has not. Also sequence before
      dev-tooling-python-package, which moves build_dashboard.py wholesale. Opened as
      draft pull request #120 on 2026-08-01, no bug label, subscribed to its activity.
      State 2026-08-12: no longer a draft - the user took it out of draft themselves, and
      it carries in-review, so the upstream pull request is open and this session does not
      re-draft it. The subscription recorded above is gone, per no-pr-subscriptions.
      Second base merge resolved 2026-08-12 (299d1d53), after fork main was
      fast-forwarded from cram2 to e123c383, 142 commits past the previous merge base. The
      two conflicts are the ones predicted for whoever landed second behind #122:
      test_build_dashboard.py, where both sides added tests at the same point and both are
      kept, with the example-locking assertion taking #122's now-correct two-entry
      ready_to_review; and dashboard-overview.png, re-rendered from the merged example
      rather than taken from either side, so it shows #122's second ready-to-review entry,
      #124's collapsed notes and this branch's chip and filter together. The other two
      screenshots were checked and left alone - the filtered sidebar hides the group that
      changed, and the action-buttons crop shows cards with no notes to collapse.
      399 tests passing across the three directories CI runs. needs-resolution left for
      the next maintenance pass to clear itself.

  - id: session-start-plan-and-setup-guards
    branch: claude/workflow-unification-setup-jgvs53
    title: "Make the plan-item and setup steps impossible for a session to skip"
    pull_request_number: 121
    track: personal-data
    depends_on: []
    status: done
    session: https://claude.ai/code/session_01JSxeoePmxWoA4aETrwhFcx
    notes: >
      Recorded 2026-08-01 from two process failures in the sidebar-bug-fix-chips session,
      both of which the conventions already forbade in prose and neither of which anything
      enforced. (1) Work began, was implemented and was pushed before the plan item
      existed, even though the request named the plan in its first sentence: session-start
      prints a bare "plan: none" for a branch absent from the generated index, which reads
      as "no plan applies here" when it can equally mean "the plan you were told about has
      no item for this branch yet". (2) check-setup.sh was never run, so the plan-dashboard
      dependencies were discovered one ModuleNotFoundError at a time across two
      interpreters, when one read-only call reports the whole picture. Three fixes, in
      increasing strength: make session-start.sh's plan line name the ambiguity and say
      what to do about it; have session-start.sh run check-setup.sh and surface a non-zero
      exit rather than leaving it to be remembered; and, if enforcement rather than
      prompting is wanted, a PreToolUse hook on Edit|Write that refuses the first source
      edit from a branch with no plan item, with an explicit opt-out for branches that
      genuinely have no plan. The first two are the recommendation; the third is the only
      one that makes the miss impossible rather than merely unlikely. Plan mode was
      considered and rejected as the mechanism: it would probably have caught the first
      failure but rests on the same judgment that missed it, and would not have caught the
      second at all. Deliberately left unimplemented here - the user is assigning it to its
      own session.
      Kicked off 2026-08-01 via /plan-item-kickoff and implemented the same session as draft
      PR #121, based on fork main and independent of the #101/#106 chain. Scope settled with
      the user at kickoff: fixes 1 and 2 only, with the third split out as
      plan-item-edit-guard below. The user's own framing reshaped both - a clone whose owner
      wants neither plans nor personal notes must see none of this - which turned out to need
      no new setting at all: everything added sits after the existing
      `fetch_personal_notes_branch || exit 0`, and within the group that does use the branch,
      whether the generated branch index exists separates notes-but-no-plans from plans-in-use.
      Delivered: five distinct plan-line outcomes (including an index entry whose manifest has
      gone missing, previously indistinguishable from having no plan); a setup line carrying
      check-setup.sh's needs-setup rows, run after CLAUDE.local.md is written so its
      claude_local_md check sees this run's output, and captured with `|| true` so a setup gap
      can never fail the hook; plan_branch_index_exists/tracked_plan_count in
      resolve-personal-notes-config.sh, which owns that path; and the first tests
      session-start.sh has ever had (9, TDD, 6 failing first). 231 tests green across both CI
      directories, was 222. One latent crash found by writing them and fixed here: a plan with
      no tracking_issue made grep exit 1, which pipefail and set -e turned into the hook dying
      with no output at all - only plans that set tracking_issue, as this one does, ever
      escaped it. ScratchRepository gained run_hook_script and write_setup_prerequisites, the
      fully-set-up layout test_check_setup_sh.py previously built inline. Textual overlap with
      #109 and #115 in the same summary block, resolve-personal-notes-config.sh and
      scratch_repository.py is the usual whichever-lands-second pattern; the new
      fixtures/plan-with-tracking-issue.yaml is byte-identical to #115's so the two cannot
      conflict on it.
      Extended 2026-08-01 after the user raised the case the first pass missed: someone who
      uses both plans and personal notes still does work that is purely a notes edit, never
      merged upstream and never off main, which needs no plan item at all. The prompt was
      firing on the default branch, the notes branch and a detached HEAD - none of which a
      plan item can ever track - so those three now report "not applicable", exactly as the
      PR-progress line directly above them already did via pr_progress_path. The two
      predicates are kept as separate copies of the same three cases rather than shared,
      because they are expected to diverge: see plan-item-edit-guard for the notes-targeting
      pull request, which still wants PR progress but never wants a plan item. An index entry
      naming the branch still wins over the whole check, so a tracked branch is unaffected.
      Two TDD tests, 233 green (was 231).

      Resolved 2026-08-07 via /plan-item-resolve (session_01JF8sH4isJxrY6Ca5NgD7es) after
      stalling since 2026-08-01 on two blockers, neither of which this notes field recorded -
      the third time on this plan the manifest has been the least accurate source, after #109
      (2026-08-03) and #115 (2026-08-05), and both times everything needed to diagnose it was
      on the pull request itself. (1) mergeable_state dirty against main, labelled
      needs-resolution, reported twice by the routine. Both conflicts were additive-vs-additive
      at a shared location and resolved keeping each side: session-start.sh runs #109's
      personal-settings sync and then this branch's setup-verdict block, and prints both
      `setup:` and #115's `plan state SHA:`; resolve-personal-notes-config.sh carries this
      branch's branch_can_hold_plan_item / plan_branch_index_exists / tracked_plan_count beside
      #115's PLAN_STATE_SYNC_STAMP helpers. The setup verdict's comment now says "last of
      everything the hook writes" rather than naming only CLAUDE.local.md, which is the
      constraint #126's git-identity write also has to sit before. #115's warning that a
      conflict report understates a conflict against a restructured document was applied - the
      merged README and session-start.sh were read end to end, and neither carries a
      resurrected or stranded section. (2) Five review threads from 2026-08-01, none outdated,
      none answered. Four are done and resolved: the tooling-file paths are a
      SetupPrerequisiteFile StrEnum with the requirements file among them; the set-up-clone
      layout is a checked-in fixture tree at tests/fixtures/set-up-clone/ mirroring the paths
      it occupies in a scratch root, so settings.json and requirements.txt are real files of
      their own type and write_setup_prerequisites is one copytree call; the summary wording
      moved into session-start-messages.sh, sourced by the hook and rendered by the tests so an
      assertion is no longer a second copy of the sentence; and the README paragraph speaks in
      the third person. The wording move carried #135's recorded trap - single-sourcing an
      external contract deletes the guard the duplicate literals were providing - so
      test_every_summary_message_reads_as_written owns the wording and nothing else does,
      verified by mutation rather than assumed. The fifth thread ("this won't be needed once
      you do my previous comment", on write_setup_prerequisites) is deliberately left open: the
      method shrank to one line but survives, because leaving CLAUDE.local.md out of it is what
      lets the session-start tests run against a clone that has none yet, and reading it as
      "delete the method" was an interpretation rather than something the reviewer said. Two
      things the fixture tree forced that are worth carrying: the repo ignores *.txt, so the
      fixture's requirements.txt needed a .gitignore exception rather than a silent `git add
      -f` (same treatment the example screenshots get for *.png), and it was only caught by
      diffing the staged tree against main - the local suite passed either way, and CI on a
      fresh checkout would not have. Every suite was therefore re-run from a clean clone as
      well as in place. 350 tests green across the three directories CI runs, was 349 after the
      merge and 233 before it. Pushed as 862da392, mergeable_state clean, needs-resolution
      dropped by re-sending the full label set per #139's replace-not-add finding, still a
      draft, description rewritten to match. Pushed to this item's own branch rather than the
      resolving session's designated one, per the precedent the #115 resolve set: a new branch
      is a new pull request, which is what produced #125.

      Second review round the same day, three comments, applied in b57d3902. Two were
      straightforward: the message names the tests pass became a SummaryMessage StrEnum, and the
      README/fixture work stood. The third reversed this session's own recommendation and is the
      one worth carrying. test_every_summary_message_reads_as_written - added hours earlier
      specifically to replace the guard that single-sourcing the wording had removed, the #135
      ReportKey precedent applied - was cut on the user's instruction ("remove these wording
      assertions or replace only one of them by being not empty"). Replaced by
      test_every_summary_message_renders_something, which iterates the enum and asserts each
      member renders non-empty. So a reword is now invisible to the suite, deliberately and with
      the trade stated on the thread rather than argued a second time; what survives is the pair
      of failures that actually break the hook, both mutation-checked - a member naming a
      function that does not exist, and a function that prints nothing (which would make
      session-start.sh emit a blank setup: line silently). Worth recording against the
      2026-08-07 entry's own generalization, which claimed the single-source-plus-contract-test
      pairing was ready to be promoted from precedent to expectation: it is the reviewer's call
      whether wording is worth pinning, and here it was not.
      The third comment asked whether session-start-messages.sh could be Python instead, "if no
      harm from it". There is harm and it was measured rather than asserted: with python3 taken
      off PATH entirely the hook still prints every line and exits 0, degrading only
      check-setup.sh's dependency row into a row that says python3 is missing - as Python, that
      whole block becomes nothing. That is also the rule resolve-personal-notes-config.sh:343
      already states for itself. Not converted; replied with the measurement and left open for
      the user, since dev-tooling-session-start-python is the item that takes the floor raise
      deliberately, behind a shim that probes for python3 and exits 0 without it.

      Third round the same day, five comments, all naming, applied in a438bcdc. The
      SummaryMessage members had been transliterated from the shell function each one calls,
      which read badly in Python: PLAN_NO_PLANS_TRACKED stuttered, PLAN_TRACKED read as though
      the plan were what is tracked, and SETUP_NEEDS_SETUP said setup twice. They now name the
      situation a test exercises - NO_PLANS_TRACKED, NO_PLAN_ITEM_TRACKS_BRANCH,
      BRANCH_TRACKED_IN_PLAN, SETUP_SCRIPT_MISSING, CHECKS_NEED_SETUP - while PLAN_NOT_APPLICABLE
      and PLAN_MANIFEST_MISSING keep the prefix, since there it carries a noun rather than
      stuttering. The knock-on worth recording: the class docstring said the members were named
      by the shell function, which the rename made untrue, so it now states the two vocabularies
      and why they differ - the enum is the one place the mapping lives, and the shell file
      stays grouped by summary line. The user resolved the write_setup_prerequisites and Python
      threads themselves, so every thread on this pull request is now closed.

      Handed over 2026-08-07T09:02Z: the user marked #121 ready for review themselves, which
      by the notes-branch convention is their record of having read and accepted it, and ends
      the resolving session's job on the pull request. State at handover: head a438bcdc, nine
      commits, 758 additions across 15 files, mergeable_state unstable (mergeable, CI still
      running), bug label, no unresolved review threads. test_claude_dev_tooling - the only job
      that reaches this .claude/-only diff - passed on the final head; of the robotics matrix
      eight had passed and eleven were still in flight, none failed. The item stays in_progress
      because it has not merged; nothing is outstanding for a session to act on. #126 is stacked
      on this branch and inherits the resolution when it lands.

  - id: plan-item-edit-guard
    branch: null
    title: "PreToolUse guard: refuse source edits from a branch with no plan item"
    track: personal-data
    depends_on: [session-start-plan-and-setup-guards]
    status: not_started
    notes: >
      Split out of session-start-plan-and-setup-guards on 2026-08-01, at kickoff, as the third
      and strongest of that item's three recorded fixes - the only one that makes the miss
      impossible rather than merely unlikely, where the two that shipped in #121 make it
      unlikely by reporting better. A PreToolUse hook on Edit|Write that refuses a source edit
      from a branch no plan item tracks. Semantics settled with the user when the split was
      agreed: block until resolved, not a one-shot speed bump - an edit is refused until either
      an item exists for the branch or the branch is explicitly exempted - but active only for
      someone who uses plans or personal notes, and completely inert for everyone else. The
      discriminator should be the same state-derived one #121 established (no personal-notes
      branch reachable, or no generated branch index on it, means silence), not a new setting,
      since the user's requirement is that this works automatically and reliably for both kinds
      of user with nothing to configure. Inertness is a hard constraint rather than a
      preference: .claude/settings.json is committed, so every contributor who ever inherits
      this repo inherits the hook, and one that blocked their edits would be indefensible
      upstream. Needs a per-branch opt-out for a branch that genuinely belongs to no plan.
      Depends on #121 for the discriminator helpers it builds on.
      Scope settled 2026-08-01, from the user's point that plans-and-notes users still do work
      that is purely a personal-notes edit and needs no item. Two exemptions, both mechanical
      rather than opt-outs: (a) the guard fires only for files tracked in the working tree, so
      notes-only work is exempt for free - CLAUDE.local.md is gitignored, and plan and notes
      data live on the notes branch, written by the save-*.sh scripts from scratch paths
      outside the repo; and (b) a branch whose pull request targets the notes branch is
      exempt, since it carries notes data rather than code bound for the default branch.
      (b) has no mechanism yet and is this item's own work: the branch names #121 excludes do
      not cover it, and the obvious local test - the notes branch tip being an ancestor of
      HEAD - collides with the hook tests' scratch fixture, which builds its work branch off
      the notes branch. Either the fixture branches from the initial commit instead, or the
      test reads the pull request base. #121's plan line has the same residual gap and should
      follow whatever mechanism this item settles on. The per-branch opt-out remains, but only
      for genuine no-plan *code* work, which is now the sole case it has to cover.

  - id: git-identity-from-personal-notes
    branch: claude/workflow-unification-git-identity-ppzcyh
    title: "Stop sessions committing as an assistant identity"
    pull_request_number: 126
    track: personal-data
    depends_on: [session-start-plan-and-setup-guards]
    status: in_progress
    session: https://claude.ai/code/session_011qPaY1qP7B8hhGQauH4k61
    notes: >
      Recorded 2026-08-01 while working session-start-plan-and-setup-guards, after that
      session found its own clone configured to commit as "Claude
      <noreply@anthropic.com>" - exactly what AGENTS.md's Version Control rule forbids,
      and the same authorship this roadmap already flags as a standing hazard on #101
      and the P3 branch. Diagnosis that makes it worth tracking rather than fixing by
      hand each time: the value is in the container's *global* git config, so a fresh
      clone inherits it and there is no repo-local identity until something writes one.
      It is the default, not a slip, and every session that commits without setting the
      identity first reproduces it. Fix: session-start.sh writes a repo-local identity
      read from the personal-notes branch (the same "personal config lives on the notes
      branch, gets written into the clone at session start" pattern personal-settings-sync
      established for settings.local.json - its own .claude/personal/ entry or folded into
      that sync), and check-setup.sh gains a git_identity check, which then surfaces
      through the setup line #121 added with no further plumbing. Write only when no
      repo-local identity exists yet: a fresh clone has none and someone who set one
      deliberately keeps it, which is precise where blocklisting known assistant
      identities would be brittle. Two limits to state rather than paper over: the hook
      covers only commits made inside a session in this clone, and the merge commits this
      roadmap already flags landed from *outside* any session (the merge button, the
      Routine), which no local hook can reach - that half needs the GitHub account's own
      commit-email setting or a changed merge path. Also independently available with no
      code at all, and stronger where it applies: GIT_AUTHOR_NAME/GIT_AUTHOR_EMAIL/
      GIT_COMMITTER_NAME/GIT_COMMITTER_EMAIL set at the environment level next to the
      CLAUDE_PERSONAL_NOTES_* variables already there - environment variables beat both
      local and global config and are in force from the session's first command, before
      any hook runs, but they are per-environment and do not travel to another
      contributor's clone. The user chose to track the tooling fix; the environment
      variables remain available as the immediate stopgap. Small independent PR off fork
      main, like #109 and #115; textual overlap in session-start.sh and check-setup.sh
      with the usual whichever-lands-second treatment.
      Superseded 2026-08-01 on implementation, both points corrected in the roadmap: the
      environment variables are *set* in the session environment this was recorded from,
      not unset, so the check resolves authorship with `git var GIT_AUTHOR_IDENT` rather
      than `git config --get user.name`, which reports the assistant on a clone whose
      commits are correctly authored. And this is not an independent PR off main - it is
      based on #121, whose ScratchRepository.run_hook_script is the only harness that can
      run session-start.sh in a test, hence the depends_on above.
      Opened as draft pull request #126 on 2026-08-01, based on
      claude/workflow-unification-setup-jgvs53 (#121) rather than fork main, no bug label
      (nothing is defective - this is tooling that never existed, the same call recorded
      for #120), subscribed to its activity. This is the plan's first item whose PR is
      stacked on another item's PR outside the #101/#106 upstream chain, so it inherits
      that chain's one live constraint: when #121 merges, GitHub retargets this to main
      by itself only if #121 is merged through the PR rather than by a push (round-2 of
      native-stacks-prototype established that push-based merges mark a PR merged without
      retargeting its children). Neither PR is a native stack member, so the Phase-1
      reparent 422 does not apply and a plain PATCH of the base is the recovery.
      A second pull request existed on this branch and was closed on 2026-08-05: #125,
      opened 95 seconds before #126 from the same head commit but based on main, so its
      diff was #121 plus this item and its title and body were GitHub's auto-fill from
      commit a525d117 - #121's commit message. It was never tracked here, never mentioned
      on the tracking issue, carried no reviews, and was the only non-draft pull request
      on the fork. Closed as a duplicate with the reasoning on the pull request; #126
      remains this item's only pull request.

      Resolved 2026-08-11 via /plan-item-resolve (session_016kC5DfwqNRAmDkWYLxpa3x). Two
      things were wrong and the manifest named only the first - the fourth time on this
      plan the pull request was the more accurate source, after #109, #115 and #121.
      (1) The recorded conflict against the base, reported twice by the maintenance pass.
      #121 had moved from 5baac7d8 to 311354a3 - its own resolve, two review rounds, two
      main merges. All five conflicts were additive-vs-additive; three carried a real
      choice. session-start.sh: this branch's git identity block and main's
      personal-settings sync landed at the same anchor, and this branch did not carry the
      settings block at all, so a keep-ours would have deleted it from the hook. Both
      kept, git identity first, both above the setup verdict that runs last of everything
      the hook writes - which is the constraint #121's 2026-08-07 resolve generalized its
      comment to state, written for exactly this arrival. scratch_repository.py:
      REQUIREMENTS_FILE and TOOLING_FILES were dropped for the SetupPrerequisiteFile enum
      #121's review round introduced over the same paths, since re-landing them
      re-introduces the duplication that round removed; run_git keeps both its new cwd
      argument and this branch's delegation to run_git_allowing_failure. README.md: the
      summary paragraph and the gitignore bullet were re-authored rather than merged,
      keeping the third-person wording #121 settled on, and both merged files were read
      end to end for the resurrected-section defect #109 and #115 produced. None present.
      (2) One review thread from the same morning, unresolved and not outdated, asking
      that GIT_AUTHOR_IDENT be spelled GIT_AUTHOR_IDENTITY. Answered rather than applied:
      git var -l lists only GIT_AUTHOR_IDENT and GIT_COMMITTER_IDENT, and git var
      GIT_AUTHOR_IDENTITY exits with a usage error, so the rename would make
      effective_git_identity report that git cannot determine an identity at all. Every
      occurrence on the branch references git's variable; everything the branch itself
      names is already spelled out. Replied with the measurement and left open for the
      user, per the rule that a thread answered differently from what it asked is never
      resolved by the answering session.

      One consequence of the merge went beyond resolving it. session-start-messages.sh
      did not exist when this branch was written, so afterwards the git identity line was
      the only summary line still wording itself inline, and test_git_identity_sync.py
      held four literal copies of that wording - the duplication #121's own review round
      had just removed everywhere else. Its four messages moved into that file with
      SummaryMessage members named for the situation, and SummaryMessage and
      summary_message moved beside summary_value in session_start_summary.py, which two
      test modules now share. Following #121's second round, no wording is pinned; what is
      pinned is the pair of failures that break the hook, both checked by mutation - a
      member naming a function that does not exist, and a function that prints nothing.

      Pushed as edca885f. mergeable_state dirty to unstable, needs-resolution dropped by
      re-sending the full label set per #139's replace-not-add finding, still a draft, no
      bug label, description rewritten. 397 tests green across the three directories CI
      runs - 107 hooks, 194 plan-dashboard, 96 stack - and the hooks suite re-run from a
      clean clone of the pushed branch as well as in place. Pushed to this item's own
      branch rather than the resolving session's designated one, per the precedent the
      #115 and #121 resolves set: a new branch is a new pull request, which is what
      produced #125.

  - id: ready-to-review-merged-dependency
    branch: claude/plan-item-kickoff-workflow-s9e8bj
    title: "Ready-to-review drops items whose dependency has already merged"
    pull_request_number: 122
    track: dashboards
    depends_on: []
    status: done
    session: https://claude.ai/code/session_01Ltrz1G8qwSHgo6jyfeV1EU
    notes: >
      Found 2026-08-01 while working sidebar-bug-fix-chips, and deliberately left out of
      that pull request to keep one root cause per bug fix. _compute_ready_to_review admits
      an item only when every dependency satisfies has_open_pull_request, which is
      LiveState.OPEN_DRAFT or OPEN_READY - and therefore false once that dependency has
      merged. The result inverts the intent: an item whose dependency merely has an open
      draft is listed, while one whose dependency has fully landed is not, so a dependency
      becoming more finished makes its dependent look less reviewable. Visible in the
      committed example, where retry-circuit-breaker never reaches the list because
      retry-backoff-strategy is merged. The method's own docstring argues the rule exists
      because "reviewing a stacked pull request before its base even has one open yet is
      premature", a rationale a merged base satisfies completely, which is why this reads
      as an oversight rather than intent - has_open_pull_request was used as the proxy for
      "the base exists and is far enough along" and happens to be false at the far end.
      Confirm that reading with the developer before changing anything, per AGENTS.md's
      rule against inventing a reason for existing behaviour. If confirmed: a dependency
      with no pull request at all must keep excluding its dependent (the base genuinely
      does not exist yet), so the fix is to admit the settled states too - the plainest
      spelling is has_open_pull_request or is_effectively_done(), and Item already carries
      is_ready_to_unblock_dependents() with almost exactly this meaning, which may be the
      right shared predicate. TDD as usual: a failing test pinning that a merged dependency
      leaves its dependent in the list, before the fix. Same family as #103 and #105 (both
      merged) and, like them, an independent bug-fix pull request off fork main carrying
      the bug label; build_dashboard.py overlap with #111, #119 and #120 is the usual
      whichever-lands-second-merges pattern for this track.
      Kicked off and implemented 2026-08-01 via /plan-item-kickoff as draft PR #122
      (bug label, based on fork main one commit off the tip). The user confirmed the
      oversight reading, so the fix went ahead. The suggested consolidation onto
      is_ready_to_unblock_dependents() was rejected on inspection and must not be
      revived: that predicate excludes OPEN_DRAFT, because a draft is unsafe to build a
      branch on, while _compute_ready_to_review deliberately includes a draft dependency
      - pinned by the pre-existing test_item_ready_to_review_once_dependency_has_an_open
      _pull_request and its explanatory comment. The two predicates differ by exactly
      OPEN_DRAFT, and that difference is the real distinction: safe-to-build-on requires
      out-of-draft, worth-reviewing-behind requires only that the base exists. Delivered
      as a new Item.is_ready_for_dependent_review() composing the existing
      has_open_pull_request with is_effectively_done(), so has_open_pull_request stays in
      production use rather than becoming test-only. 229 tests pass, was 222: three
      renderer tests (the merged dependency, a done dependency with no pull request at
      all, and closed-unmerged still excluding) plus four predicate unit tests beside the
      is_ready_to_unblock_dependents ones. One existing assertion changed deliberately -
      test_example_plan_renders_the_counts_and_sections_the_walkthrough_describes, whose
      own docstring says it exists to fail rather than let the walkthrough go stale; the
      example fixture itself is untouched and now demonstrates the fix, with
      retry-circuit-breaker reaching the list behind its merged retry-backoff-strategy.
      The walkthrough prose and dashboard-overview.png were regenerated to match (user's
      call, knowing it conflicts as a binary with #120's own regenerated copy - whoever
      lands second re-regenerates, which is mechanical since the image is deterministic
      from the committed fixture). Regeneration recipe, since no script exists: render
      example/ with build_dashboard.py, then headless Chromium with
      --blink-settings=preferredColorScheme=0 for the dark theme the committed images
      use, cropped to a 100px bottom margin. That regeneration also cleared a
      pre-existing staleness the roadmap had flagged - the old image still rendered the
      description as EXAMPLE_WALKTHROUGH.md, a since-renamed file.

  - id: dashboard-chip-notes-collapse
    branch: claude/dashboard-chip-collapse-g5uyj1
    title: "Collapse plan-item notes on dashboard chips behind an expand arrow"
    pull_request_number: 124
    track: dashboards
    depends_on: []
    status: done
    session: https://claude.ai/code/session_01HaU9fwvtLUvFh4k93K6p5C
    notes: >
      Raised 2026-08-01 against a screenshot of this very plan's own dashboard: the
      ready-to-review-merged-dependency card's notes ran to several hundred words and alone filled
      the viewport, forcing heavy scrolling and leaving only one item visible at a time - defeating
      the dashboard's purpose as an overview. item.notes (item_card macro, dashboard.html) rendered
      at full height with no truncation, so any item with a long investigation writeup did this to
      the page. Fixed by wrapping the notes div in a collapsed-by-default <details>/<summary>
      toggle, reusing the .roadmap-details CSS-only arrow-toggle pattern already in the file for the
      "Background & history" section, rather than inventing new JS. No Python changes needed -
      build_dashboard.py/render_common.py already pass item.notes through unchanged; the sidebar
      "What to do next" cards are unaffected since they render short fixed one-liners, not
      item.notes. Verified by rendering example/ through build_dashboard.py with a temporarily
      lengthened note and publishing the result as an Artifact: notes collapse by default behind "▸
      Notes", expand to "▾ Notes" on click, and items with no notes show no summary line. 194 tests
      pass (plan-dashboard suite - unaffected, since this is template/CSS only). Independent of the
      #101 chain, based on fork main; build_dashboard.py/dashboard.html overlap with #103, #105,
      #111, #119, #120 and #122 is the usual whichever-lands-second-merges pattern for this track.
      Opened as draft pull request #124, subscribed to its activity. CI: two unrelated failures
      investigated across three origin/main merges that landed on this branch from outside any
      session (coraplex/semantic_digital_twin worker crashes, a mesh-material assertion cleared by
      an upstream fix, and a missing-ROS-package error in a newly-merged Gazebo adapter's tests) -
      none had any path back to this template-only diff; each was replied to on the pull request
      rather than fixed here, per the established pattern of unrelated robotics CI noise on
      .claude/-only changes. The user marked the pull request ready for review themselves on
      2026-08-03 (not reverted to draft, per this plan's own precedent for a user-made
      draft-to-ready flip) and it merged into fork main the same day. Session unsubscribed from both
      the pull request and this plan's tracking issue #102 on merge.

  - id: add-plan-item-skill
    branch: claude/add-plan-item-skill-e89irj
    title: "/add-plan-item: decide where a new piece of work belongs"
    pull_request_number: 135
    track: personal-data
    depends_on: []
    status: in_progress
    session: https://claude.ai/code/session_01XGMaAHK8LQCoxa52KVaMCc
    notes: >
      Raised 2026-08-02. The plan skills covered creating a plan, starting an item,
      unblocking one and publishing status, but nothing covered the most frequent event:
      someone describing new work and asking where it goes. That decision was being made
      by default, which is what produced this plan's own recorded fold chain (#133 into
      #117, #117 into #106) and the #110/#106 collision where two sessions independently
      built the same artifact under two names. Adds /add-plan-item, which derives the
      paths the work touches, gathers every plan and open pull request as candidate
      homes, runs the scope check, and proposes exactly one of four outcomes - fold, new
      item in an existing plan, new plan via /plan-create, or no item at all - through
      plan mode without writing code.
      Two decisions taken with the user before implementing. The rule itself was
      triplicated as three independently-worded copies added to plan-create,
      plan-item-kickoff and plan-item-resolve by the unlanded, PR-less branch
      claude/plan-scope-before-new-item, with no shared anchor - itself the failure mode
      the rule is about. It moves into add-plan-item/scope-decision.md, referenced in a
      line by all four skills, following the setup-personal-notes/prerequisite-check.md
      precedent. And rather than stack two pull requests, this branch was reset onto
      claude/plan-scope-before-new-item (a clean fast-forward from main, commits 3e23270a
      and 96a5b30b) so one branch carries both; that branch is retired, having never had
      a pull request. That is the new rule applied to its own introduction.
      Ships check_scope_overlap.py rather than prose alone, since the #110/#106 collision
      is exactly what eyeballing missed: given a base and the work's paths it reports
      which paths the base lacks and which unlanded branches already touch them, plus
      each candidate's full changed-file list so same-purpose/different-filename
      duplicates stay findable. Pure git, no network. Eight tests under
      add-plan-item/tests/, reusing the hooks suite's ScratchRepository rather than a
      second scratch-repository helper, wired into ci.yml's test_claude_dev_tooling job
      via a new ADD_PLAN_ITEM_TESTS_DIRECTORY constant. 230 tests pass, was 222.
      Committed and pushed 2026-08-02, then opened as draft pull request #135 based on
      fork main, no bug label (tooling that never existed, not a defect), subscribed to
      its activity. Basing was checked with this item's own check_scope_overlap.py rather
      than by feel, after the user asked whether it should sit on #106: the only paths
      absent from main are the three this item introduces, the two it shares with #106
      (resolve-personal-notes-config.sh, ci.yml) already exist on main, and nothing in
      .claude/stack/ is read or referenced - so it is textual overlap, not a dependency,
      and basing on #106 would attach a dependency-free immediate-wave item to the
      unlanded upstream chain. git merge-tree against #106 reports exactly one conflict,
      ci.yml's single pytest line, which both branches extend with their own tests
      directory; the config file auto-merges.
      Review round 2026-08-05, two comments, both answered in cd6eb839 and the thread. (1)
      The report's JSON keys become a ReportKey StrEnum, read by both as_dictionary methods
      and the test instead of being spelled out twice. Applying it removed coverage the
      literals had been providing by accident - with both sides reading the enum, renaming a
      key changes them identically and nothing notices, verified by renaming SHARED_PATHS and
      watching all 8 tests still pass. One test now pins the member names to their wire
      values, so a rename fails exactly there; 9 tests, was 8. (2) The run_git duplication
      question is answered and recorded on dev-tooling-notes-core-python as a third carrier
      for the git_interface.py seam it already plans; not unified here, and that thread is
      left open for the user rather than resolved.

      Merged main twice as it moved (#106, #115, #119, #402 and others landing): the second
      integration conflicted in ci.yml, README.md and .claude/hooks/README.md, all
      additive-vs-additive and all resolved keeping both sides - the pytest line now lists
      all four test directories. resolve-personal-notes-config.sh auto-merged exactly as the
      pre-open merge-tree check predicted. 347 tests pass across the four suites, and
      .claude/stack/tests existing on main means the full four-directory command runs for the
      first time.

      Handed over 2026-08-09: the user marked #135 ready for review themselves, which by the
      notes-branch convention is their record of having read and accepted it, and ends this
      session's job on the pull request. State at handover: head db034e64, 12 commits, 907
      additions across 13 files, mergeable_state unstable (mergeable, robotics CI red),
      draft false, no bug label. The item stays in_progress because it has not merged.

      Three things outstanding, none blocking and none this session's to fix after the
      signal. (1) The needs-resolution label is stale - the branch became mergeable again
      with db034e64 and the maintenance pass clears the label itself once it does, so it was
      left rather than raced. (2) One review thread is deliberately open, the run_git
      duplication question, answered with a recommendation and left for the user to close.
      (3) The description still says 346 tests and names only semantic_digital_twin under CI;
      it is one merge behind, and was left alone because the ready-for-review flip is the
      user's record of having read what was there.

      CI at handover: test_claude_dev_tooling green on every run this branch ever had. Four
      distinct robotics failures across four runs, none reachable from a .claude/-only diff -
      semantic_digital_twin's two texture assertions in test_multi_sim.py (proven base-side:
      main fails identically on its own run, and #124 hit the same pair a day before this
      branch existed), a coraplex notebook kernel dying before replying to kernel_info, and
      giskardpy twice on two different tests (test_drive_into_apartment on the last run, an
      rclpy handle-lifecycle teardown). The semantic_digital_twin pair is the only one that
      looks like a real defect rather than flakiness, it blocks every pull request in the
      repository, and it was offered as its own bug-labelled item and not taken up. Textual overlap with #121's and #126's session-start.sh/check-setup.sh
      work is nil; the shared surface is resolve-personal-notes-config.sh (new constants
      appended) and the three plan-*/SKILL.md files, which no other in-flight item on this
      track touches.

  - id: plan-item-bootstrap
    branch: claude/plan-item-kickoff-workflow-vocs6b
    title: "Bootstrap an item's branch, draft pull request and manifest state before implementing"
    pull_request_number: 143
    track: personal-data
    depends_on: []
    status: done
    session: https://claude.ai/code/session_01VoH56TLkH5rA5EFu2xhML9
    notes: >
      Raised 2026-08-04. Today a session that has just had an implementation plan approved
      goes straight to writing code, and the branch, the draft pull request, the manifest's
      branch/session/status fields, the roadmap entry and the dashboard all follow at the
      end - so for the whole length of the implementation the plan says the item is
      not_started with no branch, and any other session reading it is reading a lie. The
      user's requirement is the inversion: the first step after a plan is approved creates
      the branch and the draft pull request, writes the approved plan plus branch, session
      and pull request number into plan.yaml and roadmap.md, flips the item to in_progress,
      and republishes the dashboard - and only then does implementation start. None of that
      waits on the code, because none of it depends on the code.
      Shape revised 2026-08-04 by the user, and the revision is what fixes the design: this
      is not one procedure two skills share, it is two operations one module offers, and each
      skill takes only the one it needs. Recording an item - write or update its plan.yaml
      entry and roadmap.md section, set its status, run save-plan.sh - is add-plan-item's
      whole business, and it is all add-plan-item gets. Opening the work - create the branch,
      push it, open the draft pull request, write the branch, session and pull request number
      back onto the item and flip it to in_progress - belongs only to plan-item-kickoff, the
      skill whose approved plan leads straight into implementing. Kickoff calls both, in that
      order, because the pull request number does not exist until the pull request does.
      The first shape had add-plan-item referencing the whole thing, which would have handed a
      branch-and-push step to a skill whose own opening paragraph promises it never creates a
      branch or pushes anything. Splitting on what each caller actually needs keeps that
      promise intact and gives each operation one reason to change.
      The skills stay prose that says when to run which operation; everything mechanical is
      Python with real dataclasses - a record request (plan id, item id, title, track, status,
      roadmap section) and an open request (branch name, base branch, session URL, pull
      request title and body), each with its own result - not steps a session re-improvises.
      It belongs beside plan_manifest_tools.py in .claude/hooks/ rather than in a skill
      directory, since two skills call into it and dev-tooling-save-plan-python already
      absorbs that file into the package - this rides the same migration instead of inventing
      a second destination that would have to be moved again.
      Exactly one part cannot be pure script, and the item's job is to hand it back explicitly
      rather than pretend: only a live session can call the Artifact tool, so the dashboard
      republish stays a /plan-dashboard step exactly as save-plan.sh already documents. Both
      operations end there, since both change what a published dashboard should show.
      Creating the pull request was the open question, and it was probed rather than left to
      inference. POST /repos/{o}/{r}/pulls with a deliberately unresolvable head returns 422
      Validation Failed on the head field, with documentation_url on docs.github.com - so the
      request reaches GitHub and is authorised, and the endpoint is not refused by request
      shape the way the base-branch PATCH is (whose refusal carries docs.anthropic.com). The
      script can therefore create the pull request itself, which is the answer this item
      needed. The user's framing - that it works given an exported token - is right about the
      outcome and worth correcting on the mechanism: the identical 422 comes back with the
      exported GH_TOKEN, with a junk token and with no Authorization header at all, so within
      a session the credential is irrelevant and the proxy supplies the identity. Nothing is
      gained by exporting a token here and nothing is lost by having none. What the probe does
      not cover, because a 422 on head stops evaluation before anything is created: that a
      creation actually succeeds, that draft: true is honoured, and which identity the result
      is attributed to. Those need one throwaway creation against the fork, in the shape #139
      used for its own live run.
      Portability is the reason the credential still matters even though it is inert here: the
      same script run from a terminal or from routine-cutover's Action has no proxy and the
      token is the credential. So creation goes through the one shared backend
      dev-tooling-github-api-unification builds, never a third copy of the gh-CLI-else-token
      rule that github-api.sh and pr_state already carry.
      Basing was decided by running add-plan-item's own check_scope_overlap.py against main
      rather than by feel. Everything the work builds on is already on main -
      plan-item-kickoff/SKILL.md, plan-create/SKILL.md, save-plan.sh, plan_manifest_tools.py,
      resolve-personal-notes-config.sh and the .claude/hooks/tests/ directory ci.yml already
      runs, so no new ci.yml pytest entry is needed and the one conflict #135, #106 and #107
      all share on that line is avoided outright. The only requested paths absent from main
      are .claude/skills/add-plan-item/{SKILL.md,scope-decision.md}, which #135 introduces,
      and they are needed for exactly one line - the reference from add-plan-item's step 6.
      By the scope rule's own prefer-the-change test that single line is a change to what
      #135 introduces, so it goes on #135's branch while #135 is still an open draft, and
      this item stays based on fork main with depends_on empty; if #135 lands first the line
      simply moves into this item's own pull request. Stacking the whole item on #135 for one
      line would hold a dependency-free immediate-wave item behind a draft in review, which is
      the trade add-plan-item's own basing note already refused about #106.
      Textual overlap is the usual whichever-lands-second pattern: resolve-personal-notes-config.sh
      gains new constants (shared with #106, #107, #115, #121, #126, #135) and
      .claude/hooks/tests/ gains a new file beside the ones #107, #115, #121 and #126 touch.

      Kicked off 2026-08-04 (session_01VoH56TLkH5rA5EFu2xhML9) as draft #143 on
      claude/plan-item-kickoff-workflow-vocs6b, bootstrapped by hand in the order this item
      itself prescribes - branch, draft pull request, manifest, roadmap, dashboard, and only
      then the implementation. Two calls settled at kickoff. The notes' instruction that pull
      request creation "goes through the one shared backend dev-tooling-github-api-unification
      builds" cannot be honoured from main: that item is not_started, github-api.sh is on #107
      only, and main carries no GitHub-calling Python at all (render_common.py imports urlsplit
      and nothing else). #115's recorded precedent applies instead - implement the call inline,
      deliberately without reproducing the gh-CLI-else-token discovery, and let the unification
      item absorb it; stacking on #107/#111 would reverse this item's own basing decision. And
      plan-item-kickoff/SKILL.md's opening promise has to be rewritten rather than merely
      extended: it currently reads "never writes code, creates a branch, or pushes anything",
      which is the same contradiction that ruled out hanging this off /add-plan-item. It becomes
      "never writes code, and creates the branch and draft pull request only once the plan is
      approved", with the branch and pull request work in a new step 6 after approval, leaving
      step 5's planning half untouched.

      Implemented the same session. The live creation probe the item asked for settled all
      three unmeasured facts and the third one changed the design: creation succeeds and
      draft: true is honoured, but a pull request the script creates is attributed to
      claude[bot] - the app its requests are proxied through - while the same creation
      through the session's GitHub tool is attributed to the user, verified minutes apart on
      the same repository (throwaway #144, closed). That is the authorship problem AGENTS.md
      already rules out for commits, and it would have applied to every plan item's pull
      request. So open takes --pull-request-number for one the caller already created and
      records it instead, with the creating path kept for an unattended run whose credential
      is a real one; its title and body are required only on that path. The one line on #135
      names the script's path rather than a ${PLAN_ITEM_BOOTSTRAP_SCRIPT} constant, since
      that constant arrives with this item and a variable reference would expand to nothing
      in the window where #135 has landed and this has not.

      Merged into fork main 2026-08-05. Verified by content rather than from the merge
      notification: f77794b0 is a git merge-base --is-ancestor of origin/main, and
      plan_item_bootstrap.py, its test module and its fixtures are all present there, with
      plan-item-kickoff/SKILL.md carrying step 6 and resolve-personal-notes-config.sh the
      PLAN_ITEM_BOOTSTRAP_SCRIPT constant. So the ordering this item exists to invert is now
      the documented procedure on main: a kickoff whose plan is approved opens the branch and
      draft pull request and records the item before the first edit.
      Four review rounds (19 + 5 + 1 + 1 comments), every thread replied to and resolved except
      the ItemStatus/build_dashboard.py unification question, which was answered with a
      recommendation and left for the user; it belongs to dev-tooling-python-package, the item
      that actually moves build_dashboard.py.
      Residue needing out-of-harness deletion: the remote branch
      claude/plan-item-bootstrap-probe-vocs6b, from the live creation probe (sessions cannot
      delete branches - the standing constraint recorded 2026-07-29).

  - id: upstream-review-reader
    branch: claude/automate-upstream-reviews-0fte9f
    title: "Read upstream review threads from a fork via a dispatched Action"
    pull_request_number: 146
    track: stack-tooling
    depends_on: [stack-tooling-on-main]
    status: in_progress
    session: https://claude.ai/code/session_01MQavvbnknGrFiw6q7ccZaE
    notes: >
      User's problem, 2026-08-07: review threads on cram2 reached a session only by the user
      reading them and retyping them, making them the bottleneck on every review round. Measured
      rather than assumed: cram2 is public and git-readable from a session, but its REST API and
      even its HTML are 403 for a session not scoped to it, and GraphQL is refused for every
      repository including the fork's own ("only the pinned set of PR-review operations is
      served"). Thread resolved-state exists only in GraphQL, and the user required it, so no
      script running in a session can produce this report.
      The read therefore runs as a workflow_dispatch Action on the contributor's own fork, where
      gh and GITHUB_TOKEN are already present, and the session reads the job log over plain REST
      (verified: runs, jobs and job-logs endpoints all answer 200). Queue time on this fork
      measured median 0s and max 0s over the last 14 completed runs, so the round trip is the job
      itself. Dispatch-only, never scheduled. Portable by construction: upstream comes from
      stack.toml and the fork owner from the runner context, so no repository is named anywhere;
      a fresh fork need only enable Actions once. plan-item-resolve gathers the report in its
      PR-state step, gated on the in-review label.

  - id: dashboard-url-cache-integrity
    title: "Record dashboard URLs from the live artifact listing instead of by hand"
    branch: claude/plan-dashboard-cache-drift-0g2s1w
    pull_request_number: 150
    session: https://claude.ai/code/session_01CtpkhY4uomdaWY2rv25WaY
    track: dashboards
    depends_on: []
    status: in_progress
    notes: >
      Raised 2026-08-09: five of six plans plus the master index pointed at artifacts
      that did not exist, and two plans had duplicate dashboards. Hand-corrected at least
      six times since 2026-07-26 and drifted again every time, so the question was the
      mechanism rather than the mapping. Classifying every URL ever written to the cache
      against the live artifact listing separates by run kind: bulk refresh 0/23 named a
      real artifact, first publish 10/13, corrections 15/15 - and every correction was
      made from `Artifact action: "list"`. The cache read was never at fault and `url:`
      was being passed; SKILL.md step 3 asked the session to hand-write the YAML with
      "your updated url(s)", but on an in-place update there is no returned URL to copy,
      so a UUID was invented and nothing checked it. The next run passed that dead URL as
      `url:`, could not update a missing page, minted a fresh artifact, and produced the
      duplicate - both live pairs arose that way. Fixed by moving the write into
      record_dashboard_url.py, which resolves the URL from the listing by title so no
      UUID passes through the session at all; it refuses an unlisted URL, a title
      matching nothing, and a title matching several (naming both and demanding --url,
      which also blocks silently repointing another plan's entry).
      DASHBOARD_URL_CACHE_PATH and RECORD_DASHBOARD_URL_SCRIPT move into
      resolve-personal-notes-config.sh. 15 tests written failing first; 291 pass across
      the two suites CI runs. Cache reconciled with the new script one key at a time -
      dag-facade-hardening keeps 49053971 by the user's choice, workflow-unification
      reported changed:false, confirming an entry already fixed by hand. Verified end to
      end: republishing landed on the same URL and the artifact count stayed at 11.
      Draft PR #150, bug label, based on fork main and independent of the #101/#106 chain;
      the SKILL.md overlap with #120's bug-chip work is the usual
      whichever-lands-second-merges pattern for this track. Not addressed: no automated
      audit of existing entries, so a URL that dies for another reason still surfaces only
      when someone opens the page.

      That unaddressed gap fired 2026-08-10, five days later, and is worth carrying as
      evidence rather than an anticipated risk. workflow-unification's reconciled entry -
      the one this item's own notes record as verified with changed:false - had since gone
      dead: WebFetch on af60607f returned "artifact not found", so the URL was correct when
      recorded and stopped resolving afterwards. That is the failure mode the recording fix
      cannot reach, since it validates at write time and nothing re-checks later. Recovery
      was this item's own prescription applied by hand: resolve from the live listing, which
      showed two artifacts sharing the plan's title - the ambiguous case the script refuses
      and demands --url for. Decided by recency rather than by the script, since a
      concurrent session had published to 07123af6 that same day, so updating it kept one
      live page instead of minting a twelfth; 36572776 (2026-08-09) is the stale half of
      that pair. Cache repointed at 07123af6. Two things this argues for when the item is
      picked back up: the audit is worth having, because a stale entry costs a duplicate
      artifact rather than an error, and the refusal on an ambiguous title is right but
      leaves the caller with no recorded rule for choosing - most-recently-updated is what
      both corrections have used.

  - id: plan-item-execution-modes
    title: "Auto and plan execution modes for the plan-item skills"
    branch: claude/plan-item-kickoff-modes-p1yuwc
    track: personal-data
    depends_on: []
    status: in_progress
    pull_request_number: 149
    session: https://claude.ai/code/session_015v3RfcEvtzNj47uEMac9p9

  - id: manifest-currency-first
    title: "Write the manifest and republish the dashboard before anything else, at every transition"
    branch: claude/plan-manifest-update-priority-ex2zst
    track: stack-tooling
    depends_on: [stack-maintenance-executor]
    status: in_progress
    session: https://claude.ai/code/session_01SgGzMvxTxaePhpkT28uuJV
    pull_request_number: 151
    notes: >
      Raised 2026-08-09 by the user: every skill that can affect a plan must treat writing the
      manifest and republishing the dashboard as its first priority, at every point that makes a
      recorded field stale. plan-item-bootstrap (#143, merged) already inverted this ordering for
      exactly one moment, the kickoff whose plan has just been approved; this generalizes the same
      rule to every transition and to every skill that can reach plan state. The premise is recorded
      rather than asserted: this plan's own roadmap names the manifest "the least accurate source"
      three separate times in one week - #109 on 2026-08-03, #115 on 2026-08-05 and #121 on
      2026-08-07 - and each time everything needed to diagnose the item was already on the pull
      request.

      Scope settled with the user 2026-08-09: six bound surfaces - plan-create, add-plan-item,
      plan-item-kickoff, plan-item-resolve, plan-dashboard and stacked-pr-maintenance - with the
      maintenance pass deliberately in, since it reparents pull requests, promotes branches and
      moves labels without ever touching plan.yaml. As scripted as possible, the instructions
      document keeping only what a script cannot do.

      Kicked off and implemented 2026-08-10 as draft #151 on fork main, bootstrapped in the order
      this item itself prescribes. Two premises the notes carried were disproved by reading the
      code, and both changed the design.

      (1) The recorded instruction that the transition check "must reuse sync_manifest_status.py" is
      wrong, and following it would have been worse than not. That script imports build_dashboard,
      which imports render_common, which imports jinja2, markdown and nh3 at module level - so a
      hook cannot import it at all, which is the constraint ItemStatus's own docstring already
      records from the other side. It also answers a different question: post-hoc, GitHub-side, one
      direction. The split is by what each can see, and it is genuinely non-overlapping rather than
      a compromise - the dashboard compares the manifest against GitHub after the fact, while check
      compares it against local git before a push, which the dashboard can never see. That keeps the
      hook tier stdlib-only and leaves check importable by plan-item-edit-guard, whose hook must
      also be dependency-free.

      (2) Writing notes through the existing setter silently corrupted the manifest, latent since
      #143 gave NOTES and BLOCKERS members but no writer. ManifestKey.NOTES is block-styled, its
      pattern matches the "notes: >" line, and render emitted a single line - so apply_item_fields
      replaced the ">" and orphaned the indented body, which YAML then read as a continuation of the
      new value, concatenating the old note onto the new one. The result still validated, so nothing
      caught it.

      Delivered on plan_item_bootstrap.py rather than a sibling module, since it already owns
      PlanDocuments, ManifestKey, ItemStatus, locate_item_block/apply_item_fields, BootstrapReport
      and run_git: block-styled field writing (a value spanning the lines beneath its key is
      replaced whole, and ValueStyle gains SEQUENCE so blockers render as a list rather than a
      folded scalar); update, which sets any recorded field without the roadmap section record
      demands unconditionally; and check, which reports the recorded fields local git contradicts
      and exits manifest_is_stale when any do. branch_is_published was extracted from WorkOpener so
      both callers share one definition. Dispatch became a handler mapping rather than a fourth
      if/elif branch.

      manifest-currency.md carries the rule once, with MANIFEST_CURRENCY_DOCUMENT beside
      SCOPE_DECISION_DOCUMENT, cited in a short subsection by each bound skill. plan-item-resolve
      took the largest change, and it was not something the notes had singled out: it wrote the
      manifest nowhere at all, though its own blockers and notes are precisely the fields that went
      stale on #109, #115 and #121. It now records what it found before proposing anything.
      stacked-pr-maintenance reports the items its pass made stale rather than writing them - it
      runs unattended under --non-interactive, where its own doctrine forbids opening a discussion,
      and which status a move implies is judgement. That asymmetry is stated in the document with
      its reason.

      The contract test derives which skills are bound from what they do - a skill invoking a
      plan-writing script is bound by that fact - rather than listing them, so a skill added later
      is covered without editing the test; only the maintenance pass is named, because it is bound
      for the opposite reason and asserts it writes no manifest.

      389 tests pass across the three directories CI runs, against 367 on main. Every new test was
      mutation-checked, each failing only for its own reason. Verified live rather than only in the
      harness: check run against all 41 items of this plan returned one true positive -
      dependency-chips-blocked-fix records a published branch with no session, exit 9 - and 40
      clean.

      One incidental fix rides along, as its own concern: the scratch repository fixture now
      disables commit signing. This environment signs by default, so every scratch commit depended
      on a reachable signing service, and the suite failed on a different test each run with
      "signing server returned status 520". It also halved the suite's runtime.

      Deferred with the user's agreement: plan_item_bootstrap.py is not renamed, though its name
      stops being true once it carries update and check - dev-tooling-save-plan-python absorbs the
      file into the package regardless, so that migration renames it once rather than twice with
      three branches rebasing across the first attempt. The one line into add-plan-item/SKILL.md
      still cannot be written from main, and #135 is marked ready for review, so it lands here only
      if #135 merges first.

      CI on the branch is red on test_each_lib and it is base-side rather than this pull request's:
      greenlet 3.5.5 was published with no Linux wheel, so uv fails to resolve before any test runs.
      main's own push run failed 11 jobs three minutes before this branch's, and
      test_claude_dev_tooling - the only job reaching this .claude/-only diff - passes. Reported on
      the pull request, not fixed here.

      Reversed 2026-08-11 at the user's request, folded into this item rather than split out, since
      it rewrites the very sections this item introduced. stacked-pr-maintenance now writes and
      publishes rather than reporting. One of the two reasons recorded above for the asymmetry was
      wrong: --non-interactive suppresses AskUserQuestion and nothing else, and routine-prompt.md
      registers the pass as a live Claude session, so the Artifact tool was reachable throughout -
      the reasoning was about the mode's name rather than what the mode does. The second reason
      survives and bounds the change: which status a reparent or a promotion implies is a reading
      rather than a mechanical fact, so the pass writes only what it decided itself (a branch it
      labels needs-resolution is blocked; one whose label it clears is not) and reports the rest. A
      landed branch still needs no write - sync_manifest_status.py corrects merged to done on the
      refresh the pass now triggers.

      plan_item_bootstrap.py gains resolve, block and unblock, all keyed on a branch because that is
      what the pass holds - update needs a plan and an item id, PLAN_BRANCH_INDEX_PATH stops at the
      plan id, and PlanDocuments only looks items up by id. The owner is written into the blocker
      under MAINTENANCE_BLOCKER_OWNER, so a pass replaces and withdraws its own entries and never a
      person's, and an item still carrying somebody else's stays blocked. All three answer for every
      item on the branch, since two items on one branch is ordinary. stacked-pr-maintenance's
      frontmatter gains the Skill grant it lacked, which was the mechanical reason it could not
      publish.

      Two defects found by writing a real blocker into this plan's own manifest rather than only
      into the harness. fold broke inside words - textwrap breaks at hyphens and through any
      over-long word, and a folded scalar reads a line break back as a space, so
      claude/workflow-unification-setup-jgvs53 returned with a space inside it - latent since the
      block-styled writer landed and reachable the moment anything wrote an identifier into a note
      or blocker. And withdrawing a blocker an item never carried wrote it an empty list, which the
      every-clean-branch sweep would have spread across the manifest one run at a time; both fixed,
      the second observed live and reverted.

      plan-create's half was nearly already done: step 8 has invoked plan-dashboard since before
      this item. The hole was the master index, which step 8 asked about, leaving a new plan missing
      from the one page that lists every plan. It now republishes both unconditionally - adding a
      plan is the single change that alters what the index lists, and _index has its own cached URL
      so that updates the page rather than minting a second.

      408 tests pass across the three directories CI runs, was 389. Verified live on the fork:
      resolve answered with both items on claude/stack-landed-parent-detection, block flipped
      git-identity-from-personal-notes to blocked with the five files GitHub's dirty verdict on #126
      is about, and the blocker round-tripped byte-exact after the fold fix.

      Review round 2026-08-11, eleven threads, applied in 0118aca6. Three were substantive, and one
      reversed a rule this plan had been treating as settled.

      The report's JSON keys become a ReportKey StrEnum and every render method is to_json - but the
      tests keep their string literals deliberately, because that duplication is what pins the wire
      format two other programs parse. #135 removed exactly that guard when it made both sides read
      its own ReportKey, and the removal was invisible: renaming SHARED_PATHS there left all 8 tests
      green. So the 2026-08-07 entry's generalization needs one more qualification - single-sourcing
      an external contract deletes a guard, and whether to replace it with a dedicated test or to
      keep the literals depends on who reads the contract. Here two programs do, so the literals
      stay, and the thread asking for the enum is answered and left open rather than resolved.

      Each of the seven subcommands is a Subcommand subclass owning its flags and its work, with
      SUBCOMMANDS built from the subclasses, collapsing what had been a parser block and a handler
      entry per command into one class. The name and description are abstract instance properties
      rather than #139's classproperty, and the reason is worth carrying: the parser here is built
      from instances, so plain ABCMeta already refuses a nameless command as the module imports.
      That avoids writing class_property.py a second time - it exists only on #139's unlanded
      branch, so a fifth same-artifact-twice instance was the alternative - and avoids the trap #139
      measured, where a plain classproperty silently loses abstractness because ABCMeta calls the
      descriptor.

      update gains --append-notes, which retires the extend-a-note trap this item's notes had left
      open as a question. The answer to whether the writer should accept both forms is no, and the
      measurement is why: a note read back out of plan.yaml separates paragraphs with a single
      newline, while a note written into a file separates them with a blank line and uses single
      newlines for its own hard wrapping. The same character means opposite things in the two
      sources, so teaching fold to split on any newline fixes the first and explodes the second.
      extend_note does the conversion and the flag says which source the caller holds; --notes and
      --append-notes are mutually exclusive.

      That flag was then used to append this very note, and the first attempt got it wrong in the
      one way the flag cannot catch: the file was written as continuously wrapped prose with no
      blank lines, so five intended paragraphs were correctly folded into one. The tool did exactly
      what it promised and the input was wrong. Worth carrying, because every later caller writes
      that file the same way: --append-notes reads a file the way a person writes markdown, so the
      paragraphs have to be blank-line separated in the file, and a run-together note is an
      authoring mistake rather than a defect to go looking for.

      Also renamed throughout, on the user's question of staleness or status: manifest-currency.md
      became manifest-staleness.md, and CurrencyReport became StalenessReport. Status was already
      taken twice - an item's lifecycle field, and the key every report leads with - and staleness
      is the word check already uses.

      416 tests pass across the three directories CI runs, was 408. Six new, each mutation-checked.
      Two threads stay open on purpose, both answered rather than acted on: the test-literals one,
      and whether blockers written before the ownership convention survive a pass. They do by
      construction, verified against this plan's real manifest - the three pre-convention blockers
      on personal-settings-sync carry no owner-shaped prefix, so blockers_not_owned_by never matches
      them.

      Two follow-ups the review round's own dogfooding produced, in 14f90753. A write that sets a
      note now reports note_paragraphs, counted through the same splitter fold uses so the number
      and the manifest cannot disagree. That is the whole fix available for the run-together note:
      which paragraphs were meant is not recoverable from a file whose blank lines do not say, but
      the outcome stops being something a later reader discovers. The first attempt at it counted
      the value before folding, where a hard-wrapped line still looks like a break, and its own test
      caught that.

      repair rejoins the words the old fold broke, which the fold fix itself does not touch - a fix
      stops new damage and leaves what is already written, with nothing looking at it. The rule is
      deliberately not a blind rejoin, and the data is why: network- and credential-free is a
      suspended hyphen, correct English with exactly the shape of a break. So a break is closed up
      only when the rejoined word appears elsewhere in the plan, which is the bug's own signature
      rather than a guess about English, and a longer compound counts as evidence. What fails that
      test is reported under exit status text_needs_repair rather than rewritten, so a genuinely
      broken word occurring once is left for a person - the safe direction, since the opposite error
      edits somebody's prose silently. Run live: four rejoined, three reported, two of those judged
      and fixed by hand, and network- and correctly still stands.

      Worth carrying beyond this item: the five words repaired by hand earlier in the day were fixed
      with a blind regular expression, and that only happened to be right. Running the same rule
      over the whole plan turned up seven more, one of which it would have corrupted. A repair rule
      needs its own evidence, not a shape match.

      The greenlet claim recorded here on 2026-08-10 is retracted. greenlet 3.5.5 publishes cp312
      manylinux wheels and an sdist, uv resolves and installs it cleanly on this platform, and CI
      run 31478716090 went green on another branch. It reads as a publish-propagation window rather
      than a missing artifact, so no pin was added and no bug pull request opened - a constraint
      below the current release, justified by a problem that no longer exists, would be the worse
      change. 424 tests pass across the three directories CI runs, was 416.

      Correction, 2026-08-11, and the review comment that forced it. Asked why a repair is needed
      rather than the write being deterministic, tracing the damage showed the premise recorded
      above is wrong: these words are not residue of the fold bug this branch fixed. Counting broken
      words per commit on the notes branch gives 0 on 07-29, rising to 7 between 07-30 and 08-09, 11
      when this item's own notes were first written on 08-10, and 12 on 08-11. fold did not exist
      until 08-11, so eleven of the twelve were never written by any writer in this pull request.
      They are hand-wrapped prose - a session composing a note and wrapping a line at a hyphen - and
      the break_on_hyphens defect, fixed in the commit that introduced it, produced none of the
      survivors.

      The deterministic half was therefore available and had not been taken. paragraphs_of now
      closes up a hyphen sitting immediately before a hard-wrapped line break rather than letting it
      become a space, because that is where the author broke a word they wrote whole. A suspended
      hyphen keeps its space, since a person types network- and on one line rather than breaking
      after the hyphen -so the two are distinguishable at write time by where the break falls, which
      is exactly what the corpus rule had to infer afterwards. The one shape it reads wrongly is a
      suspended hyphen a wrap lands on: zero instances in this corpus against eleven of the case it
      fixes.

      What that leaves for repair is genuinely open and was not decided unilaterally. Its stated
      justification was that a fix stops new damage without repairing what is already written, and
      the write rule removes the first half while the run already did the second - the manifest is
      clean across all six plans, with only correct English left. The argument for keeping it is the
      path the writer does not own: eleven of the twelve arrived by a session hand-editing plan.yaml
      and pushing through save-plan.sh, which no write-time rule can reach. Recommended cutting it;
      left in place pending the user's call, since deleting is the harder one to take back.

      427 tests pass across the three directories CI runs, was 424.

      Re-scoped 2026-08-11 on the user's decisions, taken after the two review comments about
      `repair` and about the command-class duplication were costed. `repair` is cut; this pull
      request is now based on #139; and everything the two branches duplicated is extracted into a
      new `.claude/shared/` that both import.

      `repair` is deleted with its exit status, its four `ReportKey` members and its six tests. What
      replaces it is the write-time rule from the previous round, and the correction recorded above
      is the reason: eleven of the twelve broken words predate `fold` entirely, so they were never
      this writer's to produce, and the manifest is clean across all six plans now. A subcommand
      whose stated justification is spent is the YAGNI smell `AGENTS.md` names.

      `.claude/shared/` holds four stdlib-only modules: `errors.py` (`ExternalCallFailed`),
      `git_commands.py` (`GitCommandRunner` with its `attempt`/`run` split), `command_line.py` (the
      `Command` base and `commands_of`), and `plan_model.py` (`ItemStatus`). Its tests live in
      `.claude/hooks/tests/`, which CI already runs, so `ci.yml` is untouched and the single pytest
      line #135 also edits stays unconflicted.

      The git seam is the one extraction that was not mechanical, and reading the three copies is
      what shaped it. Their contracts are deliberately opposite - this module's `run_git` raises,
      `stack.py`'s `_git` answers nothing on failure, and #135's raises a domain error - so a single
      function could not have served them. #139's runner already had both halves, so it was moved
      rather than redesigned: `attempt` reports and `run` raises. `stack.py`'s ten call sites now
      read through `attempt`, and its 152 tests pass unchanged, which is the evidence the
      never-raises contract survived rather than being quietly tightened.

      `classproperty` is deleted rather than shared, which reverses the shape #139 adopted over
      three rounds. Measured rather than argued: both parsers are built from the instances in their
      registry, and grep found no class-level reader of `invoked_as` or `description` anywhere in
      production - the only ones were #139's own two tests, asserting the descriptor's behaviour.
      Plain abstract properties give the same refusal at the same moment, since both registries
      instantiate every subclass as their module imports. That also avoided copying
      `class_property.py` into `.claude/hooks/`, which was the alternative and would have been a
      fifth duplicated file added in answer to a complaint about duplication.

      `ItemStatus` now has one definition. `display_label` did not move with it: it is presentation,
      so the label map and a `status_label` filter live in `render_common.py`, registered on the
      environment that builds every page. That was forced by a test rather than chosen - the first
      attempt registered the filter at the call site, and
      `test_template_environment_finds_the_committed_templates` failed, correctly, because a
      template must not depend on a filter its own environment does not provide.

      Four production `sys.path` inserts are the cost, and they are the first in this repository
      -the hackery decision 8 exists to end. They are what let a script invoked as `python3
      .claude/hooks/plan_item_bootstrap.py` reach a sibling directory, and
      `dev-tooling-python-package` removes all four when it creates `development_tooling`. Two of
      the six recorded duplications stay carriers because this branch cannot reach them: #135's
      `run_git` is on neither `main` nor #139, and two of the three GitHub-backend copies are on
      #107 and #111.

      Two defects the work found rather than assumed. The scratch fixture installed the hooks but
      not the shared modules they now import, so two suites failed at import with a message about
      the wrong file - the same transitive-dependency break #110 recorded, fixed by copying shared
      wherever hooks are installed so it cannot be forgotten. And `commands_of(Command)` raises,
      because only direct subclasses are found and `Command`'s are the two abstract tool bases; a
      test of mine asked for exactly that and only the combined run exposed it, since running the
      module alone never imported the other tool. The constraint is now in the function's own
      docstring.

      Verified beyond the harness: all four entry points run standalone, `check` against this plan
      still returns the single true positive it did before, and every new test was mutation-checked
      to fail only for its own reason.

  - id: no-pr-subscriptions
    title: "Stop sessions from subscribing to their own pull requests"
    branch: claude/remove-auto-subscribe-pr-oowwkp
    track: personal-data
    depends_on: []
    status: done
    pull_request_number: 153
    session: https://claude.ai/code/session_012yfsn1ZZBsUcfKK1zDW1zh
    notes: >
      Raised 2026-08-10. The workflow's standing rule was that a session subscribes to every
      pull request it opens and handles each event until that PR's job ends. The user's
      decision inverts it rather than tuning it: a session never subscribes to a pull
      request's activity, and opening one is terminal for the session that opened it - push,
      report what is outstanding in the chat, stop. A CI failure or review comment gets
      handled when the user asks, in a session started for it.
      Two halves, because the rule lived in two places. The personal-notes half (not in the
      diff) replaced the bullet in cram-notes.md and fixed the three sections written around
      the old assumption: "Scheduled checks" called event subscriptions wanted, "Comment
      routing" justified its action-only rule by saying PR comments wake subscribed sessions,
      and "When your PR's job ends" was largely teardown for subscriptions that no longer
      exist. The repository half is #153: starter-notes.md ships the never-subscribe default
      to any new user of /setup-personal-notes, and plan-schema.md and session-start.sh drop
      "(in addition to your own item's PR)".
      Scope call: pull requests only. A plan's tracking issue is subscribed with the same tool
      but is a coordination mailbox several sessions read, not a PR one session owns, so
      /plan-create, /plan-item-kickoff and /plan-item-resolve keep theirs untouched.
      Two passages a grep for the tool name would have missed were also reworded, since they
      explained behaviour by the old rule rather than stating it: stack/README.md's exception
      leaving subscription "to an interactive session babysitting that one PR", and
      stacked-pr-maintenance's needs-resolution comment claiming it may arrive as a live event
      in the owning session. That skill's own NEVER-subscribe hard rule was already right.
  - id: integration-branch
    title: "Regenerated personal integration branch: build, check, resolve, report"
    branch: claude/plan-item-kickoff-workflow-ixbvxl
    track: stack-tooling
    depends_on:
      - manifest-currency-first
    status: in_progress
    pull_request_number: 154
    session: https://claude.ai/code/session_01Ue4PvfV5LDxHGRRS5BZB4g
    notes: >
      Designed 2026-08-10; no code exists. Three implementation attempts were blocked in the
      designing session, so this enters the plan as a design with nothing behind it.

      The constraint it addresses is upstream review throughput: pull requests are produced faster
      than cram2 merges them, so shipped-but-unreviewed features are unusable in daily work, and two
      in-flight features that conflict only discover it at the far end of the review queue. The
      answer is a regenerated integration branch - upstream main with every in-flight stack tip
      merged on top, rebuilt from scratch on demand, existing to be built from rather than to be
      history. Nothing merges out of it.

      It gates nothing, and that is a decision rather than an omission. Promotion asks whether a
      branch is ready for upstream review, judged against upstream main; integration asks whether
      the branches coexist. Gating promotion on a clean build would block A because B conflicts with
      it, with no principled reason A is the one that waits.

      Almost nothing is built from scratch: board.json and GitHubRepository for the open pull
      requests, build_stack/order for the tree, has_landed_upstream for exclusion,
      maintenance.restack for staleness, GitCommandRunner.merge plus unmerged_paths to tell a real
      conflict from a refused merge, RestackWorktree for checkout safety, merge-tree for the
      non-mutating probe, MaintenanceReport and the exit-code mapping for output, and
      write-personal-notes-file.sh for durable state.

      Based on #139's branch rather than main, because maintenance.py exists only there - git
      ls-tree main -- .claude/stack/maintenance.py is empty. That makes this a genuine stack on
      unlanded work rather than an artefact of authoring order.

      Decisions taken. No CI gate: it would deadlock against the restack, since restacking rewrites
      heads, CI re-runs, every restacked branch reads pending, and a green filter yields a
      near-empty build. A single --test run on the finished branch replaces it and is strictly more
      informative - it catches semantic conflicts that per-branch CI structurally cannot, such as A
      renaming a method while B adds a caller, both green, merging clean and breaking on import.
      Conflicts skip and continue rather than halting the build, reversing the original
      stop-on-conflict spec, because a build that halts leaves nothing to work from, which is the
      whole point of the branch. Selection unit is the stack tip, since a tip already contains its
      stack. Merge order is load-bearing once skips are possible, so it is stable and stated, and
      the report names the conflicting pair rather than the casualty - "B skipped" is not
      actionable.

      Sibling conflicts are the case this hits constantly and the one with no feature-branch fix
      available: adapting B to an unlanded A makes B depend on unmerged work, which is the stacking
      this workflow exists to avoid. rerere holds those resolutions in .git/rr-cache, contaminating
      no branch, persisted to personal-notes as a tarball because containers are ephemeral. It buys
      a working daily driver, not a discharged upstream obligation, and a replayed merge is always
      reported as replayed rather than clean.

      Judgement lives in a skill, not the script. integration.py detects, attributes and skips;
      /integration-conflict-triage reads the JSON conflict report and judges each pair reconcile,
      stack or defer. Only the first two produce a pull request comment - a deferred collision is a
      pure FYI, which the comment-routing rule already sends to the manifest rather than to a pull
      request nobody is watching. No detection-time reporting is built: the maintenance pass's
      needs-resolution label and IntegrateParent comment already cover landing time, when the target
      is stable.

      Found while designing and worth telling #139 independently: stack.PullRequest.ci is declared,
      documented, read by load_board and copied onto Branch by build_stack, but never populated -
      BoardExport._pull_request sets it from record.get("ci") against a GET /pulls payload carrying
      no such key, in the one method whose stated contract is that it refuses a missing derived
      field rather than defaulting it.

      One naming constraint is not cosmetic: builds are integration-<timestamp> with a hyphen,
      because git stores refs as files, so refs/heads/integration/<timestamp> cannot exist while
      refs/heads/integration does. The obvious naming is the one git refuses.

      Open at recording time: whether --restack defaults off locally and on in the Action, whether
      --test defaults on, whether to comment on #139 about the ci field, and whether the script and
      skill are one pull request or two (recommendation: one, since a conflict report nothing
      consumes is half a feature).

      Scope widened 2026-08-10, after recording, on the user's question of whether the skill should
      resolve conflicts as well as judge them, and whether it should ask when something is unclear
      or could be harmful. It resolves, but bounded by where the resolution lands rather than by how
      confident it is - the artifact decides the risk, not the certainty.

      On a defer verdict it resolves fully, writing into .git/rr-cache. Nothing touches a feature
      branch, the integration branch is regenerated from scratch on every build, and --test checks
      the result, so a wrong answer costs one cache entry. This replaces the design's weakest step:
      resolve --record set up a worktree and expected the developer to fix the files by hand, and
      defer is both the commonest verdict and the one where the skill has everything it needs - both
      diffs and both pull request intents. On reconcile it proposes and does not apply, because that
      is a real code change to a published pull request under review and resetting an approval to
      apply an unagreed design call is the wrong default. On stack it reports: re-basing is
      maintenance.py's work, and the base-branch PATCH 403s through the agent proxy regardless. The
      boundary is restated rather than broken - the script never writes to a branch, the skill
      writes only to rr-cache, and neither pushes.

      Asking is not driven by confidence, which would make the skill a prompt generator whose
      questions get rubber-stamped. The test is whose decision it is. Uncertainty about facts - what
      a branch does, whether two things are the same abstraction - is resolved by reading the diffs,
      the pull requests and the roadmap, never by asking. Uncertainty about intent - which
      abstraction is right, whether two branches should be one pull request - is asked. That lands
      almost exactly on reconcile, and it asks before proposing, since the proposal already encodes
      the choice. #110 and #106 building the same artifact is the precedent for the cost of not
      asking.

      One hazard follows from the skill authoring resolutions rather than replaying a human's.
      rerere matches on the conflict preimage and replays automatically, so a resolution that is
      textually matching but semantically wrong is reapplied unreviewed on every later build.
      Tolerable when a developer recorded it, different when a skill did, so a recorded resolution
      carries its provenance and the build report distinguishes skill-authored replays from
      human-authored ones - the existing rule that a replay is never silent, made auditable by
      author. A --test failure following a skill-authored resolution reports and stops rather than
      attempting another, since re-resolving into the same failure is how a build starts thrashing.

      Implemented 2026-08-10, same session as the kickoff. The three open questions were closed
      first: --restack defaults off as a plain opt-in flag (it pushes to other people's branches, so
      the default path keeps "the script never writes to a branch" true of itself rather than only
      of a path nobody takes), --test defaults on with --no-test to skip (it is the whole
      replacement for the CI gate this design dropped), and #139 was told about the ci field on its
      own pull request rather than only in this manifest, since a defect in code under review
      materially changes its review context. Script and skill shipped as one pull request.

      A fourth question nobody had asked was settled at kickoff because it is unanswerable from the
      design as recorded: what --test actually runs. It is an integration_test_command setting in
      stack.toml, defaulting to the three directories CI's test_claude_dev_tooling job already runs
      - configurable because the useful suite is a property of the repository rather than of the
      tool, and defaulted because a flag that is on by default and has nothing to run is worse than
      one that is off. A build asked for a suite the checkout names none for is refused before
      anything is built, rather than reading an absent suite as one that passed.

      Two failure shapes had to be told apart, and git makes them identical. A merge that fails
      leaving no unmerged paths is not a conflict - unrelated histories, a reference that does not
      resolve, something in the way - which is #123's false-positive class, inherited before meeting
      it. A replayed rerere resolution *also* fails leaving no unmerged paths, because the replay
      has already staged them, and is distinguishable only by what git says on stderr. So the replay
      marker has to be read before the unmerged-paths rule, and removing it makes every replay
      report as integration-failed - verified by mutation rather than assumed. Both were probed
      against real git before a line of the design was committed to.

      Verified live against the real fork from a detached worktree: 23 tips, 11 merged, 12 skipped,
      exit tip-left-out (10). Six collided with main itself - stale branches, correctly attributed
      to the base rather than to an innocent sibling - and six with siblings, including #120 against
      #111 on build_dashboard.py and #135 against #111 on ci.yml, both collisions this plan already
      knew about and neither of which the run was told about. Confirmed afterwards rather than
      assumed: the invoking checkout still on its own branch with a clean tree, the pointer at the
      build, every merged tip an ancestor of it and every skipped one not, no worktree left behind,
      and nothing pushed - the fork has no integration branch and no tip moved. The selection rule
      showed on real data too: #139 is absent from the tips because this branch is based on it, so
      its commits arrive as part of this one.

      470 tests pass across the three directories CI runs, was 428. The 42 new ones were written
      failing first against real git in #139's scratch-fork harness, and each was mutation-checked
      to fail only for its own reason. The exit status is covered from the first commit rather than
      after a live run, which is the second time this plan has had a lesson available before meeting
      it rather than after.

      Three changes to files this branch's parent introduces, each a consequence rather than a
      detour: GitCommandRunner gains per-command configuration overrides, so the build turns rerere
      on for itself without writing it into the developer's own repository, plus two named git
      methods; Configuration gains integration_test_command; and print_configuration now omits a
      setting that is empty as well as one that is unset, which its own docstring already promised
      and which only became reachable when a defaultable setting existed.

      Extended 2026-08-11, from running it on the real stack at the user's request. The build worked
      and the run found a defect in this item rather than in the branches: --test caught that the
      assembled branch does not work, and the skill had no verdict taxonomy for that - it reported
      tests-failed and stopped. A gap in the item's own deliverable, flagged before the user met it.

      Two things were missing, and only the second was obvious. A failing suite over a dozen merged
      tips names no branch, so a verdict cannot even be reached without localising it first - and
      localising it by hand is several worktrees and several suite runs, which this session got
      wrong once (a reused worktree path let two merges run in the invoking checkout; recovered by
      git merge --abort, nothing committed). So integration.py gains bisect, which re-assembles the
      tips in the same order, runs the suite after each, names the tip whose arrival turned it, and
      narrows to the earlier tip that alone reproduces it. Probes run on a detached head, so a
      bisect leaves no ref behind - the first version named them after the pair and left one in the
      clone.

      The verdicts for a semantic break are adapt, reconcile and sequence, all proposed rather than
      applied. What matters more is what is *not* available: rerere replays a merge conflict's
      resolution, and a semantic break has no conflict to key one on, so nothing can be recorded and
      every later build carries it until a branch changes. Reasoning by analogy from the merge case
      is the mistake the section exists to prevent, and a contract test pins that sentence because
      an agent that recorded a resolution would be reporting a fix that does not exist.

      The live run is what made this concrete. 22 tips, 10 merged, 12 skipped, then tests-failed:
      #110 alone passes 32 tests, #111 alone is green, merged they fail 18. #111 gives stack.py a
      module-scope import of the repository-root development_tooling package; #110's scratch fixture
      builds a minimal project without it, so stack.py configuration dies and every dependent check
      degrades to "not checked". A clean merge, no textual conflict, and invisible to both pull
      requests' CI - the exact failure class the design was written against, found on the first real
      run. Reported on #110 and #111 with the measurements, proposing nothing: which side absorbs it
      is a design call. bisect then reproduced it independently, naming #111 against #110 and
      correctly leaving out the innocent tip merged before them.

      479 tests pass across the three directories CI runs, was 470 and 428 before the item. The
      integration branch was pushed to the fork at the user's request, which the tool itself never
      does.

      Review round 2026-08-11, 28 threads. All are answered; none is actioned and none is resolved.
      The five needing a decision were settled with the user and the implementation was handed to a
      fresh session on their instruction, so this entry records decisions rather than work. The full
      plan is in the PR-progress note for the branch, which is what a session's own SessionStart
      hook loads.

      The base changes to #151 rather than #139. #151 rebased onto #139 while this round was being
      worked, so 6fd229ff3 contains ebf67734 and one merge now brings the eleven-module split,
      class_property.py and the manifest subcommands together. That matters beyond tidiness: most of
      the round's "you duplicated #139" threads exist only because this branch was cut before that
      split, so it independently grew its own error base, command classes and git helpers. They are
      answered by merging the parent, not by writing new abstractions. A conflicted #139 merge was
      started before the rebase landed and has been aborted.

      The escalation decision reverses this item's own recorded design. A semantic break was to be
      reported and left; the skill now pushes a mimic test to the breaking branch, comments, applies
      a blocking label, writes the manifest and republishes the dashboard, reusing the scripted
      pipeline rather than restating it as prose. The test belongs on the breaking branch because
      the relying branch cannot express a test against an import that does not exist on it yet,
      which is exactly the #111 against #110 shape.

      The label must be a new one, and the reason is a defect this round found rather than a
      preference. WithholdBranchStillConflicting clears needs-resolution whenever mergeable_state is
      not dirty, and a semantic break never makes a pull request dirty, so reusing that label means
      the next maintenance pass strips it and the loop the label exists to close reopens. The new
      label still has to block, so Configuration.needs_resolution_label generalises into a
      collection read by both the withholding step and promotion's exclusion, with the auto-clear
      left keyed to needs-resolution alone.

      Two smaller reversals. A suspect-replay now discards the bad rr-cache entry and triages
      normally instead of stopping, since stopping left the developer with a poisoned cache and no
      instruction. And a pull request based on both conflicting branches was proposed and declined:
      a pull request has one base, so it is a diamond, its diff swells to contain the whole second
      branch, it cannot promote alone, and restack_plan derives exactly one parent per branch.

      The verdict moves to GitHub CI. build pushes the branch, prints the run URL and exits, and a
      separate subcommand reads the conclusion; localisation pushes every prefix at once so CI runs
      them concurrently. integration_test_command, --test and --no-test all go, and the tests-failed
      status stays with only its source changed. This narrowly reverses the "the tool pushes
      nothing" property, to a branch the tool owns and regenerates and never to a feature branch.
      Reachability was not assumed: #146 already measured that actions/runs, jobs and job-logs all
      answer 200 from a session, and that this fork's queue time is a median of 0s.

      Also retracted here, since this entry is where it was recorded: the greenlet claim. 3.5.5 does
      publish cp312 manylinux wheels and an sdist, so a test_each_lib red must be judged on its own
      evidence rather than against that note.

      depends_on corrected 2026-08-12 from stack-maintenance-executor to manifest-currency-first.
      The recorded dependency was the one this item was kicked off against; #151 has since rebased
      onto #139, making the chain #139 to #151 to #154 linear, and the 08-11 review round was
      replanned against #151 as the base for that reason. #139 stays a dependency transitively
      through #151, so naming it as well would record the same edge twice. Not yet true of the pull
      request itself: #154's base ref is still #139's branch, so a reparent is outstanding.

      Resolved 2026-08-12 (session https://claude.ai/code/session_01AYLtTRh7uZu64oLpMhGjQR). Four of
      the handover's five parts are done; part D is not started. 595 tests pass across the three
      directories CI runs, from 479. All five entry points run standalone.

      The base landed rather than being merged from the sibling: #139 merged into main at 14:36, so
      the eleven-module split arrived through main and only #151's `.claude/shared/` extraction had
      to come from the branch. `abb7d994d` merges main, `336b393e2` merges #151, `c3d0d1082` deletes
      `integration.py`'s own command ABC for `.claude/shared/command_line.py`'s `Command`. Four
      conflicts were one rename seen from four places, resolved to #151's rename while keeping
      main's docstring formatting - #151 predates that reformat, so taking its side wholesale would
      have reverted it.

      Three of the handover's instructions had expired and following them literally would have been
      wrong. `GitCommandRunner` is `.claude/shared/git_commands.py`, not
      `maintenance_git_commands.py` (which now holds only `MaintenanceGitCommandRunner`);
      `maintenance_errors.py` is `.claude/shared/exceptions.py`; and `class_property.py` is deleted
      in favour of abstract instance properties, so re-adding a `classproperty` would have put back
      the file its own base had just removed. The lesson is the one this plan already records about
      basing, met one level down: a handover note is a claim about a moment, and its first step
      should be re-reading what it points at rather than trusting it.

      Only reviewed work is carried, which was the session's added requirement and is not the
      one-line filter it reads as. A tip contains its whole stack, so filtering tips would merge
      ready #36 together with drafts #33, #34 and #35 as its ancestors - the reading of "only ready
      pull requests" that quietly does the opposite. Readiness is therefore read down the entire
      chain by `select_for_build`: a stack that is draft at its root is left out entire, and the
      branch merged for a stack is the last one reached before its first draft.
      `BranchStatus.is_out_of_draft` covers `IN_REVIEW` as well as `READY`, because `derive_status`
      gives `in-review` precedence and asking for `READY` alone would drop every branch already
      promoted upstream. Live board: 22 tips to 9. Everything left out is named in the report as
      `unreviewed` and does not reach the `tip-left-out` exit status, since a draft excluded is the
      rule working rather than a build going wrong.

      Two consequences have their own tests because both are easy to get wrong: `claimed_as_parent`
      ranges over the carried branches only (a parent is left out because a child contains it, and a
      draft child is never merged), and every branch left out is named with the draft beneath it
      when that is the reason. A mutation check found a real hole rather than confirming the tests -
      nothing covered the wiring from the selection into a real build's report, so removing it left
      the suite green; closed with a scratch-fork test. One test was removed rather than kept,
      because mutation-checking showed it failed only for reasons its neighbours already covered
      while its docstring claimed a rule it did not exercise.

      `integration-conflict` is a second blocking label rather than a reuse of `needs-resolution`,
      and reading the code first is what caught it: `WithholdBlockedBranch` clears
      `needs-resolution` whenever GitHub stops reporting a pull request as conflicted, and a
      semantic break never makes one conflicted - so reusing that label means the next maintenance
      pass strips it, silently reopening the loop the label exists to close.
      `Configuration.blocking_labels` is read by both the withholding step and promotion's
      exclusion; only the automatic clearing stays keyed to `needs-resolution`.
      `WithholdBranchStillConflicting` became `WithholdBlockedBranch` with it.

      `integration.py escalate` localises a break, labels the branch causing it and comments naming
      what it breaks. `bisect` became `locate-break` throughout, `TipOutcome` became
      `PullRequestStackTipOutcome`, the report's keys became a `ReportKey` StrEnum and the settings
      keys a `ConfigurationKey` one, the git configuration pair became a `GitSetting` dataclass, and
      the test module's repeated literals became named constants with its suite runner extracted to
      `tests/dataset/check_the_build.py`.

      All 28 review threads have inline replies and 22 are resolved. Six stay open deliberately: the
      draft conversion (declined, with reasons), the per-pair GitHub issue (left for the user to
      decide), the `DataclassException` thread and the whole-CI thread (both answered but pending
      part D), and the two `classproperty` threads (#151 reversed the ask by deleting the file).

      Part D - CI as the verdict - is not started, and the reasons are worth carrying. It needs a
      GitHub Actions client that does not exist in this tree, since #146 measured the reachability
      but is unlanded; it rewrites the localisation path that `escalate` now depends on, so a
      half-migration leaves the verdict split across two mechanisms; and its verification is a real
      CI run on a pushed branch, which is the one thing everything else on this branch was able to
      check against real git in the scratch fork. `integration_test_command`, `--test`, `--no-test`,
      `TestCommandNotConfiguredError` and `_run_tests` all still exist and all still work - part D
      is what removes them.

      #154's base ref is still #139's branch: the base-field `PATCH` 403s through the agent proxy,
      so the reparent onto #151's branch remains a manual step in the UI.

      Correction to the paragraph immediately above, made minutes after writing it by actually
      attempting the reparent rather than restating the recorded claim. Both halves of it were
      wrong. The base-field change is not blocked by the agent proxy - the GitHub MCP tool performs
      it, which is exactly what #143 recorded on throwaway PR #129 (only the raw `curl` `PATCH`
      403s), and it succeeded here twice. And the reparent itself is wrong to do today: #151's
      branch is 159 commits behind `main` and 17 ahead, so basing #154 on it swelled the diff from
      45 files to 261, adding the whole of `main` that #151 does not carry. Reverted immediately;
      the pull request is back to `main`, 45 files, 28 commits, still a draft.

      So `main` is the honest base rather than a placeholder: this branch is 0 behind `main`, and
      `git merge-base --is-ancestor` confirms it already contains #151's head, so nothing is lost by
      sitting on `main` while its parent is unlanded. The reparent becomes correct once #151 merges
      `main`, and not before. That is the inflated-diff shape PR #41 cost this plan a whole item to
      repair, met from the other direction - there a child sat on a base that had landed, here a
      child would have sat on a base that had not caught up.

      The generalizable half is about the manifest rather than about git: a note saying an operation
      is blocked is a claim with an expiry date, and this one had two expiries at once - the client
      changed (#143 found the MCP tool works) and the target moved (#139 landed, so `main` overtook
      #151). It had been carried forward verbatim through two entries. Attempting a
      recorded-as-blocked step, when it is cheap and reversible, is worth more than restating it.
  - id: setup-runs-without-asking
    title: "Run the personal-notes setup from the planning skills instead of asking first"
    branch: claude/auto-run-setup-notes-02nhxv
    track: personal-data
    depends_on: []
    status: in_progress
    pull_request_number: 156
    session: https://claude.ai/code/session_01RydZutnNtFBYeT73Qt4Bxv
    notes: >
      Raised 2026-08-12. Every planning skill defers its step 0 to one shared document,
      prerequisite-check.md, which told the session to run check-setup.sh and then ask, via
      AskUserQuestion, whether to run /setup-personal-notes. So a clone missing any part of the
      setup - in practice a fresh container, where the dashboard dependencies are never installed
      - opened every /plan-dashboard, /plan-create and /plan-item-* invocation with a yes/no
      question before doing any of the work asked for. The user's decision removes that gate:
      state what is missing, invoke the skill, re-run the check, carry on.
      The questions that survive are the ones inside /setup-personal-notes itself, and they are a
      different kind: which remote holds the notes, what the notes start out saying, whether to
      create the merged/bug/in-review labels, whether to restore a diverged tracked file. Each
      picks a destination to write to rather than deciding whether to act, and guessing one pushes
      personal notes somewhere the user did not choose. Only "shall I run this at all" is gone,
      which had exactly one useful answer from a user who had already invoked the skill.
      Standalone rather than stacked, checked mechanically: all eight touched paths exist on main,
      so this edits landed files, not an unlanded PR's. Two open pull requests are
      conflict-adjacent without owning this work - #107 rewrites setup-personal-notes/SKILL.md into a thin
      wrapper over a new script, and #149 edits step 0 of plan-item-kickoff and plan-item-resolve
      for execution modes - and neither touches prerequisite-check.md, where the gate actually
      lived.

      Review round 2026-08-12, one comment posted twice: the step 0 section is duplicated across
      the skills, so define it once and share it - unless another pull request already does that,
      in which case leave it there. It does: #149 collapses the two plan-item copies into
      plan-dashboard/plan-item-gathering.md, so the deduplication is left with it rather than two
      branches restructuring the same paragraphs. Answered with the measurement rather than a
      judgement: the procedure is already single sourced in prerequisite-check.md, and what
      repeats is a one line citation plus a per-skill consequence clause - the same shape
      scope-decision.md, dependency-readiness.md and pr-data-fetching.md already have. Both
      threads replied to and left open, the outcome being a deferral rather than a change.

      That review corrected this entry's own conflict resolution, which was wrong. #149's
      plan-item-gathering.md and #135's add-plan-item/SKILL.md are both new files carrying the
      superseded "offer /setup-personal-notes" wording, so neither conflicts with this branch and
      nothing flags them at merge time - landing either afterwards silently reinstates the gate
      this item removes. For the two plan-item skills the resolution is to take #149's deletion
      and carry this item's wording into its shared document, not to keep both edits. #135 also
      conflicts in prerequisite-check.md itself, where it only adds add-plan-item to the opening
      list, so there the resolution is this rewrite plus that one name. Both were flagged on
      their own pull requests, each being a change to what that pull request ships.

      That flagging is coordination, which nothing enforces, so the rule stopped being prose
      only: test_setup_prerequisite_documents.py sweeps every markdown document under
      .claude/skills for a verb of offering governing /setup-personal-notes. Discovered
      rather than listed, because the case it exists for is a document that does not exist
      yet - #149's plan-item-gathering.md and #135's add-plan-item/SKILL.md are both new
      files, so no merge flags either. Narrow by design: it catches the sentence the rule
      replaced rather than every paraphrase of asking, and must not fire on the prose that
      explains the rule, since prerequisite-check.md discusses offering and asking
      throughout. Passes here over 15 documents and flags 4 on main, 3 on #149 and 5 on
      #135, which is the mutation check. What it buys is that landing order stops mattering:
      whichever lands second goes red rather than silently reinstating the gate.
  - id: deferred-items-hidden-by-default
    title: "Hide deferred items in the dashboard by default, behind their own toggle"
    branch: claude/hidden-deferred-plan-items-6weef1
    track: dashboards
    pull_request_number: 157
    depends_on: []
    status: in_progress
    session: https://claude.ai/code/session_01Je624Mq716GToQRqtkTXXn
    notes: >
      Raised 2026-08-12. A deferred item is intentionally paused or superseded, so it is
      the same kind of noise on a dashboard as a done one, and done items have been hidden
      behind a sidebar toggle since the dashboard existed. rdr-refactor is the plan that
      shows it: eight of its forty-five items are deferred, and they sit inline among the
      live ones. This gives deferred its own "Show deferred items" checkbox, hidden by
      default, exactly parallel to "Show done / merged items".
      The substance is the indentation, not the checkbox. An item's indent level is its
      depth in the same-track depends_on chain, and the existing toggle already carries a
      second precomputed level ("as if every done item were not a dependency at all") so a
      dependent dedents to zero rather than hanging one level in from an invisible parent.
      A second independent toggle makes that four combinations, not two, so
      StackedItem stops carrying named pairs of fields and carries one position per filter
      state instead - keyed by a StatusFilter enum whose value doubles as the page's CSS
      class and the item's CSS custom-property suffix, so adding a third hidden status
      later is data, not another pair of fields and another template branch.

  - id: pinned-stack-tooling
    title: "Pin the stack tooling for the length of a maintenance pass"
    branch: claude/stack-tooling-pinning-qf5r2m
    track: stack-tooling
    pull_request_number: 158
    depends_on: []
    status: in_progress
    session: https://claude.ai/code/session_016DukdvsJzggwAAF3CkKaDZ
    notes: >
      Bug found while running /stacked-pr-maintenance on 2026-08-12, and fixed at the
      source rather than worked around. The skill invoked the tooling by its in-tree path
      for a whole run, which assumes .claude/stack/ is the same file from step 0 to the
      end - and it is not, since that path is tracked content and a pass switches branch
      in the checkout it runs from. The failure is either loud (a command validated in
      step 0 comes back as an argparse invalid choice, which is exactly what the
      preflight -> check-move rename produces) or quiet (the same command name answering
      with semantics the skill does not describe). In the reported run it produced a
      string of spurious failures partway through a restack pass, survived only because
      the operator hand-copied the tool to a stable path and continued from there.
      stack.py pin-tooling copies the modules and stack.toml - not board.json, which
      belongs to the pass that exported it - to a digest-named directory outside every
      checkout and prints the copy's stack.py. Digest naming makes pinning idempotent per
      version and stops a later pass overwriting the copy an earlier one is still
      running; the copy is staged beside its destination and moved there whole, so a
      caller never invokes a half-written one. SKILL.md step 0 gains part c, pinning the
      moment configuration answers, and every command after it names the pinned copy.
      Deterministic rather than a note to remember: it is a subcommand, and a test asserts
      the skill runs nothing else from the working tree.
      New rather than a change to something in flight, tested mechanically:
      git ls-tree main -- .claude/stack/stack.py .claude/skills/stacked-pr-maintenance/
      returns both, so this edits files #106 and #139 already landed. #110 is the only
      in-flight branch touching .claude/stack/ and owns setup, not the maintenance pass.
      #139's RestackWorktree already isolates the executor's own branch switching, which
      is one instance of this hazard; pinning closes the rest - the commands a session
      runs by hand and the resume paths - and removes the assumption instead of leaving
      each future step to remember the worktree.

      Review round 2026-08-12, two items, applied in 0c7eef93. The inline one asked the test
      helper to use the GitCommandRunner class from the branch that implemented it; there is
      nothing to base on, since that class landed on main with #139 hours earlier, so the helper
      calls main's copy directly. #151 only moves it to .claude/shared/ and is 159 commits behind
      main, so basing a standalone bug fix there would have inflated the diff the way #41's stale
      base did. Replied and left open, since the literal ask was answered differently.

      Using the runner exposed a weakness in the test that the raw invocation had hidden: the
      helper created the branch and then wrote the other version, so the working tree changed
      because the test wrote it rather than because version control moved it - a weaker
      reproduction than the bug. Both versions are committed at install time now and the step is
      nothing but a branch switch. Mutation-checked: removing the switch now fails the hazard
      test, where before the write masked it.

      The review-level question - whether fast-forwarding fork main and restacking everything
      first would solve this instead - is answered on the pull request and calls for no change. It
      is circular, since the restack is performed by the tool whose version is in question, so
      uniformity arrives at the end of the pass and the tool must be stable at the start. It also
      cannot reach the branches that differ most: #110 and #111 both carry preflight rather than
      check-move and no maintenance.py at all, and both are needs-resolution, which is exactly
      what WithholdBranchStillConflicting withholds. And a branch whose own diff edits the tooling
      is meant to differ. 471 tests pass across the three directories CI runs.

  - id: plan-item-bootstrap-yaml-indent
    title: "Fix plan_item_bootstrap writing item fields at the wrong indentation"
    branch: claude/plan-item-bootstrap-yaml-indent-y8qs0m
    track: personal-data
    depends_on: []
    status: in_progress
    pull_request_number: 160
    session: https://claude.ai/code/session_0157EHWeTzFSYf1nqHX5J7NC
    notes: >
      Bug fix off main, found by using #143's script on rdr-refactor: open patched the
      item's fields at a hardcoded four-space indent, which does not parse in a manifest
      whose items are written flush with items: (rdr-refactor's style), so save-plan.sh
      failed in yaml.safe_load. The depth is now read off the manifest being edited. Two
      defects in the same path fixed with it: the save swallowed save-plan.sh's output,
      and reported success without checking the write landed - it now raises with the
      script's own message, and reads the plan back off the notes branch afterwards.
      This item's own entry was written by the fixed script.

  - id: promotion-summaries-and-table
    title: "Promote with a written summary, and report the links as a table rather than a notification"
    branch: claude/plan-item-kickoff-workflow-y38l38
    track: stack-tooling
    depends_on:
    - stack-maintenance-executor
    - pinned-stack-tooling
    status: in_progress
    pull_request_number: 162
    session: https://claude.ai/code/session_01RTWnbUYSauqy9peX7JFJUz
    notes: >
      User's request, 2026-08-13, about the promotion phase of a maintenance pass: the
      compare-and-create link must be built by the script rather than assembled by the
      session; the upstream pull request must open with a title (the fork pull request's
      own, unless the caller overrides it) and with a body that always links back to the
      fork pull request and otherwise carries text the caller supplies; the skill must
      always supply that text as a point-based summary; neither the skill nor the Routine
      may send a notification; and every promoted branch must be reported as a table, in
      the session the pass ran in, with its link ready to open.

      Three of those already have a home. PromotionLink.build takes a title and a body and
      encodes the URL; maintenance_promotion.promote passes the fork pull request's title
      through unchanged; and _prefilled_description already appends the "Full detail" link
      back to the fork pull request. What is missing is the body itself. promote derives
      it with promotion_summary, which takes the fork description's first paragraph
      verbatim - so the upstream reader gets whatever that description happened to open
      with, and a description opening with a heading, a badge or a link opens the upstream
      pull request with that. Nobody has written a word for the upstream reader.

      The body cannot be derived, which is what makes this the one model-shaped step in an
      otherwise mechanical pass: the script owns the title, the fork link, the encoding,
      the length budget and the table, and the session supplies only the bullets. Proposed
      interface, to settle at kickoff - promote and run-report read the summaries from a
      file keyed by fork pull request number, and a promotable branch with no entry is
      reported as awaiting one rather than promoted with a body nobody wrote (the report
      already carries promotable, so the session has the list it must write for). Two
      invocations is the honest shape, since a summary cannot exist before the pass has
      decided a branch is promotable: run the pass, read promotable, write the bullets,
      promote. Whether that is worth collapsing into one is the kickoff's call.

      The table replaces a hand-assembly the skill asks for today. SKILL.md's Finish
      section already wants every pending create-link listed - built this pass, plus every
      fork pull request still carrying cram2-link-sent without in-review, its link rebuilt
      with promotion-link - and leaves the session to compose it. That is a rendering, so
      it belongs to the executor: a command emitting one markdown row per pending
      promotion (number, title, branch, link), which the session pastes. maintenance.py's
      commands are auto-discovered MaintenanceCommand subclasses, so adding one is writing
      a subclass and nothing else has to be told it exists.

      Notifications off is the reason the table has to exist. SKILL.md's Finish section
      currently rests on "a scheduled run is configured to email its summary, so the
      summary *is* the delivery", and that premise goes. The link also stays written into
      the fork pull request's own description under the Promote heading, which is what
      makes a summary nobody reads harmless. Open question, not to be invented: an already
      registered Routine's notification setting has no field on update_trigger, so whether
      it can be changed in place or needs re-registering has to be checked before
      routine-prompt.md tells anyone how to do it.

      New-vs-change was tested rather than judged. git ls-tree main returns
      .claude/stack/maintenance_promotion.py, .claude/skills/stacked-pr-maintenance/SKILL.md
      and the skill's routine-prompt.md, so this edits files #106 and #139 have landed.
      Two in-flight branches overlap. #158 (pinned-stack-tooling) rewrites every command
      invocation in SKILL.md and asserts the exact set still run from the working tree, so
      a command added here must be pinned too - a real ordering dependency, recorded above.
      #155 (claude/routine-prompt-refresh-ps5l3z, untracked by any plan item, unlanded,
      carrying cram2-link-sent but not in-review) is what introduces "turn its completion
      email on" - the one paragraph this item reverses. Under the fold rule that paragraph
      is #155's own work while #155 is unlanded, so check at kickoff whether its upstream
      pull request has been created yet: if not, fold the reversal into #155 and leave
      everything else here, which stands alone either way.

  - id: integration-branch-ci-verdict
    title: "Move the integration branch's verdict to CI, with a stable branch and a targeted job"
    branch: null
    track: stack-tooling
    depends_on:
    - integration-branch
    status: not_started
    notes: >
      Split out of `integration-branch` (#154) on 2026-08-13, on the user's decision, rather than
      folded into it. Part D of that item's own review round: the verdict moves from a local
      `--test` run to GitHub CI. It was deferred there with three reasons and has since gained two
      more requirements from review, which is what made it its own item.

      The prefer-the-change test was run rather than judged by feel. Part D does modify what #154
      introduces - it deletes `integration_test_command`, `--test`, `--no-test`,
      `TestCommandNotConfiguredError` and `_run_tests`. But strip those deletions and what remains
      is a GitHub Actions client, a CI job, a pytest marker, a second long-lived branch and a
      subcommand reading a run's conclusion, which stands alone by a wide margin. Same answer the
      rule gave #110 against #106.

      The three reasons it was not started on #154 all still hold. It needs an Actions client this
      tree does not have, since #146 measured that `actions/runs`, `jobs` and job-logs answer 200
      from a session but is unlanded. It rewrites the localisation path that `block-branch` now
      depends on, so a half-migration leaves the verdict split across two mechanisms. And its
      verification is a real CI run on a pushed branch, which is the one thing nothing else on #154
      needed - everything there was checkable against real git in the scratch fork.

      One measured fact shapes the whole design and should be re-checked at kickoff rather than
      inherited: `ci.yml` triggers on `push` to `main` and on `pull_request`, and nothing else. A
      pushed integration branch therefore gets no CI at all unless a pull request exists for it.
      That is why the user's "a PR into the existing stable integration branch" framing is the only
      shape that reaches a verdict.

      First added requirement: a stable branch and a candidate. `integration` becomes the last build
      whose CI went green rather than a pointer moved to whatever was built last; each new build is
      a candidate, opened as a pull request into it, and merged once green. A tension to settle
      rather than paper over: a build is regenerated from scratch from the upstream base plus the
      tips, so a candidate shares no history with the stable branch, its pull request's diff is
      everything that changed between two independent builds, and merging it makes `integration`
      accumulate history -the one property this design has held since the item was recorded. Whether
      the stable branch is a real merge target or a second pointer force-updated on green is the
      first decision.

      Second added requirement: a pytest marker, and a CI job that runs only what it marks. The
      failing test step 5 pushes to the breaking branch carries a marker named for the label; a job
      runs only marked tests. That gives a verdict in a fraction of the matrix's time, and it gives
      `integration-conflict` an automatic clearing condition it does not have today - the 08-11
      round established that `WithholdBlockedBranch` cannot clear it, since a failure between two
      cleanly merging branches never makes a pull request conflicted, and the label ships documented
      as never cleared automatically for that reason. A marked test that passes is a different and
      correct clearing condition. The user also offered a third option worth keeping: the script
      that writes and clears the label could run the marked tests itself, or trigger only that job,
      rather than depending on the whole run.

      Two review threads on #154 stay open pointing here rather than being resolved: the
      `DataclassException` one, whose subject `TestCommandNotConfiguredError` this item deletes
      rather than converts, and the whole-CI thread this item is the answer to.
