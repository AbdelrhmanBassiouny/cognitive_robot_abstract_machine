schema_version: 1
id: rdr-refactor
title: RDR / EQL Master Roadmap
description: 'Programme to land the EQL-native RDR engine, the feature layer ("poor man''s Rete"), MCRDR/GRDR
  multi-tree engines, concept trees (NRDR), OO integration, and TMS justifications on top of EQL — plus
  the Why & Montessori explanation track that was prioritized alongside it.

  '
default_repository: AbdelrhmanBassiouny/cognitive_robot_abstract_machine
tracking_issue: 94
waves:
- id: wave-0
  name: Wave 0 — finish landing the engine
- id: why-montessori
  name: Why & Montessori track (priority after Wave 0)
- id: wave-1
  name: Wave 1 — independent foundations
- id: wave-2
  name: Wave 2 — concept trees
- id: wave-3
  name: Wave 3 — OO integration + TMS
- id: expert-framework
  name: Expert framework — capability-declaring experts + collaboration
- id: wave-final
  name: Wave Final — land once the RDR engine is stable
tracks:
- id: S0-steward
  name: Steward — the split-PR stack
  wave: wave-0
  description: 'Babysit the open stack bottom-up; merge in dependency order; propagate restacks forward
    after each merge.

    '
- id: S1-d-ui
  name: D-ui — interactive expert interface
  wave: wave-0
- id: S2-d-deco
  name: D-deco — @rdr decorator + file store
  wave: wave-0
- id: oo-prototypes
  name: OO-track prototypes (stale bases, harvested by Wave 3)
  wave: wave-3
- id: why-track
  name: Why track (W1-W3, C1)
  wave: why-montessori
- id: montessori-track
  name: Montessori demo (M1-M2, deferred)
  wave: why-montessori
- id: track-f
  name: Track F — feature layer
  wave: wave-1
- id: track-g
  name: Track G — multi-tree engines (MCRDR/GRDR)
  wave: wave-1
- id: track-t
  name: Track T — truth unification
  wave: wave-1
- id: concept-trees-track
  name: Concept trees (NRDR)
  wave: wave-2
- id: oo-definitions-track
  name: OO integration
  wave: wave-3
- id: justifications-track
  name: TMS justifications
  wave: wave-3
- id: expert-capabilities
  name: Capability-guarded experts + ensembles
  wave: expert-framework
- id: comms-track
  name: Audience-dependent explanation rendering
  wave: why-montessori
- id: engine-runtime
  name: Engine runtime behaviour (no-fire handling, defaults, provenance)
  wave: wave-1
- id: docs-final
  name: Docs — land the architecture brief once the engine is stable
  wave: wave-final
items:
- id: query-class-refactor
  title: 'EQL query refactor: spec/product lifecycle, subquery caching, result-transformer pipeline'
  branch: query-class-refactor
  pull_request_number: 5
  track: S0-steward
  depends_on: []
  status: done
  notes: Merged to main.
- id: eql-core-prep
  title: EQL core semantics (foundation for the RDR engine)
  branch: eql-core-prep
  pull_request_number: 28
  track: S0-steward
  depends_on:
  - query-class-refactor
  status: done
  notes: Merged to main.
- id: code-extraction
  title: Extract source-extraction utilities into krrood.code_generation
  branch: code-extraction
  pull_request_number: 58
  track: S0-steward
  depends_on:
  - eql-core-prep
  status: done
  notes: Merged out-of-band 2026-07-21 (branch pushed directly, then closed by hand; GitHub never set merged_at, so this PR carries a manually-applied 'merged' label instead). Previously open, CI green; picked up an in-review label 2026-07-20.
- id: code-generation-extract
  title: Extract code_generation package (generation half)
  branch: code-generation-extract
  pull_request_number: 39
  track: S0-steward
  depends_on:
  - code-extraction
  status: done
- id: ripple-down-rules-refactor
  title: ripple_down_rules refactor (reconciled to code_generation)
  branch: ripple-down-rules-refactor
  pull_request_number: 53
  track: S0-steward
  depends_on:
  - code-generation-extract
  status: done
  notes: 'Replaces closed PR #40. Qt/GUI + JSON serialization removed here.'
- id: rdr-backward-inference
  title: 'RDR engine: backward inference + condition resolvers'
  branch: rdr-backward-inference
  pull_request_number: 41
  track: S0-steward
  depends_on:
  - ripple-down-rules-refactor
  status: done
  session: https://claude.ai/code/session_012xQWiZExn9frHtbfm44eR1
  notes: 'Recorded blocker (base_expressions.py conflict over _last_parent_of_type_) cleared:
    PR #89 merged to main 2026-07-30 and cascaded the fix down the stack as predicted. Verified
    live, not just via the notes: GitHub reports #41''s mergeable_state as clean, and an independent
    local `git merge-tree` dry run of main against rdr-backward-inference produced zero conflict
    markers. The 4 review threads open then were stale GitHub bookkeeping only -- their content was
    already addressed by commit 7faa806, confirmed against the current file heads -- replied to with
    the specific evidence and resolved 2026-07-31.
    2026-08-02: reparented onto main (PR comment 5157212715) now that ripple-down-rules-refactor''s
    content has landed via #53, so depends_on below is satisfied and the diff is the real
    7 files / +1,318 rather than the stale-merge-base 268 files.
    2026-08-03: the "ready for the steward to merge" claim above is superseded -- a new review
    thread (r3702021144, backward_inference.py:64) asks whether GuardCondition.negated should be
    replaced by wrapping the guard expression in Not(), considering every use of the guard across
    this plan rather than just this PR. It is a discussion request, so the PR is blocked on the
    developer''s call, not on code. Answered at r3702169709 recommending the field be kept: the
    alternative was already designed and rejected in backward_inference_design.md ("no live tree
    mutation"), and the hazard was re-verified live -- factories.not_ -> _invert_ -> Not(self) ->
    base_expressions.py:299 child._parent_ = self reparents a live rule-tree node, the same defect
    class dag-facade-hardening (#96) exists to fix. Recommendation carries an expiry: Not()-wrapping
    is cleaner at 4 of 6 concrete sites and wins outright once #96 lands a non-mutating negation.
    CI green, 20/20 checks on head cbbf7bf3. Thread deliberately left unresolved. Full account in
    roadmap.md section 12.
    2026-08-03 (later): resolved -- the developer resolved thread r3702021144 without counter-argument
    and marked #41 ready for review. Decision: keep GuardCondition.negated. All 23 review threads are
    now resolved, draft: false, mergeable_state: clean, so #41 is genuinely ready for the steward to
    merge as the stack bottom. Three small follow-ups offered in the reply are NOT yet applied and
    need the developer''s go-ahead, since pushing would force the PR back to draft per the standing
    convention: record the no-live-tree-mutation rationale in the field docstring (its design doc is
    on the dropped krrood/docs/ path), correct the misleading evaluation comment at
    backward_inference.py:82-90, and TDD-pin whether the isinstance(OperationResult) branch in
    holds_for is reachable before deleting it.
    All three applied and pushed as 29c27cca; PR converted back to draft per convention. The
    reachability probe corrected an earlier claim of mine: evaluate() does not only yield true
    results -- a leaf predicate yields a literal False when it fails, so bool(result) is
    load-bearing and "did it yield anything" would have been wrong. Only the isinstance branch
    was dead. Verified against a clean run of the previous head in the same container: 206 failed
    / 935 passed before, 206 failed / 937 passed after, the +2 being exactly the new tests.
    scripts/format_docstrings.py was deliberately not run on the result -- it rewrites the whole
    module (125 unrelated lines) and regresses ":return: ``True``" to ":return:``True``"; flagged
    on the thread as deserving its own pass. The _materialize live-node mutation is filed as
    dag-facade-hardening''s non-mutating-negation item, per the developer''s call; when that
    lands, GuardCondition.negated should be revisited, since Not()-wrapping was cleaner at 4 of
    6 sites and the mutation hazard was the only thing against it.
    2026-08-03 (final): the developer marked #41 ready for review again after the push. CI green
    20/20 on head 29c27cca including test_each_lib (krrood), draft: false, mergeable_state: clean,
    all 23 threads resolved. CI passing is the stronger verification here -- the resolve session''s
    own container lacked the dependency set entirely (206 environment failures, identical before and
    after), whereas CI runs the real one. Nothing outstanding: #41 is ready to merge as the stack
    bottom, and the next steward pass should cascade it forward through #63-#67/#98. 2026-08-08:
    reopened by the developer as a design question -- should GuardCondition move into
    rules/conclusion_selector.py, should the selectors move into rdr/, or is the isinstance dispatch
    in _leaf_guards/_collect_rule_paths fine as a closed set? Answered: neither move (conclusion_selector.py
    has EQL-core consumers in factories.py/scope.py/query_graph.py, so moving the selectors inverts the
    layering; GuardCondition has only rdr/ consumers, so moving it does the same in miniature), but the
    open/closed instinct was right for a reason other than extensibility -- ConclusionSelector already
    makes the insertion side polymorphic (_get_current_context_condition, _create_between_two_expressions)
    while traversal was an isinstance ladder over the same three classes, and one per-selector rule set
    lived in two functions with nothing forcing them to change together. Landed as rdr/branch_semantics.py
    (SelectorBranchSemantics, one class per selector holding both sibling_guards and branches), dispatching
    through krrood/patterns/specificity_ranking.py -- the same primitives behind the grammar PhraseRule
    registry and the SpecificityRule families -- plus a new rdr/exceptions.py for
    AmbiguousBranchSemanticsError. Behaviour unchanged: the 33 existing tests pass untouched, 12 new ones
    added (33 -> 45), and the test_eql + test_eql_rdr sweep went 109 failed/921 passed -> 109 failed/933
    passed with the 264 failed+errored ids byte-for-byte identical. A suspected soundness bug was probed
    and NOT confirmed: _leaf_guards(Alternative, negated=False) returns both sides into a conjunctive
    SufficientConditionSet, but instrumenting four DSL-built shapes recorded 0 positive-Alternative calls
    (only a hand-built Refinement(Alternative(A,B), C) triggers it), because refinement() anchors on a
    with-entered condition while alternative()/next_rule() anchor on the conditions root -- so an
    Alternative always sits above a refinement, never as its left child. No semantics changed; the
    constraint is now documented on AlternativeBranchSemantics. Pushed as 19e387a9 and converted back to
    draft per the always-drafts-until-ready convention. Scoping note: the change touches only files #41
    introduces, so per the fold-don''t-stack rule it went onto #41 rather than becoming a separate PR from
    the session branch claude/rdr-guard-conclusion-arch-8htmfu. Neither subscribe_pr_activity tool could
    subscribe this session to #41, so its CI result is unwatched -- check it manually.
    2026-08-08 (review round): 11 new threads on 19e387a9. Developer chose to keep the
    branch-semantics family and apply every other fix; my own recommendation in the session
    had been the opposite (collapse it into methods on the selector classes), so that thread
    is deliberately left unresolved for the developer to close. Landed as b0107c76: the
    dispatch key is now a bound generic parameter via SubClassSafeGeneric instead of a
    ClassVar (frozen=True had to be dropped -- a frozen dataclass cannot inherit from a
    non-frozen one); every method is a classmethod so most_specific_for returns the class and
    nothing is constructed; GuardCondition moved to its own rdr/guard_condition.py, which is
    what let LeafGuardDecomposition drop its quoted type arguments (backward_inference
    runtime-imports branch_semantics, so the reverse import was a cycle); the abstract methods
    take ConclusionSelector rather than SymbolicExpression; GuardedBranch.node became
    child_expression. The reviewer also caught a real test defect: comparing SymbolicExpressions
    with == builds a truthy Comparator, so 9 assertions in test_branch_semantics.py asserted
    nothing about which expression was returned -- proven by making the expected value
    deliberately wrong and watching them pass. They now compare _id_, and three fresh mutants
    (swapped Refinement branch order, Next repeating one child, Alternative dropping a side)
    each fail exactly one test. Sweep unchanged against the 19e387a9 baseline: 109 failed/933
    passed, 264 failed+errored ids byte-for-byte identical. AGENTS.md rule for
    SubClassSafeGeneric opened separately as PR #148 off main (draft), deliberately not
    migrating PhraseRule.construct or the SpecificityRule families.
    2026-08-09: developer asked whether UNSET can be removed in favour of ... (Ellipsis), to
    conform with underspecified statements. Answer: yes -- and my first answer was wrong. I
    argued a sentinel over Any must sit outside the value space; EQL deliberately does the
    opposite. CountRange (operators/aggregators.py:234,268) already counts `value is ...` over
    evaluated results and domains, widening an int to a closed SimpleInterval, so ... in a
    *result* already means "not yet determined" -- exactly UNSET''s meaning. Every other
    Ellipsis check in krrood/src reads the query template, never a case instance
    (backends.py:181,198, parameterizer.py:160-170,237, query/match.py:462-463,
    planner.py:199), so a case attribute holding ... confuses nothing; type(Ellipsis) is
    already in ormatic leaf_types so it persists; both are singletons compared with `is`, so
    every site substitutes unchanged. The unifying reading is the developer''s: ... means "an
    oracle must supply this value", and rdr/backend.py is the RDR playing the role
    ProbabilisticBackend plays for a model. One open narrowing, deliberately not guessed at:
    parameterizer.py:167 raises InvalidEllipsis for a field whose type is outside
    random_events compatible_types, while ConclusionDomain supports str/arbitrary-class
    conclusions -- so an unclassified case with a str conclusion could hit it IF such a case
    can reach the parameterizer. Reachability is a probe for the slice that has consumers.
    Landed on #41 as efc8a0679: rdr/utils.py deleted outright, because UNSET had ZERO
    consumers here (git grep matched only utils.py; nothing imported rdr.utils) -- all ~40
    sites live on d-core-expert and above. This also cleared the catch-all `utils` filename
    AGENTS.md forbids. The same commit restored query_graph.pdf to main''s blob. Verified
    45/45 test_eql_rdr and a test_eql+test_eql_rdr sweep of 207 failed/982 passed on both
    sides with 214 failed+errored ids byte-for-byte identical (3 files excluded for the
    documented probabilistic_model relational.rspn gap; this container could not load the root
    conftest at all -- giskardpy_bullet_bindings -- so the sweep ran with --confcutdir and CI
    stays the load-bearing check). PR converted back to draft, fourth time. The
    query_graph.pdf thread (PRRT_kwDOQhJw3c6Xg0sj) was replied to but deliberately LEFT OPEN:
    its ask had two halves and only the revert was done -- untracking the generated PDFs is a
    main-level fix. Root-caused it: QueryGraph.render defaults to filename="query_graph.pdf"
    in the working directory (query_graph.py:160), test_rendering.py exercises it, and the
    file is both gitignored (.gitignore:152) and tracked, so any local suite run dirties it.
    Reproduced accidentally -- the verification sweep also dirtied drawer_explanation.pdf
    (.gitignore:154), so it is at least two files. Still to do, on d-core-expert and above:
    the is UNSET -> is ... substitution, the parameterizer reachability probe, and fixing
    single_class.classify, annotated -> Optional[Any] with "or None if no rule fires" while it
    returns the sentinel (observer.py:132-137) -- the #68 review''s classify()->UNSET decision
    was implemented in the observer and the signature never followed.
    2026-08-09 (closed): the developer marked #41 ready for review at 13:20Z, which under the
    standing convention ends the working session''s job on it. CI green 20/20 on efc8a0679
    (the earlier total_count:0 reading was simply taken before the run queued -- it is NOT
    another instance of the #98 CI-trigger problem in Sec.14). draft: false, mergeable_state:
    clean, 11 files / +1,946. Session unsubscribed from #41 activity and armed no check-in.
    Two review threads remain open by design: the branch-semantics family (Sec.16, the
    developer''s to close) and query_graph.pdf (half-answered -- the revert is done, untracking
    the generated PDFs is a main-level fix, offered but not taken). #41 is ready for the
    steward to merge as the stack bottom; the next pass should cascade it through
    #63-#67/#98.'
- id: D-core-aid
  title: 'RDR engine: ConclusionAid (expert-labelling assistance)'
  branch: D-core-aid
  pull_request_number: 63
  track: S0-steward
  depends_on:
  - rdr-backward-inference
  status: in_progress
- id: D-core-underspecified
  title: 'RDR engine: underspecified-match adapter'
  branch: D-core-underspecified
  pull_request_number: 64
  track: S0-steward
  depends_on:
  - D-core-aid
  status: in_progress
- id: D-core-corner-case
  title: 'RDR engine: corner-case provenance store'
  branch: D-core-corner-case
  pull_request_number: 65
  track: S0-steward
  depends_on:
  - D-core-underspecified
  status: in_progress
- id: D-core-serialization
  title: 'RDR engine: rule-tree unparser (rdr_to_python)'
  branch: D-core-serialization
  pull_request_number: 66
  track: S0-steward
  depends_on:
  - D-core-corner-case
  status: in_progress
  notes: 'Restack conflict (see 2026-07-31 blocked note, superseded) resolved by a
    /plan-item-resolve session: merged origin/D-core-corner-case into
    D-core-serialization, resolving the two files the restacking bot flagged
    (function_case.py, object_to_source.py) - both were the same rename-vs-independent-edit
    conflict (this PR renamed type_hints.py -> object_to_source.py; the
    code-generation-extract cascade kept evolving type_hints.py under its old name).
    Pushed as commit 5f897c27. CI then caught a third file the merge missed:
    template_file_creator.py imports stringify_type_hint from the now-deleted
    type_hints module directly and was never touched by either side''s diff, so
    git''s merge never flagged it - broke every CI job via the ripple_down_rules
    import chain. Fixed and pushed as commit 2577a2e3. Verified locally before
    each push: test_code_generation (98/98), test_eql_rdr (57/59), and
    test_ripple_down_rules (all but 2, both pre-existing) - failures are the
    same pre-existing, unrelated probabilistic_model.probabilistic_circuit.relational.rspn
    PyPI/dev-version gap and a missing `dot` (graphviz) binary in this sandbox,
    neither in files this PR touches. Full account in roadmap.md''s 2026-07-31
    addendum (§9).'
- id: D-core-support
  title: 'RDR engine: observer, rule-tree view, and remaining support modules'
  branch: D-core-support
  pull_request_number: 67
  track: S0-steward
  depends_on:
  - D-core-serialization
  status: in_progress
  notes: 'Investigated by a /plan-item-resolve session 2026-07-31; not yet actioned. Three findings.
    (1) The branch (8eb7518, 2026-07-19) is 2 commits behind its base D-core-serialization (2577a2e3)
    — exactly §9''s two fixes — so its green 18/18 CI predates the current base and nothing has built
    #67 on it. A local `git merge-tree --write-tree` of the two is conflict-free and the merged tree
    has zero stale code_generation.type_hints references (#67 touches no code_generation file), so the
    restack is mechanically safe; it just has to happen and be re-verified. (2) 2 of 86 review threads
    are unresolved and both wait on the developer, not on code — rule_tree_view.py:255
    (enforce_parent_consistency "code smell"; resolved as keep-and-point-at the façade fix, see §10)
    and test_conclusion_domain.py (the AGENTS.md half; resolved as covered by main''s existing
    "narrate the review or implementation history" rule). (3) #67 adds a new
    `current_result.is_condition_false` call site at base_expressions.py:410, and eql-truth-unification
    (#99) deletes that accessor — #99''s coordination note lists #89/#90/#92 but not #67. Also: #67''s
    description is stale (claims a conftest.py and an __init__.py export block absent from the current
    16-file diff).'
- id: D-core-engine
  title: 'RDR engine: EQLSingleClassRDR core inference engine'
  branch: D-core-engine
  pull_request_number: 68
  track: S0-steward
  depends_on:
  - D-core-support
  status: deferred
  notes: 'Superseded by the topic split (d-core-expert -> d-core-single-class -> d-core-backend) after
    the PR #68 review asked to split the mega-slice. PR #68 closed once the split PRs open.'
- id: d-core-expert
  title: 'RDR engine: Expert policy split'
  branch: D-core-expert
  pull_request_number: 98
  track: S0-steward
  depends_on:
  - D-core-support
  status: in_progress
  session: https://claude.ai/code/session_01Gid5Qw9cbedssFEqojo1SK
  notes: 'expert.py + its tests; DataclassException hierarchy, validators on ConclusionDomain, AnswerName
    enum. Done and pushed as PR #98 (draft): AnswerName is a StrEnum (not str+Enum);
    CaseContext is now built by the caller, not by Expert, per roadmap.md''s locked
    decision; interface.py''s AnswerRequest.validate/ExpertInterface.interact also
    retyped to carry the exception itself (touches D-core-support''s file, confirmed
    with the developer). test_ask_for_rule.py from the D-core-engine mega-branch is
    actually an EQLSingleClassRDR integration test in disguise and was not ported -
    flagged for d-core-single-class to pick up instead. 2026-08-03: d-core-single-class
    planning handed four more items back to this PR, reported at PR #98 comment 5156702002 -
    ConditionResolver.resolve taking CaseContext (4 definitions in condition_resolver.py
    plus test_condition_resolver.py''s 10 call sites), Null-Object defaults for progress
    and save, and a ProgressDescription StrEnum replacing the _FITTING_DESCRIPTION global.
    All target files already on D-core-expert, so they belong to this PR''s parameter-object
    topic. Revised the same day (comment 5163364467): the fourth item was originally a yes/no
    on promoting save() / make_progress_reporter() from ExpertInterface onto Expert; it is
    now a proposal to segregate ExpertInterface instead, moving progress and save onto the
    RDR as its own collaborators and leaving only the expert Q&A surface on the interface.
    That subsumes the Null-Object item - the defaults attach to the RDR''s collaborators
    rather than to ExpertInterface. Larger ask of this PR than the original framing, and it
    has an integration cost D-ui (#76) absorbs, so it needs the developer''s call.
    2026-08-06: developer chose to segregate ExpertInterface. All four items implemented
    and pushed as 28a89ff4. ConditionResolver.resolve takes CaseContext across all four
    definitions, reading the firing anchor off context.trace (absent trace and absent
    anchor both mean "no active path", both pinned). ExpertInterface lost on_save/save()/
    make_progress_reporter(); ModelSaver (NullModelSaver + FileModelSaver over the existing
    save_rdr_with_case) landed in serialization.py beside the function it wraps rather than
    in a new module, and NullProgressReporter in progress.py; the RDR holds both, which
    deletes the mega-branch''s expert.interface.on_save reach-through. ProgressDescription
    StrEnum replaces _FITTING_DESCRIPTION. Verified before/after rather than on absolute
    counts (this container lacks the probabilistic_model rspn submodule): test_eql_rdr
    6 failed/113 passed before, 6 failed/125 passed after - identical failure set, +12 exactly
    the new tests; test_eql failure set byte-for-byte identical (211 entries) either side.
    format_docstrings.py churn reverted in serialization.py (~180 unrelated lines) and
    test_serialization.py, kept where the PR substantially rewrites the file - same call
    and reason as #41''s backward_inference.py. D-ui (#76) told what it must absorb at
    comment 5201827158 (interactive.py:231/335/440, magics.py:171).
    Still true: no CI run has ever been queued for this PR since b772e959 (2026-07-30).
    The 28a89ff4 push did not trigger one either, so "push a commit to get a baseline" is
    not the fix - see roadmap.md section 14.'
- id: d-core-single-class
  title: 'RDR engine: EQLSingleClassRDR core inference engine'
  branch: D-core-single-class
  pull_request_number: null
  track: S0-steward
  depends_on:
  - d-core-expert
  status: not_started
  notes: 'single_class.py + engine tests; classify->UNSET, raise+drop max_passes, CaseContext object.
    2026-08-03: implementation plan approved and saved to
    .claude/personal/pr-progress/D-core-single-class.md - not started, no branch cut. Scope
    narrowed: the Null-Object progress/save work, the ConditionResolver CaseContext change and
    the ProgressDescription enum were handed to d-core-expert (#98), since every target file
    already exists on D-core-expert; this item consumes them rather than building them. What
    stays: single_class.py itself, RDRDidNotConvergeError + the expert-required exception in
    rdr/exceptions.py, and the six engine test files ported from mega-branch e650d968. Before
    starting, get a CI or local-test baseline on D-core-expert (#98 has no CI runs). Do not
    cascade the stack first - it is stale but merge-tree-clean and disjoint from this item''s
    files.'
- id: d-core-backend
  title: 'RDR engine: underspecified-query RDR backend'
  branch: D-core-backend
  pull_request_number: null
  track: S0-steward
  depends_on:
  - d-core-single-class
  status: not_started
  notes: backend.py + its test; infer/fill split, typed ModelKey, key_from_attribute -> helpers.py. New
    stack tip.
- id: rdr-engine-umbrella
  title: RDR/EQL engine umbrella (superseded by the split stack)
  branch: rdr-engine
  pull_request_number: 38
  track: S0-steward
  depends_on:
  - eql-core-prep
  status: deferred
  notes: 'Closed unmerged once the remainder was split into #58..#68 above.'
- id: conditions-root-drop-dead-parent-recovery
  title: Drop the dead _last_parent_of_type_ conditions-root recovery
  branch: conditions-root-drop-dead-parent-recovery
  pull_request_number: 89
  track: S0-steward
  depends_on: []
  status: done
  notes: 'Side-quest off main, not part of the original wave plan: removes _conditions_root_''s dead fallback
    (distinct from insert_at''s own live use of the same method name, independently reintroduced by #78
    for a real bug — the two were verified fully compatible). Unblocks rdr-backward-inference''s (#41)
    merge conflict once merged; verify the restacking bot cascades the fix through #58/#39/#53/#41 afterward.

    '
- id: D-ui-splice-fix
  title: 'RDR engine: fix rule-tree splice for attributes reused across sibling branches'
  branch: D-ui-splice-fix
  pull_request_number: 78
  track: S1-d-ui
  depends_on:
  - d-core-backend
  status: deferred
  session: https://claude.ai/code/session_01B3Ji1kNxRinif4y1dD2xPy
  notes: 'bug label. CLOSED 2026-07-31 as superseded by #118, which landed the same fix at the façade
    level on main. Its regression test was not carried over: it lives in test/krrood_test/test_eql_rdr/
    (absent on main) and asserts through walk_rules/classify_case, so #118 covers the same defect
    DSL-only at the accessor''s own contract level instead. The RDR-level test can be re-added against
    the fixed API once test_eql_rdr/ lands — no production change needed for it to pass. Reintroduced
    _last_parent_of_type_ for insert_at''s anchor-parent recovery — a real,
    proven bug distinct from PR #89''s target. SUPERSEDED 2026-07-31 by dag-facade-hardening''s
    insert-at-ownership-parentage item: the same bug is fixed there at the façade level, from the
    owning rule-tree context rather than by reintroducing _last_parent_of_type_ (a symbol #89 deleted
    from main, and a structural-accessor read that dag-facade-hardening''s Wave-1 guard test forbids
    and its rename would break). This PR''s own scope note — "insert_at does not exist on main yet" —
    is stale: rules/conclusion_selector.py landed on main, so the fix needs none of the Wave-0 stack.
    See roadmap.md §10.'
- id: D-ui-rendering
  title: 'RDR engine: case-table rendering + serialization test coverage'
  branch: D-ui-rendering
  pull_request_number: 79
  track: S1-d-ui
  depends_on:
  - D-ui-splice-fix
  status: in_progress
  session: https://claude.ai/code/session_01B3Ji1kNxRinif4y1dD2xPy
  notes: 'Still based on the D-ui-splice-fix branch, whose PR (#78) was closed as superseded on
    2026-07-31. The branch still exists so nothing is broken today, but #79 can no longer land through
    #78 and must be re-targeted onto D-core-engine before it can merge. Not re-targeted here — that is
    this PR''s own session''s call. Its depends_on is kept pointing at the closed item so the stack''s
    real shape stays visible until the re-target happens.'
- id: D-ui
  title: 'RDR engine: interactive expert interface (IPython shell, magics, prompt sections/examples)'
  branch: D-ui
  pull_request_number: 76
  track: S1-d-ui
  depends_on:
  - D-ui-rendering
  status: in_progress
  session: https://claude.ai/code/session_01B3Ji1kNxRinif4y1dD2xPy
- id: D-store
  title: 'D-store: RDRFileStore for the @rdr model-file lifecycle'
  branch: D-store
  pull_request_number: 80
  track: S2-d-deco
  depends_on:
  - D-ui
  status: in_progress
  notes: Path resolution + model-file lifecycle; standalone, no decorator dependency.
- id: D-deco
  title: 'D-deco: @rdr decorator + RDR file store (final rdr-engine slice)'
  branch: D-deco
  pull_request_number: 77
  track: S2-d-deco
  depends_on:
  - D-store
  status: in_progress
  notes: rdr() factory + RDRWrapper, retargeted from D-ui base to D-store.
- id: rdr-architecture-brief
  title: Re-land RDR architecture brief and its bibliography as a docs-only page
  branch: rdr/architecture-brief
  pull_request_number: null
  track: docs-final
  depends_on:
  - rdr-oo-definitions
  - rdr-justifications
  status: deferred
  notes: 'Repositioned 2026-07-24 from S3-docs/wave-0 to the final wave: the doc describes the
    architecture as a finished whole, so it reads better landed once the engine is actually stable
    (Wave 3 done) rather than mid-refactor. PR #75 (which re-landed closed PR #20''s content; CI
    was green, mergeable clean) was closed for now; branch rdr/architecture-brief is kept so the
    content can be re-landed without redoing the work. depends_on is a best-guess placeholder for
    "engine stable" (Wave 3''s two tips) — revisit the exact edge when this item is picked up.'
- id: rdr-oo-recognition
  title: 'OO recognition layer: candidate generators + definitions'
  branch: rdr/oo-recognition
  pull_request_number: 21
  track: oo-prototypes
  depends_on: []
  status: deferred
  notes: Prototype input for Wave 3's rdr/oo-definitions, then close. Base is stale (rdr/oo-plan), do
    not merge as-is.
- id: rdr-backend-unification
  title: Unify recognition with the query/reasoning backend frontend
  branch: rdr/backend-unification
  pull_request_number: 22
  track: oo-prototypes
  depends_on:
  - rdr-oo-recognition
  status: deferred
  notes: 'Same disposition as #21: harvested by Wave 3, then closed.'
- id: rdr-why-answer
  title: 'Why-question core: WhyQuestion/WhyAnswer, EQLSingleClassRDR.why(case)'
  branch: claude/rdr-why-answer-6fnw2o
  pull_request_number: 81
  track: why-track
  depends_on:
  - d-core-backend
  status: in_progress
  session: https://claude.ai/code/session_01V2DJMF1C2bQjhLcPo5sFkx
  notes: 'W1. KNOWN-STALE per-branch progress note: pr-progress/rdr/why-answer.md still says "Not started"
    even though this PR is real, open, and W2/W3 already build on it — the exact drift class this system
    exists to catch. This entry reflects the verified live state, not that note.

    '
- id: eql-causal-verbalization
  title: '"because" vocabulary + causal grammar assembler, WhyAnswer.verbalize()'
  branch: eql/causal-verbalization
  pull_request_number: 82
  track: why-track
  depends_on:
  - rdr-why-answer
  status: in_progress
  notes: 'W2. Deep in review: 20/22 threads resolved, 2 blocked on PR #83''s boolean-predicate goldens.'
- id: eql-attribute-predicate-verbalization
  title: Verbalize boolean-attribute conditions as declarable predicates
  branch: eql/attribute-predicate-verbalization
  pull_request_number: 83
  track: why-track
  depends_on: []
  status: done
  session: https://claude.ai/code/session_01JCEFrP4eBx2pgmf5FarFLm
  notes: 'Merged 2026-07-21. Sibling fix, not part of the original wave plan; blocked W2''s last 2 open
    review threads until it landed. Was open as non-draft (ready for review) before merging — same as
    several of the S0 stack items (#58/#39/#53/#41/#63-#67), so not unique to this PR; each was presumably
    marked ready deliberately once stable, per the always-drafts-until-ready personal convention.

    '
- id: rdr-why-query-surface
  title: why(...) EQL factory + docs + bibliography
  branch: rdr/why-query-surface
  pull_request_number: 84
  track: why-track
  depends_on:
  - eql-causal-verbalization
  status: in_progress
  session: https://claude.ai/code/session_0148J38dvujaob3AYJqfNzwa
  notes: W3. %why magic deferred until D-ui (#76) lands.
- id: rdr-decision-queries
  title: Backend explanation semantics + the decision-query pattern
  branch: rdr/decision-queries
  pull_request_number: 85
  track: why-track
  depends_on:
  - eql-causal-verbalization
  status: in_progress
  session: https://claude.ai/code/session_013NepUGgubeWHnLT5TUPLdu
  notes: C1. Reframed 2026-07-17 (ExplainableChoice protocol dropped as YAGNI). Coordinates with W3 via
    a shared ExplanationCarrier seam.
- id: montessori-choice-policies
  title: 'Demo policy wiring: pick/hole policy RDRs as decision queries'
  branch: montessori/choice-policies
  pull_request_number: null
  track: montessori-track
  depends_on:
  - rdr-decision-queries
  status: deferred
  blockers:
  - Deferred until Tom's montessori_ijcai branch is ready.
  notes: M1. The generic half (decision-query semantics) already proceeds as C1; only the demo-specific
    remainder is left here.
- id: montessori-why-demo
  title: Narrated demo loop, headless CI mode, README
  branch: montessori/why-demo
  pull_request_number: null
  track: montessori-track
  depends_on:
  - montessori-choice-policies
  - eql-causal-verbalization
  status: deferred
  notes: M2.
- id: rdr-feature-registry
  title: Feature layer core, the "poor man's Rete"
  branch: rdr/feature-registry
  pull_request_number: null
  track: track-f
  depends_on:
  - d-core-backend
  status: blocked
  blockers:
  - Blocked until the Wave-0 stack lands on main.
  notes: Registry + CaseView memoization + EQL name resolution.
- id: rdr-feature-capture
  title: KA-time feature capture
  branch: rdr/feature-capture
  pull_request_number: null
  track: track-f
  depends_on:
  - rdr-feature-registry
  status: not_started
  notes: '%feature magic, AST-hash dedup, registry autocomplete.'
- id: rdr-multi-class
  title: 'MCRDR port: multi-valued attributes'
  branch: rdr/multi-class
  pull_request_number: null
  track: track-g
  depends_on:
  - d-core-backend
  status: blocked
  blockers:
  - Blocked until the Wave-0 stack lands on main.
  notes: Lifts the single-valued guard in underspecified.py.
- id: rdr-general-fixpoint
  title: GRDR naive fixpoint engine
  branch: rdr/general-fixpoint
  pull_request_number: null
  track: track-g
  depends_on:
  - rdr-multi-class
  status: not_started
  notes: Monotonic-conclusions contract + recorded dependency edges.
- id: eql-truth-unification
  title: Unified OperationResult truth
  branch: claude/eql-truth-unification-refactor-pfux7f
  pull_request_number: 99
  track: track-t
  depends_on:
  - eql-core-prep
  status: done
  session: https://claude.ai/code/session_01YTSdjBA54dAdBxsCujdjTX
  notes: 'Implemented and pushed as PR #99 (draft, off main). The OperationResult.is_false field is dropped;
    is_false reads bindings[operand._id_] directly and is_condition_false is gone. Branch is the session''s
    designated claude/... branch, not the originally planned eql/truth-unification. Two deliberate divergences
    from the design doc, which predates the current code: (1) the invariant needed qualifying — an expression
    has one binding, and for Entity/aggregators/arithmetic that binding is the selected value, so taken
    verbatim the doc dropped query results whose value was 0 or empty; a new TruthValuedExpression tells
    truth-binding from value-binding expressions apart and only truth-recording roots are filtered by truth.
    (2) The _is_false_flag transition was rejected — removing the field outright makes a missed call site
    fail loudly instead of silently compiling. _evaluate_child_as_condition_ was kept (now a pass-through)
    so PRs #90/#92 keep their recording hook. Design doc landed with the PR, rewritten to match.

    2026-08-03: a /plan-item-resolve session found and fixed the real blocker — mergeable_state had gone
    dirty (restack conflicts in base_expressions.py, evaluation.py, test_explanation.py, recurring since
    2026-07-30 across 3 failed bot restack attempts). Root cause: PR #89 (conditions-root-drop-dead-parent-recovery)
    merged to main first and independently added a _true_results_() method plus reworked
    SatisfiedConditionTracker.on_conclusions_processed — the same two things #99 changes — without knowledge
    of #99''s TruthValuedExpression fix (#99''s own coordination note named #89/#90/#92 but was written before
    #89''s actual content existed). Resolved: _true_results_() kept as the named method (main already uses
    it elsewhere) but given #99''s TruthValuedExpression guard; on_conclusions_processed kept #99''s simplified
    uniform-bindings-lookup body but wired onto main''s newer, already-tested active_conditions_root.has_condition
    guard rather than this branch''s own expression._conditions_root_ is expression._root_ check; the duplicate
    test helper _get_true_results consolidated to delegate to the fixed method. Verified: test/krrood_test/test_eql
    before/after the merge has byte-for-byte identical failed+error sets (61 failed / 78 errors both sides,
    all tracing to a pre-existing random_events packaging gap in the sandbox, unrelated to this change) with
    +8 passed from main''s own growth; every test directly exercising the touched logic passes (test_evaluation_context.py
    10/10, the satisfied-condition/condition-graph family in test_explanation.py 11/11, test_operation_result_truth.py
    26/26 excluding the one environment-blocked test). Pushed as 857fb74f, PR converted back to draft per the
    always-drafts-until-ready convention pending CI. Also resolved the 4 review threads left open from the
    2026-07-30 round (base_expressions.py:376,394,907,1183) — each already had a full analysis with no code
    change proposed, just awaiting sign-off; replied and resolved all four.

    '
- id: rdr-concept-trees
  title: NRDR concept layer
  branch: rdr/concept-trees
  pull_request_number: null
  track: concept-trees-track
  depends_on:
  - rdr-feature-registry
  - rdr-general-fixpoint
  status: blocked
  blockers:
  - Blocked on Track F (rdr/feature-registry) and Track G (rdr/general-fixpoint).
  notes: 'Concept declaration, explicit DependencyGraph, stratified order, semi-naive firing, cornerstone-regression
    gate on shared-feature/ concept edits. Likely splits into 2-3 stacked PRs at execution time; this
    item is the umbrella placeholder.

    '
- id: rdr-oo-definitions
  title: 'OO integration: definitions, candidate generators, taxonomy discriminators'
  branch: rdr/oo-definitions
  pull_request_number: null
  track: oo-definitions-track
  depends_on:
  - rdr-concept-trees
  status: blocked
  blockers:
  - Blocked on rdr/concept-trees.
  notes: 'Harvests prototypes #21/#22 (rdr-oo-recognition, rdr-backend-unification), then closes both.'
- id: rdr-justifications
  title: TMS (JTMS) justification recording
  branch: rdr/justifications
  pull_request_number: null
  track: justifications-track
  depends_on:
  - rdr-concept-trees
  - eql-truth-unification
  status: blocked
  blockers:
  - Blocked on eql/truth-unification (single truth source) and rdr/general-fixpoint (dependency graph
    + blackboard).
  notes: Justification value object + JustificationRecorder(EvaluationObserver) + retraction propagation
    over the dependency graph.
- id: expert-capability-guards
  title: 'Capability-guarded Expert: EQL eligibility guards, resolver-as-expert'
  branch: null
  pull_request_number: null
  track: expert-capabilities
  depends_on:
  - d-core-single-class
  status: blocked
  blockers:
  - Blocked until the Wave-0 engine stack lands on main.
  notes: 'Abstract Expert declares full question vocabulary; each impl carries an evaluable EQL capability
    guard. Lit: blackboard activation conditions, Contract-Net, Chain-of-Responsibility.'
- id: expert-ensembles
  title: 'Cooperating experts: Hint mode as composition, multi-expert ensembles'
  branch: null
  pull_request_number: null
  track: expert-capabilities
  depends_on:
  - expert-capability-guards
  status: blocked
  blockers:
  - Blocked on expert-capability-guards.
  notes: 'Multiple experts assisting each other. Lit: mixed-initiative / critiquing systems.'
- id: expert-capability-verbalization
  title: 'Verbalize expert capabilities: why-can / why-cannot handle a situation'
  branch: null
  pull_request_number: null
  track: expert-capabilities
  depends_on:
  - expert-capability-guards
  - rdr-why-answer
  status: blocked
  blockers:
  - Blocked on expert-capability-guards and W1 (rdr-why-answer).
  notes: The guard is EQL, so it is inspectable and verbalizable. Ties to the Why track.
- id: explanation-rendering-by-audience
  title: 'Explanation rendering by recipient: NL for humans, EQL for programs'
  branch: null
  pull_request_number: null
  track: comms-track
  depends_on:
  - eql-causal-verbalization
  status: blocked
  blockers:
  - Blocked on W2 (eql-causal-verbalization).
  notes: 'Separate explanation content from rendering, chosen from the recipient model. Lit: user-tailored
    NLG (Paris), Reiter & Dale, Grice, Sperber & Wilson.'
- id: no-rule-fired-resolution
  title: 'No-rule-fired handling: warn / ask-expert / default-conclusion modes + provenance'
  branch: null
  pull_request_number: null
  track: engine-runtime
  depends_on:
  - d-core-single-class
  status: blocked
  blockers:
  - Blocked until the Wave-0 engine stack lands on main.
  notes: 'Beyond interim classify->UNSET. Conclusion provenance coordinates with Wave-3 JTMS
    (rdr-justifications).
    2026-08-09: the sentinel itself is being unified with ... (Ellipsis) -- see
    rdr-backward-inference and roadmap Sec.17 -- so this item now inherits "no conclusion" being
    spelled the same way as "generate one". Two consequences it owns. (1) Expert delegation:
    once ... is the sentinel, an expert typing `conclusion = ...` in the shell is asking the
    generative backend to supply the value, but make_conclusion_validator (rdr/expert.py:94)
    currently rejects that as "No rule fired for this case" unless allow_unset. Routing it to a
    backend instead is a capability the engine does not have, and it belongs here rather than
    on any D-core slice. (2) The parameterizer narrowing: parameterizer.py:167 raises
    InvalidEllipsis outside random_events compatible_types, so a non-enumerable conclusion
    (str/arbitrary class) left unclassified may be unrepresentable as ... -- if reachable, the
    no-rule-fired modes are where the answer lives.'
