## integration block defects (branch claude/integration-block-defects-zrdua3) - FOLDED

Finished: #249 merged into #211's branch (claude/plan-item-kickoff-workflow-unification-wg4w4x)
as merge 287a431d7 on the user's word. Three commits: 3d9fba88d (block record, readmission,
lifting, blocked-without-record, skill/README), 020d7ed09 (clear-fixed-breaks ClearingReport),
9b02dbf98 (review round: GitCommandRunner.remote_branch_heads / write_remote_references +
ReferenceUpdate; BlockRecords.forget_lifted drops records of lifted blocks when the stack is
read). Both review threads replied to and resolved; review question answered on #249; #211's
description carries a fold section. Full suite 1013 passed (from 963).

#77: carried no integration-conflict label by the time it was checked, so nothing to remove.

Left for #211's tree: PassedChecks.record still pushes raw (its test double would have had to
change); it can adopt write_remote_references in one line.

Nothing armed, nothing subscribed. This branch's job is over.
