Throwaway file for the `session-safe-pr-reparent` base-change credential probe.
This branch and its pull request exist only to answer one question: can a Claude
Code session change a pull request's base branch through the GitHub MCP server,
given that a raw PATCH through the session git proxy returns 403?
