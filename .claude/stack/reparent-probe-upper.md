Throwaway upper layer for the `session-safe-pr-reparent` probe. A native GitHub
stack requires at least two pull requests, so this branch exists only so the
probe stack can be created and the stack-member base-change question answered.
